# Planning-package verification

Date: 2026-09-20. Product source baseline: `6f6222b74e4f552ce697e61364cf653f4b6be29f`. This record verifies the planning deliverable, not Cockpit runtime behavior. All 25 required execution tasks remain pending; the 2 deferred provider tasks remain deferred.

## Structural checks performed

An in-memory Python validation parsed the actual JSON ledger and Markdown files and asserted:

- 27 unique task IDs, 25 required and 2 deferred.
- One correctly titled brief per task, with the seven required sections exactly once.
- Numbered Changes/Acceptance sections have consecutive numbering.
- Dependencies resolve, have no self-edges/cycles, and required work does not depend on deferred scope.
- ACCEPT-01 transitively covers all 24 other required tasks.
- All relative Markdown links and the referenced local acceptance anchor resolve.
- All 10 open issue numbers have required-task coverage; the three closed regression anchors remain represented.
- A01–A25 each have an acceptance-map row, with A23 explicitly excluded rather than missing or claimed.
- Initial status/owner/evidence/commit fields do not fabricate completion.

The checks passed. A scheduling simulation returned only RUN-01 initially; after RUN-01, TERM-01, WEB-07 and NATIVE-01 become dependency-ready. This does not bypass file locks or external prerequisites.

The two documented jq queries were executed against the actual ledger: the status query listed all 27 records, and the dependency-ready query returned RUN-01 only. No permanent validation framework or product tests were added for this documentation task.

## Read-only review and repairs

An independent execution-plan review found no blocking dependency, ownership, completion-hash, fixture-permission or platform-proof issue.

An independent coverage review found two acceptance defects:

1. WEB-02 allowed an initial Element pick to remain at a readiness message instead of succeeding on supported content. Outcome, changes, acceptance and evidence now require successful first annotation without a prior Browse gesture; explicit refusals are negative cases only.
2. GLAB-04 unconditionally prohibited all remote writes while also requiring authorized fixture updates to prove refresh. Acceptance now forbids production-adapter and unauthorized writes while allowing separately authorized, ledger-recorded fixture mutations.

The reviewer reread those repairs and the revised setup/GitLab/browser-durability contracts and reported no remaining concrete blocker in that scope.

Integration review also tightened setup to existing plan/receipt/cache semantics rather than a new operation framework; kept the portable publication repair to Linux/macOS; required unsent-work preservation rather than ordinary-close data loss; distinguished runtime owner from input controller; retained GitLab's verified canonical work-item URL; and included labels/assignees/approval metadata and fork provenance without coupling MR source import to a locally available branch.

## Delivery boundary

Only files under this campaign directory belong to the planning commit. Application code, existing authority documents, installed applications, Herdr sessions and remote tracker state were not changed by this planning-package task. The previously authorized GitLab fixture issue #1 remains available for execution; no new remote fixture was created here.

No application build, test suite, browser/native smoke, or product repair is claimed. Required runtime evidence will be generated by the future task execution under `runs/`. Staged whitespace/scope checks and the resulting planning commit are the final repository delivery gate.

## Autonomous-launch setup verification

The follow-on setup adds `AUTORUN.md` and the read-only `campaign.py` gate, integrates the one-line native goal trigger into the README, and adds bounded increment receipts to the existing evidence template. It does not execute any campaign task.

Actual environment checks: `omp --version` returned `omp/18.2.6`; `omp config get goal.enabled` and `omp config get compaction.enabled` both returned `true`. The native `/goal <objective>` dispatch and continuation contract were inspected in that version's upstream implementation. The campaign goal itself was deliberately not submitted: the user requested preparation only. No custom slash-command discovery, plugin reload, extension or daemon is required.

Actual final helper invocations from the repository:

```sh
python3 planning/stability-and-gitlab-2026-09-20/campaign.py check
python3 planning/stability-and-gitlab-2026-09-20/campaign.py ready
python3 planning/stability-and-gitlab-2026-09-20/campaign.py complete
```

Results: `check` exited 0 with 27 valid records; `ready` exited 0 with only RUN-01 eligible, 24 waiting and no active/blocked tasks; `complete` correctly exited 1 naming all 25 unfinished required tasks. The real ledger's SHA-256 was unchanged across setup verification.

A throwaway Python harness invoked the actual CLI against an isolated temporary Git repository and passed 27 scenarios:

- Dependency root readiness; successor readiness after committed evidence; all required tasks complete while deferred work remains excluded.
- Unknown dependency, cycle, deferred prerequisite, boolean schema version, vacuous required set and silently activated deferred scope rejected.
- Missing evidence, nonexistent/unreachable commits, done-with-incomplete-dependency, brief-as-evidence, traversal, dirty/empty/uncommitted/symlink evidence rejected.
- Active ownership excludes overlapping candidates; overlapping held locks rejected; blocked ownership retains locks and reasons; releasing a blocked owner retains historical start time and unblocks independent work.
- Missing blocker/active owner rejected; ready output remains a candidate list rather than a reservation; malformed JSON fails concisely and `--ledger` works after the command.

Integration review corrected the blocked-owner/start-time mismatch before those checks. The worker's bounded advisory consultation supported detail-object candidate/blocked/waiting output and conservative retained-lock behavior; actual CLI scenarios, not advisory agreement, establish the verification above.

The temporary repository, fixture evidence, symlink and unreachable commit object were removed with their owned temporary directory. No permanent smoke harness, synthetic product evidence, native session, remote fixture or product change was created. Focused Ruff formatting and lint passed; final CLI behavior was rerun after cleanup. The README and autorun trigger agree and all 80 local Markdown links resolve.

This gate validates scheduling/completion bookkeeping only. It cannot certify the truth of a runtime claim from Markdown; the future orchestrator must still exercise and review every required product acceptance scenario before completing the goal.
