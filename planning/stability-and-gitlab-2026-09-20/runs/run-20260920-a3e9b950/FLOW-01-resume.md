# FLOW-01 resume — 2026-09-24

Scope: evidence-only continuation; no shared product files, status ledger, tests/builds/formatters, or commits changed/run. This record maps the original acceptance against the prior FLOW-01 evidence and one fresh native session smoke. It does **not** claim FLOW-01 complete.

## Prior evidence map (kept intact)

`FLOW-01.md` remains the detailed record for the earlier independent runs:

- Browser local setup: plain borrowed directory, nested borrowed filesystem path, owned child worktree; actual setup/cleanup dialogs; dirty owned refusal; clean owned teardown; parent/plain/nested content hashes; child hierarchy and TUI comparison. The nested case was a directory *inside* a parent Git repository, not a linked child worktree identity. Its TUI onboarding overlay prevented an independent focus assertion.
- Browser Review: all-local, staged, unstaged, untracked, and explicit-base branch diffs with distinct markers; staged/working overlap distinguished; refresh preserved selected file and refreshed diff. A transient terminal attach failure was recorded, not counted as pass.
- Browser and native Context-to-real-Pi paste: exact same-tab selected target and payload hashes; changed-source refusal preserved saved comments and did not dispatch; explicit stale-excerpt choice preceded one accepted bracketed paste, and Enter was not sent. Native operation IDs/states, source hashes, screenshots, Herdr pane reads, TUI snapshot and owned teardown are recorded in the previous file. This did not test lost-response reconciliation.
- Browser Review line and whole-file comment drafts survived scope switch, reload and changed source. The prose-only save false-current bug was reproduced, fixed in a prior owned implementation increment, and browser response/UI verified; native Review comment-edit parity failed twice with WebDriver `invalid session id` and remains unproven.
- The prior record explicitly left source/Markdown/Mermaid/media/search breadth, source import, lost paste response, process/session restart, and native Review parity open.

BATCH-FINAL at lines 100–103 adds a distinct current-batch Chromium A→B→A annotation retention/capture path, a native exact no-Enter paste to an explicit disposable PTY fixture, and partial security/sustained-native evidence. Those are not substitutes for missing FLOW-01 source/Review/lifecycle criteria. Its native browser journey timed out before nested wheel; the browser journey's feedback was rejected because that independent fixture had no eligible agent, not a successful agent paste.

## Fresh native surface smoke

Ran `python3 scripts/verify/native_browser_acceptance.py --skip-build --native-only-open --timeout 30` using only the settled binaries/dist. It created root `/tmp/cnative-hitxosst`, isolated HOME/XDG/config and Herdr session `native-aa575202984b`, Space `w1` (`Native browser 9d164e8a`), fixture `http://127.0.0.1:38527/`, and private native WebKit/Tauri compositor. This was an actual native UI open/navigation/render/input journey for the built-in browser pane, **not** Context or Review.

Observed native Tauri/WebKit at 676×835 CSS, DPR 2; its browser pane opened the fixture from the native navigation field, reached `Live browser view`, controlled its private view, and painted 362 canvas frames over 6.006 s (60.28 fps). The actual remote fixture received trusted CDP pointer down/up and scrolled from 0 to 3900 CSS px; final painted header marker was RGB `[0,167,75]`. A real WebKit screenshot is retained below. Harness explicitly reports `image_quality.acceptance=not_evaluated`; observed canvas was 674×188 at DPR 2 (about 1 bitmap pixel/CSS pixel), not an image-quality acceptance claim.

Owned child PIDs and harness exit status: WebKitWebDriver `3643190` exit `-15` (expected termination), private Niri `3643143` exit 0, gateway `3643114` exit 0, Herdr `3643078` exit 0. Follow-up `ps -p` showed none of those PIDs; the named private Herdr socket was absent; `ss -ltnp` showed no listener on port 5173 or this run's fixture/driver/gateway endpoints. Removed only `/tmp/cnative-hitxosst`. The harness's private root auto-selected its fixture/gateway/driver ports; Tauri's dev frontend occupied exclusive port 5173 during the run and was stopped before release. Main and WEB-08 were notified; WEB-08 may now acquire native port 5173.

Artifact: `FLOW-01-resume-native-window.png`, 117,669 bytes, SHA-256 `5529ec7727c4033319c897869605f06731de5c2930457091fafb28f6ffe7ae60`. It is a genuine native WebKitDriver screenshot of the browser panel/fixture, not a Context/Review capture.

## Fresh browser Context, cancellation, and restart proof

This owned browser-only run used `/tmp/cflow-resume-6IevT9`, isolated session `flowresume95b88c53`, gateway first PID 3825616 at `127.0.0.1:35395`, then PID 3870298 at `127.0.0.1:44089`; the fixture-owned Herdr server PID was 3657334. The copied read-only `herdr-file-viewer` plugin was registered only in this fixture’s private Herdr config. Process `/proc` inspection confirmed HOME, XDG config/state/cache/data/runtime, Herdr config/socket and Cockpit config/session/socket all resolved beneath the owned root. The six protected RUN-01 HOME hashes matched their recorded values exactly.

Cockpit’s actual reviewed Setup created operation `14f90f66-39ae-4d40-955b-0e379c6d8f63`, branch `flow-resume-b88c53`, owned linked checkout `/tmp/cflow-resume-6IevT9/worktrees/repo-14f90f66-39ae-4d40-955b-0e379c6d8f63`, companion `/tmp/cflow-resume-6IevT9/companions/14f90f66-39ae-4d40-955b-0e379c6d8f63`, and child Space `w2` under `w1`. Cockpit **Open Context right** opened the actual plugin pane `w2:p3` in selected tab `w2:t2` beside terminal `w2:p2`; setup manifest recorded owned worktree+companion and completed generation 11.

Context **Import local snapshot** copied the two-file fixture from the owned task checkout (222 bytes); after adding a 48×32 PNG to the owned parent repository, a second snapshot copied three files (462 bytes). The parent source `README.md` and imported snapshot both have SHA-256 `8d8529d670fae20ba07ed967981158d04133cef20bc31fc661187e5419510797`; source and imported `preview.png` both have SHA-256 `a417afa701babf7615f595ac483fc904b9d152f5bacec354c83340ebf92f0529`. Context rendered the README Markdown and Mermaid `graph TD; A-->B` into a visible diagram (actual screenshot below); importing and rendering `preview.png` loaded an actual image element with natural size 48×32 (screenshot below). SVG was displayed as source text, not claimed as an image preview.

Context content search returned exactly the nested imported README result at original line 3; opening that result displayed its immutable snapshot path and selected source line. A controlled cancellation probe delayed the first *real UI fetch response* for 1.8 seconds (page-only instrumentation, no product change), then submitted a second unique non-matching query. Request generation 101’s first fetch signal was aborted before it could return; generation 134 completed for the newer query; old result text was absent and the search-results region was empty. This is a positive canceled-search/no-stale-result observation.

The Resources UI reported exactly: `No source provider is configured. Configure a provider before importing a source.` Therefore provider source import was unavailable in this isolated config; local snapshot import above is a different operation and is not counted as provider source import.

The private Cockpit gateway was stopped (PID 3825616, exit 0) and restarted (PID 3870298). After reloading the browser, the same session, child Space, Context pane, companion tree and README snapshot path/source bytes were still present; the manifest and companion remained on disk. This proves browser/gateway-process restart identity only, **not** Herdr server/session-process restart.

After starting a genuine isolated `pi` agent in new pane `w2:p4`, `herdr agent start flow-pi --kind pi --pane w2:p4` returned `agent: "pi"`, `agent_status: "idle"`, `interactive_ready: true`, same task checkout and selected tab `w2:t2`. Cockpit then surfaced a Recovery alert, `snapshot.agents[0].agent must be a string`; that interruption prevented comment save and same-tab target-change testing. Captured `herdr --version` was 0.9.1 and `herdr api schema --json` reported protocol 22/schema 1. Raw private `herdr api snapshot` JSON had `result.snapshot.agents[0].agent == "pi"` and `name == "flow-pi"`; HTTP GET `/api/v1/sessions/flowresume95b88c53/snapshot` returned 200 with normalized `agents[0].name == "pi"` and `status == "idle"` (no `agent` member). The parser candidate `crates/cockpit-herdr/src/cli.rs::parse_snapshot` requires raw `.agents[i].agent` at lines 331–357. This is reported to Main with both shapes; the exact source of the UI alert remains unresolved, and no unchanged retry or product edit was made.

Artifacts: `FLOW-01-resume-context-mermaid.png` (SHA-256 `236febbb68d650f4ba5f48e2f6fa31fb4ff449b62c64ed5c25db8a83d9649e5c`) visibly shows rendered Markdown+Mermaid; `FLOW-01-resume-context-media.png` (SHA-256 `ce40439962fbdf06cd88f0a9f69dddf137981d500c04de9368a6cc3392eb0955`) shows the loaded PNG image.

Cleanup: managed browser tab closed; run-owned agent pane `w2:p4` closed without submitting text; private Herdr `server stop` succeeded and socket disappeared; gateway PID 3870298 exited 0 (old PID 3825616 had exited 0 at restart). PID check and `pgrep` found no Herdr/gateway/Pi/run-root descendants; TCP ports 35395 and 44089 no longer listened. Removed only `/tmp/cflow-resume-6IevT9`; no fixture repository, checkout or companion remains. Port 5173 was released after the earlier native smoke and is currently held by the separately notified WEB-08 process, not this run.

## Fresh browser Context comment save and failure retention

A second isolated browser run used `/tmp/cflow-save-XIHHev`, session `flowsave59247bd2`, gateway PID 3939417 at `127.0.0.1:45527`, Herdr PID 3939380, and a private copied `herdr-file-viewer` plugin. Actual reviewed setup operation `f6dcc9fa-28be-45d3-8ded-26b153ad4e7e` created child Space `w2`, owned checkout, and companion; actual **Open Context right** used pane `w2:p3`. The local snapshot imported the task README into the companion; parent and copied source both had SHA-256 `124595cb394d727fb438f3e1216a6a95b4314009b57a36f7b926e68c64d7cb1d`.

In the real Context UI, selected source line 3 and saved `FLOW-SAVE durable line comment on source revision.` The source rendered one `current` comment after an explicit **Refresh Context files**, at the same immutable source path/revision.

For the negative save, edited that saved draft to exact prose `FLOW-SAVE retry text must remain after the actual injected HTTP 500.` A page-only fetch fault injector returned one controlled HTTP 500 `fixture_save_failure` for the actual POST `/api/v1/sessions/flowsave59247bd2/panes/w2%3Ap3/comments/upsert` (batch `30c6f4a6-7a6d-4282-af38-abcf48f83d5b`, draft `f447b547-e5a7-48f1-b344-e655928eb595`, expected generation 1, same source binding `sha256:946c1305afd3a8e5e089c02ab77827ac30eab5706ff7a061257a783508e5a1e9`). The actual UI error was `Comments unavailable — Cockpit comment upsert endpoint returned HTTP 500`; the edit textarea remained open with the exact unsaved retry prose, while the prior saved comment remained visible. A subsequent explicit save sent the upsert to the real private gateway once; it succeeded, replaced the same draft, and the source still displayed exactly one comment with the retry prose and no alert. This demonstrates retention after a *deliberately injected HTTP failure* and explicit successful retry, not an unmodified backend outage or paste-response loss.

Cleanup: closed managed browser tab, gateway PID 3939417 exited 0, private Herdr server stop succeeded, `ps`/`pgrep` found no fixture processes, port 45527 was absent from `ss`, and only `/tmp/cflow-save-XIHHev` was removed. No worktree edits remained (`git status --porcelain` empty before cleanup).

## Fresh borrowed linked-checkout hierarchy and Herdr TUI comparison

- Separate browser-only fixture `/tmp/cflow-nested-owned-3qQ3Sa`, Herdr session `flownestedcf2f5fba`, gateway `127.0.0.1:36625`; borrowed parent fixture `/tmp/cflow-parent-retained-jZ0oFe/source` was intentionally retained as the acceptance target. Parent and nested linked checkout `/tmp/cflow-parent-retained-jZ0oFe/source/nested-worktree` were both at commit `db20127ace0f76d2c2ae04d5240d78afc8654470`; parent branch `main`, linked child branch `nested-borrowed`; `README.md` SHA-256 in both: `720a429a6cbd9574fbb0e08e27e613af296c0593275680cbabe0dcd98e6fb08e`.
- Actual Cockpit Setup → Existing directory reviewed and opened the nested checkout as borrowed operation `41fbc0e9-6735-4691-9642-74af94a7aeb6`. It recognized the linked worktree and its Git common parent, child checkout path, and branch; manifest generation 11 recorded borrowed ownership and `created_by_operation: false` for that checkout. Cleanup UI identified it as borrowed and blocked removal (“the checkout was opened as borrowed and cannot be removed”); actual Close Space removed the child workspace from the session while retaining parent and linked checkout bytes.
- To compare the same private session in Herdr TUI, closed only duplicate run-created workspaces, reopened the borrowed checkout as `w4` beside retained parent workspace `w1`, and launched actual `herdr --session flownestedcf2f5fba` in a private pty. TUI sidebar visibly showed `FLOW Borrowed Parent` (`main`) followed by `FLOW Nested Borrowed Child` (`nested-borrowed`) as sibling rows, same as Cockpit’s sidebar. Before the TUI’s first-run informational overlay, its rendered terminal showed cwd `/tmp/cflow-parent-retained-jZ0oFe/source/nested-worktree`; authoritative snapshot focus was `w4:t1` / `w4:p1`, while `w1` remained the parent. The snapshot exposed no parent/worktree-parent relation field; it exposes separate workspace IDs, labels, tabs, and panes. No key was sent through the onboarding overlay, and no synthetic Cockpit parent relationship is inferred or added.
- Cleanup stopped the exact private TUI and Herdr PIDs (`3979301`, `4000135`; both absent afterward; Herdr exit 0), removed only owned fixture `/tmp/cflow-nested-owned-3qQ3Sa`, confirmed gateway port `36625` and private session socket were absent, and retained `/tmp/cflow-parent-retained-jZ0oFe` with nested checkout intact and both README hashes unchanged. Parent repository status showed only its expected untracked linked-worktree directory; `git worktree list` still showed `main` parent plus `nested-borrowed` child. No port 5173 or 9519 listener belonged to this run.

This proves correct borrowed linked-checkout identity, non-deletion, retention, and Cockpit/TUI sibling ordering/focus parity for this actual Herdr data model. It does not establish a parent/child Space nesting relation, which the observed Herdr snapshot does not express.

## Acceptance matrix

| # | Result | Evidence / exact limit |
|---|---|---|
| 1 | **Observed pass for prior plain-directory scenario; overall partial** | Prior browser Setup recognized an ordinary plain path as borrowed and non-deletable; close retained it and original file hash. No new run. |
| 2 | **Partial / linked borrowed identity and teardown parity observed** | Prior nested borrowed path preserved parent/nested bytes and teardown refused deletion. This run additionally opened an actual nested *linked* worktree as borrowed, resolved its Git common parent/checkout/branch, refused destructive removal, and retained it after Close Space. Cockpit and actual Herdr TUI both displayed parent and child as sibling Spaces in the same order; Herdr snapshot has no parent relation field. Hierarchical nesting semantics beyond that flat Herdr structure remain unproved; do not infer a Cockpit hierarchy defect or create a synthetic relationship. |
| 3 | **Partial** | Prior actual browser owned-worktree creation, Review, dirty refusal preserving file+manifest, and clean explicit removal passed. This run reviewed/created a real child owned worktree+companion with completed generation-11 manifest; browser/gateway restart retained identity and source. Herdr session/server-process restart and pending-intent recovery remain untested. |
| 4 | **Partial** | This run rendered actual Markdown/Mermaid and PNG media, imported local snapshots, opened nested line-3 search, and delayed/canceled the old search response without stale results. Provider import was visibly unavailable because no provider is configured. Prior source identity/hash and changed-source refusal remain valid; cancellation and gateway restart preserved the same snapshot path/source. |
| 5 | **Partial** | Prior browser Review all-local/staged/unstaged/untracked/branch scopes and diffs were exercised; refresh updated selected diff; line/file comments survived browser refresh/reload and changed revision after the prior repair. Native Review comment parity was attempted and failed with invalid WebDriver session. No complete native Review/comment proof. |
| 6 | **Partial** | Prior browser and native Context runs accepted exact prepared payload into selected same-tab eligible real Pi without Enter; native and browser changed-source refusals recorded rejected receipts, preserved comments and unchanged target input; accepted/rejected operation IDs and payload hashes are in prior record. No lost-response/unknown receipt proof; target-change-before-send path not covered. |
| 7 | **Partial** | The changed-source paste refusal retained user text and real rejection state in prior browser/native runs. This run visibly retained exact edited comment prose in an open draft after one controlled HTTP 500 (`fixture_save_failure`); the UI reported `Comments unavailable — Cockpit comment upsert endpoint returned HTTP 500`. Explicit retry persisted that prose in place, leaving one comment. Lost paste response, unknown-outcome reconciliation and duplicate-risk acknowledgement remain unproved. |
| 8 | **Partial** | Prior browser/native paste journeys and Herdr hierarchy/selection/TUI comparisons exist; this run independently observed parent `w1` → owned child `w2`, selected `w2:t2`, terminal/Context/plugin/agent pane order and authoritative raw snapshot. Fresh native smoke proves only built-in browser rendering/input. The actual private Herdr TUI for the borrowed linked checkout displayed parent then child as sibling rows and focused child `w4:t1/w4:p1`, matching Cockpit’s row order; no parent relation exists in the snapshot. Native Context/Review parity and broader keyboard/focus journey remain unestablished. |

## Status

An actionable Herdr/Cockpit agent-snapshot Recovery alert was reproduced after starting an isolated Pi; raw CLI schema/snapshot and Cockpit normalized snapshot were captured and reported to Main, but the failing surface’s exact source remains unresolved. No product edit was made. FLOW-01 remains open. Highest-value remaining work: native Review comments/scope and teardown parity; provider source import with an authorized/configured provider; Herdr server/session-process restart and pending-intent recovery; changed-target-before-send and lost-paste-response reconciliation; native full-workflow comparison. The nested linked borrowed checkout identity/refusal/retention and Cockpit-versus-TUI sibling ordering/focus are now recorded; Herdr provides no parent relation to infer. The native session for the earlier smoke was released, and Main/WEB-08 coordination for 5173 is recorded in the run evidence. Reuse precise evidence above; do not reinterpret partial results as full criteria.