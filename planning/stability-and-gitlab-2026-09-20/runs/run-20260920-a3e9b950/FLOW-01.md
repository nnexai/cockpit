# FLOW-01 — same-tab comment-to-agent paste increment

Accepted bounded plan, 2026-09-23; baseline `58f2158834515576d4d84a0c71aa139e4b607417`. FLOW-01's plain/nested/owned setup, Context/Review feature matrix, source revision/reopen, dirty/borrowed teardown, real failure retention and browser/native parity remain required. This increment targets one fresh local source → saved comment → exact prepared same-tab paste into a real selected agent without Enter, plus a refused wrong-target/changed-source path. No generic comment-paste source rewrite without reproduced failure; do not conflate a synthetic `pane report-agent` identity with a real agent.

Own only a new uniquely named `/tmp/cflow-*` root, temporary repository/checkouts/config/state/Herdr session/gateway/browser profile and this evidence. Create a small local Git repository with an identifiable source line; use the existing Setup flow to establish a reviewed companion checkout rather than writing a fake manifest. Start a supported installed agent (`codex`, `omp`, or `pi` only if normal startup works in the isolated HOME) in an idle pane with `herdr --session <unique> agent start`; never use agent prompt or submit Enter. Inspect live pane/agent/workspace/tab/terminal IDs and compare Herdr TUI selection/focus with Cockpit. Open the authorized Context source in the same tab, save a line comment, prepare the payload and select the real agent. Record root/path/revision, batch generation, target tuple, preview bytes and SHA-256, operation ID and accepted/refused/unknown receipt. On accepted paste, visually inspect the real agent's unsubmitted input buffer and idle state; don't claim raw inbound PTY bytes unless a controlled independent recorder exists. The app receipt and preview hash provide additional transport evidence, not a substitute for rendered user action.

Negative: change the intended target or source before send, verify a refusal with no agent dispatch and preserved comment, then prepare again explicitly; never auto-retry an unknown paste. After refresh/reopen, compare persisted batch/receipt and source identity. Browser gateway is the first surface; native Tauri follows on a separate disposable session after this browser route is stable and after the current WEB-06 native run releases port 5173. Teardown only resources created here and preserve a dirty/borrowed fixture untouched if deliberately tested. If real agent launch/auth or Context companion association is unavailable, record the exact attempted prerequisite and continue other campaign tasks; neither a mock receipt nor no-eligible-agent banner completes criterion 6.

## Browser and Herdr observation

- Isolated root `/tmp/cflow-mphnnpte`, session `flow-71c22f02b1`, gateway `127.0.0.1:40679`; locally committed one-file repository. SetupDialog operation `126dcbcd-e0f7-4ec7-8c81-ba423f663a05` completed and created the linked checkout, reviewed companion, and child Space. A copied installed `herdr-file-viewer` 1.15.0 plugin was linked only into this root and the private Herdr server reloaded. The browser's previously disabled Context action became enabled for source pane `w2:p2` at the reviewed checkout. Context opened the real plugin as `w2:p4` in the selected `w2:t2`, not a synthetic renderer.
- The source provider was not configured; Context's **Import local snapshot** copied exactly one tracked file (99 bytes) into its companion. Its `README.md` line 3 was `Line one: retain this comment.`; the original and restored source SHA-256 was `74d04e290c88d0728f7b252cfba22b5e058c640167e18e1390c3e7acf54a0ecf`. A line-three comment was saved through the browser: `FLOW01: preserve line one; paste this source note without submitting the agent prompt.` Preview produced the file path and line, the original excerpt, then precisely that comment with trailing newline.
- Real installed `codex` started via `herdr agent start` in `w2:p3`, but displayed its sign-in screen in the isolated HOME; it was not used as an input target. Real installed `pi` started with `interactive_ready: true` in token-scrubbed `w2:p5`, same selected tab, with no configured model. Its no-model warning remained visible; no agent prompt was submitted. Herdr's authoritative snapshot showed selected Space `w2`, tab `w2:t2`, source `w2:p2`, Context `w2:p4`, Codex `w2:p3`, Pi `w2:p5`; focused pane `w2:p4`. An attached real Herdr TUI displayed the same parent/child Space, selected tab, four panes, and both agents; its first-run onboarding overlay prevented a separate TUI focus assertion. It was detached without pressing Enter.
- Negative: after preparing the preview for explicit Pi target `w2:p5`, the run-owned snapshot line 3 was changed. **Paste to agent** returned `Paste rejected: one or more sources changed, disappeared, or became unavailable`; the saved comment was still displayed and `herdr pane read w2:p5` did not contain the comment. Restoring the exact original file bytes left the saved comment revision marked `changed`, so no automatic retry occurred. A browser-driven comment-delete request was aborted during confirmation handling (`net::ERR_ABORTED`), and was not counted as a successful deletion. After inspecting the restored SHA and exact preview, the operator explicitly chose **Include stale excerpts** and the Pi target before retrying once.
- Positive: the second preview was byte-identical to the first; the browser action delivered it into the real Pi editor in `w2:p5` without Enter. Herdr `pane read` and the browser both displayed the original line excerpt plus the comment in the unsent Pi input; the no-model warning remained, with no assistant response. Current comments became zero while the saved batch remained one. Reloading the browser preserved the selected tab, actual Context pane, imported file (reselected through its picker), saved batch, and unsent Pi editor text. Actual screenshot `FLOW-01-pi-draft.png` SHA-256 `2f0d6bcc20bba95b413eaff8ed98ef664345120b9d169fc8781257e896f19176` captures the post-reload file, pane topology, and draft.
- This is browser delivery proof only, not model execution or the original full FLOW-01/native/dirty-borrowed/Review matrix. The stale-excerpt override was explicit after an observed refusal, not a silent fallback. The Codex login, Pi missing model, initial absent plugin, and aborted browser-confirm deletion are retained as observed failure states. Native FLOW parity and wider lifecycle remain blocked.
- Cleanup: browser tab released; private gateway and Herdr server stopped with exit 0; TUI detached with exit 0; `/tmp/cflow-mphnnpte` removed. Only this committed evidence and the screenshot remain.

## Accepted native parity follow-up — 2026-09-23

Baseline is the committed WEB-06 diagnostic checkpoint `00705d0`. Keep this follow-up independent of browser-helper edits: use a new disposable `/tmp/cflow-native-*` root, uniquely named Herdr session/Space and source checkout, private Niri display/WebKitWebDriver, and the current built Tauri executable. Own only the fixture, this evidence, and a narrow reusable native verification script if necessary. Do not use the active Herdr session or an existing browser/profile; leave borrowed repositories untouched.

Positive: in the real Linux-native UI, open an eligible Context source in the same selected tab as an idle real `pi` agent, save a line comment, prepare a payload, paste into the selected agent without Enter, and observe the exact unsent text in both Herdr's authoritative pane read and the native view. Reopen the source and confirm durable comment/paste receipt identity. Negative: change the local source between preview and send; require a visible changed-source refusal, unchanged Pi input, retained comment and explicit fresh preparation before the eventual send. Compare selected Space/tab/pane order with the Herdr snapshot. Record fixture IDs, source bytes/hash, payload/operation ID and cleanup; a failed prerequisite leaves the original FLOW-01 native/borrowed/teardown criteria blocked rather than simulated.

### Native, authoritative and failure observation

- The real Tauri app (`target/debug/cockpit-tauri`, SHA-256 `583900cc080b1f04ec6147dd6bef576d11ddc4fdd265e111188be12a11a8bd9d`) ran at 676×835 CSS pixels and DPR 2 under a private Niri 26.04 display, `webkitgtk6.0-2.52.5-1.fc44.x86_64` WebKitWebDriver, and Herdr 0.9.1. The owned root `/tmp/cflow-native-kbwfeytp`, session `native-flow-add14802e3ef`, and parent Git repository `/tmp/cflow-native-kbwfeytp/source` were new. The initial 71-byte `README.md` SHA-256 was `d047881481eaa17ff82988cc3aaf91ddc6d88bd5579686a60b611fda4da9ed29`. The installed `herdr-file-viewer` 1.15.0 was linked only into this private Herdr config. Native **Set up a task Space** reviewed and created owned operation `de285ec1-8a71-44ee-92f0-927c1fc55be6`, branch `native-flow-add14802e3ef`, checkout under this root's `worktrees/`, companion under `companions/`, and child Space `w2` of original `w1`. Its manifest recorded `ownership: cockpit`, parent repository key, session identity, checkout path, and `w2`.
- Herdr selected `w2:t2`; original terminal `w2:p2`, idle real Pi `w2:p3`, and native **Open Context right** created viewer `w2:p4` in that same tab. Context **Import local snapshot** copied exactly one tracked README (71 bytes) from the owned checkout into the companion. In native Source view, the user selected line 2, entered `FLOW01 native: preserve line two; paste this source note into Pi without submitting.`, and **Save comment** acknowledged one durable comment. The exact prepared `README.md:2` excerpt and comment, including trailing newline, SHA-256 `a1eaacc94729b68535fe6b3f95c7d10fbaa7a1bb2693164f9f73640326e7c7c1`, targeted Pi pane `w2:p3` / terminal `term_65c25083c493d4` in `w2:t2`.
- Negative: only the run-owned *companion snapshot* line was changed (`d0478814…` → `acd36f299557fe2ef4ed631a5422a4efa92aa9a1020d558f870c8f8caaebc3d3`); the original repository README stayed byte-identical. Native **Paste to agent** showed `Paste rejected: one or more sources changed, disappeared, or became unavailable`, marked the saved comment `source changed`, and retained its text. Herdr `pane read w2:p3` showed no comment. Durable operation `727c974b-16e3-41b3-a812-1e2a2a6fa573` recorded `state: rejected`, `sent_draft_ids: []`, the same payload hash and target; `FLOW-01-native-changed-source-refusal.png` (123,543 bytes, SHA-256 `5e6dafdc5987548cdd40426873a0fb5d0e4362f7e8be88f324158ee9b9533532`) visually captures the failure and retained comment. No paste was replayed automatically.
- Positive: restoring the exact snapshot bytes did **not** silently clear the changed-source status. After inspecting their matching SHA, the operator explicitly selected **Include stale excerpts**, refreshed the preview, verified the identical payload hash and Pi target, and clicked **Paste to agent** once. Durable operation `46e6d7b6-111d-489a-a27a-b40d955b86ff` recorded `state: accepted`, sent draft `b3a2c1df-c06e-40f4-bf0c-080d8d93f352`, and `Herdr accepted one raw bracketed-paste write; no Enter was sent.` Herdr `pane read w2:p3` and the actual Tauri terminal displayed the same excerpt/comment in Pi's unsent editor; its no-model warning remained and no response appeared. Native Context showed zero current comments and one saved batch. `FLOW-01-native-pi-draft.png` (248,827 bytes, SHA-256 `07f517854c4ff8df80424cb481c9b2da039851bc10545005d7aa394e10743bf1`) is the visual check. Native WebView reload retained selected child Space/tab, actual Context and Pi editor text; reselecting README through its picker restored exact source content and the one saved batch. `FLOW-01-native-paste-receipts.json` (3,450 bytes, SHA-256 `1632db73395ccdb3f91e0bc4ac1633b9992f58fd3e7710798a6704a0982de693`) preserves both authoritative receipts, target fingerprint, revision and sent-draft evidence.
- A separate attached Herdr TUI displayed the parent `Native FLOW add1480…`, child task tab, `Files` pane and `flowpiadd148` agent; the authoritative Herdr snapshot identified selected `w2:t2`, focused Context `w2:p4`, and the other two pane IDs/order `w2:p2`, `w2:p3`, matching the native split. The TUI was detached before teardown. This proves one representative native/browser workflow parity path, not all TUI focus or FLOW-01 matrices.
- Teardown: after evidence capture, only disposable Pi pane `w2:p3` was closed without submitting its draft. A fresh native **Review task cleanup…** reported the linked worktree `owned created` and `clean`. Explicit **Remove task worktree** and typed `REMOVE WORKTREE` yielded `Herdr removed the owned worktree without force`; `FLOW-01-native-owned-teardown.png` (88,049 bytes, SHA-256 `0f00cb30cdd50489dea721a6ea5156a8d0267bf8b17a06a70cf55699fe069ac4`) captures the native result. Herdr removed child `w2`; its checkout and companion disappeared, while original parent `w1`, original repository and `main` checkout survived with the original README hash. Git listed only the original worktree afterward. WebDriver session was deleted; run-owned TUI, Niri and Herdr exited 0, frontend/WebDriver stopped with exit 1 after supervised termination, and all run-owned services were non-running. Finally `/tmp/cflow-native-kbwfeytp` was removed. Nothing in the user's active session or borrowed repositories was modified.

At the native checkpoint, FLOW-01 still lacked borrowed and dirty teardown, Review staged/unstaged/branch/untracked and source/media/mermaid/search breadth, save/paste response-loss reconciliation, and full process/session-restart parity. The follow-up below covers the borrowed and dirty teardown slice; a WebView reload is not a Herdr server restart.

## Accepted borrowed and dirty teardown follow-up — 2026-09-23

Baseline `618c14b`. Use a new uniquely owned `/tmp/cflow-borrowed-*` root, private named Herdr session and browser gateway; leave the user's active session and repositories untouched. Inside the fixture place an ordinary plain directory, a nested path within a Git parent repository deliberately outside task-worktree cleanup ownership, and a separately created Cockpit-owned linked checkout. Through the actual browser Setup and Review task cleanup dialogs, verify which path is borrowed versus owned, exact Space hierarchy and selected tab/pane against Herdr, available/refused teardown actions, and preserved bytes after a borrowed close. Deliberately dirty only the owned task checkout; removal must be unavailable or rejected without deleting its files/manifest. Restore exact contents, get a fresh clean review, and remove only that checkout/companion through its explicit confirmation. Record operation/manifest IDs, source hashes, Git status, screenshots and authoritative Herdr before/after; a path that cannot be opened or torn down leaves its criterion blocked. This slice does not claim the Review content matrix, source/media breadth, paste response-loss reconciliation or process restart.

### Browser, Herdr, Git and filesystem observation

- Isolated fixture `/tmp/cflow-borrowed-_3pl1rw6`, session `flowborrowed8f8875fc`, private browser gateway `127.0.0.1:36877`, parent Space `w1`, repository `source` at commit `fe1dfdee00d2dcd5d40e32c46499e58504d0c058` on `main`. Only this fixture's `herdr/config.toml` enabled `[experimental] allow_nested = true` so the attached run-owned Herdr TUI could start; the user's Herdr configuration and active session were untouched. Original SHA-256: parent `README.md` `4e7ec08cf5ec41d778dae6042e0b16473ea0cc922d42c9fa2124e43f08233860`, nested `nested/nested.txt` `4626f387b64473cac9ec4808695674c828766ea2a38017bbeab26ef1208b77be`, ordinary plain `plain/notes.txt` `e956a9e3508a9627b58aedc96bcbc1df9ce8d4dcf5267b671fceb26d2ab259ea`.
- Actual browser **Existing directory** Setup reviewed operation `da0029e4-2e45-4b81-a630-f7e2a355ee56` for the ordinary non-Git `plain` path: `borrowed directory`, no repository/branch, and the explicit effect “Cockpit will never delete it”. Starting Setup created Space `w2` and a terminal; Herdr's `workspace list` showed focused `w2`, and Cockpit rendered it alongside `w1`. Browser **Review task cleanup…** reported `borrowed opened`, Git status `unknown`, “the checkout was opened as borrowed and cannot be removed”; only **Close Space** and **Forget association** were offered. Explicit **Close Space** returned “workspace closed; the checkout and companion were retained”. Herdr then listed only `w1`, and the plain file retained the exact original SHA-256.
- Actual browser Setup reviewed nested Git path `source/nested` as operation `97a91bff-ba53-43a4-b5c3-b6e7961f65e5`, `borrowed directory`, repository `source`, explicit non-deletion effect; created focused Space `w3` and context terminal. This was a nested *filesystem path*, not a linked child worktree Space. Teardown again reported `borrowed opened`, `unknown`, the same removal block, and only **Close Space**/**Forget association**. Closing it retained checkout and companion, removed `w3` from Herdr, and left parent, nested and plain file SHA-256 values unchanged.
- Actual browser **New worktree** Setup reviewed Cockpit-owned operation `c8328124-7c18-486a-beb3-e62c41d17256`, branch `flow-owned-dirty-8f8875fc`, checkout `worktrees/source-c8328124-7c18-486a-beb3-e62c41d17256`, and then created child Space `w4` under parent `w1`. Git `worktree list --porcelain` showed both `main` and the linked branch at the same fixture commit. Cockpit showed `w1` → `w4` and selected context tab `w4:t2`; a separate private Herdr TUI displayed the same parent-child order and the linked checkout's terminal. Herdr's first-run overlay prevented an independent TUI focus assertion; the authoritative Herdr workspace snapshot identified the focused child. The TUI was detached without terminal input.
- A *run-owned* untracked `KEEP-DIRTY-8f8875fc.txt` was added only to that linked checkout: Git status `?? KEEP-DIRTY-8f8875fc.txt`; content SHA-256 `3f4126654defbec42e59eb72921872b63b041590e75285bb6b4622f6d0282b7c`. Fresh browser teardown preview reported `owned created`, `dirty`, “the worktree has tracked or untracked changes”, and offered only **Close Space**, not **Remove task worktree**. Without closing, the file bytes and checkout remained; the real manifest `cockpit-state/c8328124-7c18-486a-beb3-e62c41d17256.json` still had completed state, generation 11, and both worktree/companion owned resources, SHA-256 `f49ef8d65efecc8b6dce188c2863674fd32f94e6c90184260248bd68a4737d04`.
- Removing *only that untracked fixture file* restored a clean Git status. A **fresh** browser teardown review then reported `owned created`, `clean`, and offered **Remove task worktree**; its final button was disabled until typing exactly `REMOVE WORKTREE`. After the explicit confirmation, the UI displayed “Herdr removed the owned worktree without force”. The owned checkout and companion disappeared; Git listed only the primary `main` checkout; Herdr listed only parent `w1`, selected in both the browser and authoritative server. Parent, nested, and ordinary plain file hashes remained byte-for-byte identical to their originals.
- Real browser screenshots (filename → SHA-256): `FLOW-01-browser-borrowed-plain-refusal.webp` → `42e1b9086a6419f3cf0c68c56c44c6605c943ab7029a72050417d05f26763428`; `FLOW-01-browser-nested-borrowed-space.webp` → `404c56d0cea009ee6c6e21ac1454da42deb510fd3a5b2c6ea2e597801e5cae6e`; `FLOW-01-browser-borrowed-nested-refusal.webp` → `7fe1b11fad5437a33ae7df0ebc3f94d1a275e0a6ef89ac32ec6658e50a6720f0`; `FLOW-01-browser-owned-child-space.webp` → `dae574c06e49a0defb25fb6342158036fc43f7e0903f5def07a3d88ebce093da`; `FLOW-01-browser-owned-dirty-refusal.webp` → `26bd30d6e9c567fd8e72c5e25e0f9fd2cef1022b9a1dab69b7cbd04b1cac7bc7`; `FLOW-01-browser-owned-clean-review.webp` → `2f3b152523a7f83fecf6823b5cd8c419b96e89b53b96cfb7e90ef7bff492b4fb`; `FLOW-01-browser-owned-removal-result.webp` → `4c3246d38f25e101985f22719d3052d0205397f98039a596d1e40e04cd1ffbb2`.
- Cleanup: the browser tab was released, private TUI detached with exit 0, private gateway and Herdr server stopped with exit 0, and only the run-owned fixture `/tmp/cflow-borrowed-_3pl1rw6` was removed. This proves borrowed plain/nested non-removability and dirty-owned refusal followed by explicitly reviewed clean-owned removal in the browser; it does **not** prove native borrowed/dirty parity, Review staged/unstaged/branch/untracked content, source/media/mermaid/search breadth, save/paste response-loss reconciliation, or process/session restart. FLOW-01 remains blocked.

## Accepted local Review scope follow-up — 2026-09-23

Baseline `5700cf8`; original FLOW-01 and A17/A19 review/source criteria remain attached. Own only a new `/tmp/cflow-review-*` fixture, named Herdr session, private browser gateway/profile, and this run evidence. Use an isolated Git parent at `main`, a Cockpit-reviewed linked child checkout on a unique branch, and the installed `persiyanov.reviewr` plugin linked read-only into this fixture's private Herdr registry. Freeze distinct branch-committed, staged, unstaged and untracked marker files, including staged-plus-unstaged overlap on one path. No user's active Herdr session, repository, browser profile, plugin registry or other working tree changes.

In the real browser, open **Review right** from the child checkout, inspect the actual selected Herdr tab/pane and compare the same fixture in an attached private Herdr Reviewr/TUI. Switch **All local changes**, **Staged**, **Unstaged**, **Untracked** and **Branch** with explicit `main` base; verify a representative file and actual diff marker for each scope, and verify overlap does not conflate index/working changes. Negative: change only a run-owned worktree file, refresh Review, and require an updated diff without stale marker or loss of the selected surviving file; a failed scope/refresh is recorded as blocked, not inferred from a Git command. Capture rendered screenshots, Herdr and Git authoritative state, operation/checkout IDs and source hashes. Exercise only clean, explicitly reviewed owned worktree cleanup after resetting the run-owned changes; original parent and any borrowed paths must survive. This slice does not assert the full Review corpus/performance, media/search, native borrowed/dirty parity, save/paste response-loss or server-restart criteria. Stop and preserve actionable failure evidence if installed plugin or suitable runtime cannot be enabled safely.

### Local Review browser/TUI observations

- Private fixture `/tmp/cflow-review-2oigo2qm`, session `flowreview8f88949c`, gateway `127.0.0.1:46291`; the installed `persiyanov.reviewr` 0.29.0 plugin was linked **only** into this fixture's `XDG_CONFIG_HOME`. Parent Git commit `4f21ce99a5f3668d240dda24bad727f5801bf328` on `main`; browser-reviewed owned operation `648d7611-3481-4af8-98e5-6b38d3dec530` created linked branch `flow-review-8f88949c` and child Space `w2`. Branch-only commit `a7094b5afeec08a636abe3146629f606cfbf1a92` added `branch-only.txt`; only the linked checkout then received one indexed `overlap.txt` line, a different working-tree line on that same path, staged rename, unstaged deletion and untracked file. Before testing, Git status was ` D deleted.txt`, `MM overlap.txt`, `R  rename-original.txt -> rename-target.txt`, `?? untracked.txt`; Git's cached and working diffs independently showed the corresponding markers.
- The installed plugin auto-opened its original Reviewr pane in the child's tab `w2:t1`, while Cockpit's Setup context terminal occupied selected tab `w2:t2`. Browser **Open Review right** created Reviewr pane `w2:p4` beside terminal `w2:p3` in that same selected tab; Herdr's authoritative snapshot reported focused `w2:p4`. A separately attached private Herdr TUI showed parent `w1` → child `w2`, the live Reviewr changes view and the same deleted/modified/renamed/untracked paths. Reviewr's own list collapsed index/working overlap into a modified path; Cockpit deliberately showed two distinguishable rows for that path. The TUI onboarding overlay was dismissed within the private client only; no agent or terminal prompt was submitted.
- Actual browser **All local changes** displayed five rows: deleted `deleted.txt`, staged and unstaged `overlap.txt` separately labeled `index`/`working`, staged renamed `rename-target.txt`, and untracked `untracked.txt`. **Staged** displayed only indexed overlap plus rename, with added diff line `INDEX_ONLY_MARKER_8f88949c`; **Unstaged** displayed deletion plus working overlap, with added line `WORKING_ONLY_MARKER_8f88949c` and the indexed line only as context. **Untracked** displayed only `untracked.txt` with added `UNTRACKED_MARKER_8f88949c`. **Branch** required an explicit base ref; `main` displayed only the committed `branch-only.txt` with added `BRANCH_ONLY_MARKER_8f88949c`. These are observed browser file lists and rendered diffs, not merely Git CLI output.
- Negative revision control: with `untracked.txt` selected, only that run-owned file was changed to `UNTRACKED_REFRESHED_MARKER_8f88949c`. Browser **Refresh** preserved selected `untracked.txt`, displayed the new line, no longer displayed the prior marker, and showed no Review alert. A separate transient failure was **not** counted as a pass: the adjacent terminal showed `terminal attach is still pending` with **Retry**/**Resync** after Review split and remained in that state across comparison/refresh actions. One explicit **Retry** restored the live fish prompt while retaining Review mode/file/diff; root cause was not determined. Queued as `OBS-025` for FLOW-01 and terminal-stream follow-up, not silently suppressed.
- Before teardown, only the run-owned untracked file was deleted and the linked branch was reset to its own fixture's `main` commit, restoring an empty `git status --porcelain`. Fresh browser **Review task cleanup…** reported `owned created` and `clean`; **Remove task worktree** required the exact `REMOVE WORKTREE` challenge. The browser displayed “Herdr removed the owned worktree without force”; linked checkout/companion disappeared, Git listed only original `main`, and Herdr listed only parent `w1`. Parent files remained byte-identical: `README.md` SHA-256 `4f186663564bca4de92a86b947d5a88ccf83102d7f6f30d64c11214e9a2e32ed`; `overlap.txt` `8af39f9ea22dd3ad04ed372f7e4c91e1e6c786791fe35653d7ff1241dd94bc01`; `deleted.txt` `7c14a9ea8911c2177c3b6c4f3ca50353ad1d160a0d2ea38aaff1054846c82918`; `rename-original.txt` `43489fb5f32ec1dde44f62794a53c6c52f151654c2cc998d877a3f5c39180fe6`. Browser tab released; private TUI, gateway and Herdr processes stopped with exit 0; `/tmp/cflow-review-2oigo2qm` removed.
- Real browser screenshot SHA-256: `FLOW-01-review-all-local.webp` `74e5084db0b7c3e2b36b164dab679d2e4c85e22ad8b73d434a2dda2da361e48e`; `FLOW-01-review-staged.webp` `eebee727a83528e482178e397e5624aa033ce8c24a6766b6cdd2085ff0a062c1`; `FLOW-01-review-unstaged.webp` `8a259c1523c3037768c08e43e10cc91cb50adce285075522ef7f07b3c20b4ff4`; `FLOW-01-review-untracked.webp` `dc5f5e794395f1a5e418b3daa4b0d35ab9feb54eb771921a517223c292768afa`; `FLOW-01-review-branch.webp` `d634ad98be6c2916c799ddd9701dc6c4aa5876c395312eb25c65c0d2ad583ac5`; `FLOW-01-review-refresh.webp` `516240b0378e182ace096a7178804633bf255f98cedb38d219f0ce2daadc6b45`; `FLOW-01-review-terminal-recovered.webp` `b71d0c943106f517ed7a1ebd58d8317dc5e9e9988dfc05d3dd9bfef80416eefb`; `FLOW-01-review-clean-teardown.webp` `303016bead6e15241447f84bfbf8064ecd609db3dfb71c1f9942bcf18895a7e9`.
- This targeted local Review comparison/refreshed-source path passes in the browser and agrees with the private Herdr TUI on changed paths; it does **not** close FLOW-01's larger Review corpus, native Review, media/search, workflow response-loss/restart or OBS-025 first-attach error.
