# WEB-04 — browser navigation/history increment

## Accepted bounded plan (2026-09-23)

Baseline `febc60a` with existing unrelated `.omp/*`, `.audit/`, and user-modified `tasks.json` protected. Prior OBS-020/024 proved only native pointer/first frame and repeated wheel routing, not A09/A10 navigation. WEB-04 completion dependencies WEB-02, WEB-03, SYNC-01 are recorded done. This increment targets browser-gateway A09/A10 navigation with an isolated real Chromium Cockpit client and a disposable localhost page. WEB-04's A04–A08/A12 plus native navigation and broader error/ownership checks remain attached to this same task; no full-matrix claim or status closure follows one browser pass.

Positive: create a uniquely named private Herdr session and Space, private Cockpit gateway/browser profile and localhost fixture with distinct URL/title for landing, redirect, hash, SPA push/replace and reload. Pre-open a target only in this private association. Through Cockpit's visible navigation controls and real browser-page interactions, compare the rendered address, tabs, enabled back/forward and loading with authoritative fixture URL/title/history after each transition. A documented physical click on the browser canvas and a URL submit must each produce an authoritative state event and corresponding painted page. Repeat back/forward across hash/SPA/full navigation; do not infer success solely from HTTP command response.

Negative: submit an invalid/rejected address through the Cockpit editor; the displayed confirmed URL must not change to the requested URL without an authoritative navigation event; visible failure must be recoverable. Keep the browser target distinct from any user's active session. If the negative reveals a navigation to an internal error document, record the exact authoritative URL rather than pretending prior URL survived. Capture the browser screenshot, page-side reports and client state diagnostics; do not use synthetic DOM events as a substitute for the Cockpit controls. Source changes only for a reproduced defect with a red and fixed browser case. The disposable fixture script is local to /tmp and removed after its process and descendants stop. Commit only run-owned evidence/repair and index-only task checkpoint, with WEB-04 blocked until remaining criteria pass.

Implementation owner: Main. Resource prefix `cwebnav-`, private Herdr session prefix `webnav-`, bounded private HTTP ports; no native display, default Herdr session, persistent browser profile, remote GitLab fixtures, shared user pane or active Cockpit session. Stop boundary: missing prerequisite/ownership isolation; navigation error with uncertain target/document identity; or hostile URL action requiring permission. Browser proof must include actual rendered result, authoritative event, failure state, and cleanup. No source/API contract is changed merely by the plan.

## Measured result: full-navigation tab-title repair, partial A09/A10

The private red run `webnav-59f0f884a27c` (gateway `127.0.0.1:42463`, fixture `127.0.0.1:40417`) reproduced a real browser discrepancy: after Back/full navigation, Cockpit's address and painted page showed the fixture's `/page2` and **Fixture page two**, while its selected browser tab still said **Fixture landing**. In the same fixture real canvas Hash and SPA push/replace actions previously produced authoritative page reports; an early immediate read after a click confused async feedback and was not counted as a verified transition. The red full-window screenshot and page-side reports are retained below.

`browser-runtime/browser-helper.mjs` now reconciles selected tab title from the bound page **after a load**, sends `targets_changed` only for an actually different selected title, and guards the asynchronous result by page binding, document generation, URL and previous selected title. This is not a new polling loop. Reviewer `NavigationTitleReviewer` found that the first candidate awaited `page.title()` inside the serialized tab transition and could deadlock tab selection on a JS dialog; a second candidate's target-array equality over-invalidated on unrelated target changes. Both candidates were replaced. The final detached, value-guarded callback had **no remaining P1/P2 in bounded read-only review**; review alone is not runtime proof.

The final rebuilt gateway SHA-256 `090ead24ebc39721ce5c5fdf16e1405ab553f52fe717e92d3418c559cfdd7339` embedded helper source SHA-256 `f0d3bf00d2430863011c0aac0420ff0a79497dd428bbb8df515b7bb85306a483`. `node --check browser-runtime/browser-helper.mjs` and `cargo build -p cockpit-host --bin cockpit` passed. In the **fresh** private final session `webnav-249d909ea111`, Space `w1`, root `/tmp/cwebnav-jy2fsg2g`, gateway `127.0.0.1:43477`, fixture `127.0.0.1:36839`, managed Chromium at 1392×835/DPR1.25:

| Real user action | Authoritative page and visible Cockpit result |
| --- | --- |
| Type an exactly selected `/page2` into the local Page URL editor, then Enter | Before the server event, Cockpit retained the prior confirmed `/`; after the fixture's `/page2` load, its address, selected **Fixture page two** tab, and painted frame matched. |
| Cockpit Back and Forward | Back loaded `/` and selected **Fixture landing**, with Forward enabled; Forward loaded `/page2` and selected **Fixture page two** again. The red stale-title state did not recur. |
| Submit `/redirect` through the address editor | The fixture returned a real 302; Cockpit confirmed only `/landed?from=redirect`, selected **Fixture redirect landed**, and painted that page. |
| Cockpit Reload then explicit `#hash-step` address navigation | Reload produced a second authoritative `/landed?from=redirect` load, returned from “Showing last confirmed frame” to Live browser view and kept the title; hash navigation produced `popstate`/`hashchange` reports, the confirmed hash URL and current painted page. The fixture's `/landed` title intentionally remains **Fixture redirect landed** when its hash changes. |
| Submit `file:///etc/passwd` via Page URL | Rejected safely with a visible “browser URL has an unsupported or unsafe scheme” status and Retry; the previous `/page2` URL and tab stayed confirmed and the fixture report count remained 5→5. Explicit Retry returned to Live browser view without a page mutation. No local file was read. |

**Separate physical-click failure (OBS-027):** after the full-document Forward to `/page2`, a real managed Chromium canvas click at CSS `(948,427)` on the visually painted **Hash** button did **not** produce a hashchange. The fixture recorded another `/page2` load and Cockpit retained `/page2`; a lower **Page two** link was visible in the screenshot. This is a failed portion of the accepted physical-click/SPA matrix, not passed by the toolbar URL tests. The helper resets its CDP input scale on a main loader change, but the exact fractional-DPR coordinate mode is unproved; do not ship a multiplier or count native OS pointer input as verified. A root-cause probe and positive browser/native action are still required. No WEB-04 status change; A04–A08/A12 and the rest of A09/A10 remain unproved.

Artifacts (file → SHA-256): `WEB-04-title-red-events.json` → `8db16d1326a688de9b5e751a9ff3495920c5a32b2a9d37ada6d515263570df5e`; `WEB-04-title-red.png` → `fb6afcda549a800b1f0c9d5f181214ec8b1b5efb681a94a4b9dfea847bbd93fd`; `WEB-04-title-green-events.json` → `89b7e35c358bf88eb6345d80c817d693586297ca781ed92a9499fb6157459b9d`; `WEB-04-title-green.png` → `c37de398abee3d2788808106b430f9dc8bda91ad22baba23595b1a04c3852e1a`; `WEB-04-pointer-miss.png` → `07514338c4549f57d83b83d562ee5cd80360cda9700bcac149d18e518bcf1aba`. Browser screenshots are real Cockpit/remote pixels, not simulated page output. The saved red and green JSON include authoritative fixture `url`, `title`, transition cause and history length.

All four run-owned `webnav-*` fixtures (including the reviewer-interrupted build attempt), their Herdr/gateway descendants and localhost HTTP server exited; final cleanup reported children `herdr=0`, `gateway=0`, nine fixture reports. The managed Chromium tabs were explicitly closed and temporary `/tmp/cwebnav_fixture.py` plus every owned `/tmp/cwebnav-*` root removed; the user's session, browser profile, `.omp/*`, `.audit/` and unstaged `tasks.json` were untouched. This was a **browser-gateway** proof only; native Tauri navigation, physical OS input and full WEB-04 acceptance are still outstanding.

## Accepted next bounded increment: distinguish post-navigation CDP pointer coordinate mode

Baseline `0042f0f` after the verified title-only commit. OBS-027 is a separate broken primary browser click path, not a reason to undo the independently passed tab-title repair. WEB-04 remains blocked. No new protocol, UI hierarchy or global input interception. Preserve the documented native DPR2 pre/post-resize behavior and existing matching-frame/lease barriers while identifying the browser-gateway fractional DPR1.25 failure. Main exclusively owns any helper change, the private fixture, actual browser/native checks, evidence and commit.

Use a new uniquely named disposable Herdr session/Space, gateway, remote Chromium profile and fixture page that records trusted `pointerdown`, `pointerup` and `click` `{targetId, clientX, clientY, button, dpr, viewport}` at known painted element positions. Compare a **real Cockpit canvas click** at initial attach, after full-document navigation, and after one controlled viewport-size change, with the actual page event target/coordinates, screenshot and current frame/viewport metadata. A direct isolated Chromium/CDP control without Cockpit may disambiguate CSS versus host-pixel interpretation. No synthetic event qualifies as the positive. Test a negative outside a button: it must not activate another page target or write to the terminal.

If the mismatch pins to `cdpInputScale` reset, prefer restoring a verified CSS-coordinate mode via an identity-pinned viewport override after a main loader change rather than a DPR threshold/special case or two divergent input scales. Before accepting code, prove the action at DPR1.25 real browser gateway **and** the existing DPR2 native post-navigation pointer action, unchanged first responsive native frame/FPS, correct painted target and no extra press/release; a failed native first-paint/FPS gate rejects the candidate. A helper instrumentation copy in `/tmp` is diagnostic only, not a shipped duplicate input path. Clean only owned resources and keep broader WEB-04 A04–A12 criteria open. If this bounded mode change cannot pass both surfaces, revert the candidate, commit truthful red evidence and stop repeating without a new discriminant.

### Baseline pointer event discrimination — real browser, product unchanged

Private session `webpointer-fff80fdc6f65`, fixture `127.0.0.1:43977`, gateway `127.0.0.1:45645`, root `/tmp/cwebpointer-p96xdjrb`; managed Chromium 1392×835 at DPR1.25. The page placed a **Hash** button at CSS x40–190/y210–250 and **Page two** at x40–190/y270–310 and posted every real `pointerdown`/`pointerup`/`click` `{target,clientX,clientY,dpr,viewport}`. On first Live frame `.browser-surface` was `(902.45,217)`, 488.55×617 CSS. The actual Cockpit-canvas click at host CSS `(1002,447)` (remote intended x≈100,y230) produced exactly one `pointerdown`, `pointerup` and `click` on **hash**, remote `(99.64,230)` at DPR1.25, followed by hashchange.

After a full-document `/page2` URL submit and new Live frame, the **identical host click** at `(1002,447)` produced exactly one down/up/click at remote `(124.55,287.5)`—the intended coordinates multiplied by **1.25** in both axes—on **page2**, then another `/page2` load. This is direct, captured input-event proof of the misroute; not an inference from a missing hash event. A physical drag of Cockpit's internal browser separator from x≈897 to x≈837, with the **outer 1392×835 unchanged**, settled the surface to `(838,185)`, 553×649 CSS. Clicking the same relative x100/y230 at host `(938,415)` then produced down/up/click at remote **(100,230)** on **hash** and a new painted hash title. An out-of-target canvas click at relative `(400,230)` yielded one down/up/click with target `null`, no activation and no URL change. The fixture logged 17 reports and both run-owned Herdr/gateway children exited with code 0; its profile/root were removed after saving evidence. No user's active session was touched.

Raw authoritative sequence: `WEB-04-pointer-coordinate-trace.json`, SHA-256 `63adb1d44c80574e27586ed4f8a7959e13315213d266048ce7463f6d3323f3a1`; physical-resize Cockpit screenshot: `WEB-04-pointer-after-resize.png`, SHA-256 `511a15e2f3716ed3d4374660e94bdb576c8c00e0788a797b932107c452461ff0`. Existing `WEB-04-pointer-miss.png` shows the prior independently observed visual target. This baseline proves the post-loader `cdpInputScale` assumption is wrong on the exercised browser-gateway DPR1.25, but not that an alternative is safe on native DPR2.

### Rejected viewport-reapply candidate: browser input passed, native 0 FPS

The candidate made a full loader's `frameBarrier` stop screencast, reapply the accepted `Emulation.setDeviceMetricsOverride` with the same CSS width/height/DPR, then set CDP input scale to 1 before new-document publication. `node --check` and `cargo build -p cockpit-host --bin cockpit` passed, but neither constitutes native acceptance. Candidate source SHA-256 `95fbdbc572711613cbc46994dc6d836b0a76b94a0bea495f77dcb96dc032b148`; candidate gateway binary SHA-256 `5ca74260b3f6bcb55ea122e6f3bd61c2df32eebd09d30d88bb50d4a9d42c4a72`.

Fresh private browser session `webpointer-45b1da99691e`, fixture `127.0.0.1:36485`, gateway `127.0.0.1:44565`: after typed full-document `/page2` and a new Live frame, a real canvas click at the original `(1002,447)` produced **one** down/up/click on **hash**, remote `(99.64,230)` at DPR1.25, with a painted `#hash-step` page. Clicking lower **Page two** at `(1002,507)` produced one down/up/click at `(99.64,290)` and a real `/page2` load; repeating `(1002,447)` after that **second** full loader again produced exact Hash events and the painted hash result. This verifies only browser-gateway pointer/loader correctness. Raw reports: `WEB-04-pointer-candidate-browser.json` SHA-256 `51b27d7eb7b0f0819be352e1dfa209e08757f3207cb31b78299bacd7defc049d`; real Cockpit screenshot: `WEB-04-pointer-candidate-browser.png` SHA-256 `8ce0c46ae55b0f7fe25131ea250f3000d6801a5486f1e84f4b5cd0e0cb884da8`. Its Herdr/gateway children exited with code 0 and fixture logged 14 events before the separate native FPS run.

The **required** fresh private Linux-native DPR2 run `native-7a24430fe285`, `/tmp/cnative-4nytd2o3`, used the same candidate gateway with `python3 scripts/verify/native_browser_acceptance.py --skip-build --native-only-open --timeout 30`. Its first **actual WebKit frame** was responsive: canvas and surface both 674×188.47 CSS, canvas 1348×376 pixels, fixture `innerWidth=674`, `innerHeight=188`, DPR2. The routed native pointer started the fixture's animation, which reached authoritative `scrollY=3900` in 6007.5 ms; **WebKit canvas performed 0 `drawImage` presentations during the subsequent 6.0073 seconds, 0.00 FPS against the unchanged >15 FPS floor**. The final native screenshot still painted the initial “waiting for routed native pointer” image. This violates the user-visible native scroll criterion even though the browser-gateway pointer case passed. Complete raw result: `WEB-04-pointer-candidate-native-red.json` SHA-256 `2ea3fbda3938f9c06a874e04943dcd3d8d95054513a2152204644cc957bdcc38`; first screenshot `WEB-04-pointer-candidate-native-first.png` SHA-256 `f8141585e9b61f1074c03a2b9e18506b1dd47103c1ee43d60597fc36dbae3da2`; final stale screenshot `WEB-04-pointer-candidate-native-stale.png` SHA-256 `320ed3e7103d52dff090ebba004c1082ba6dca0ed877d93423af93d8d641fcc5`. The native fixture's own frontend server, WebDriver, Niri, gateway, Herdr and fixture HTTP server all reported stopped; only its run-owned root was removed after copies.

Independent read-only reviewer `PointerScaleReviewer` also found a P2 failure path: `Emulation.setDeviceMetricsOverride` rejection after stopping screencast leaves the new document without a restarted stream/metadata, while `frameBarrier`'s catch lets a navigation command report accepted. **The candidate was rejected and reverted**, not shipped. `cargo build -p cockpit-host --bin cockpit` restored the executable; source SHA-256 `f0d3bf00d2430863011c0aac0420ff0a79497dd428bbb8df515b7bb85306a483` and gateway binary SHA-256 `090ead24ebc39721ce5c5fdf16e1405ab553f52fe717e92d3418c559cfdd7339` match the verified title-only baseline. No product code changed in this increment. Keep WEB-04 and WEB-06 blocked: the next discriminant is whether native frame descriptors are admitted/forwarded versus rejected after the extra CDP override while scroll changes, alongside a design that cannot leave capture stopped on failure. Do not repeat the same reapply candidate or weaken the >15 actual-paint floor without new evidence.

## Accepted OBS-029 input consequence of stream-first resize batch — 2026-09-23

Against `96f79c81768428413027c59bbc12a127bb8a0c93`, Main adopts the user's policy that a same-target viewport fit mismatch is a recoverable asynchronous resize state, not a frame-stream stop or standing warning. Worker A's helper code and worker B's BrowserPane/presenter code are separately owned in WEB-06/WEB-05's accepted plans, with unchanged wire shapes. During a brief fit transition present the latest authenticated same-target/document/epoch pixels and correct the requested resolution via existing control signals; input must map to the **actually painted capture descriptor**, not pretend an old layout is current. If the geometry is insufficient to dispatch one safe gesture, hold or cancel that gesture quietly without sending to the wrong target, preserving ordered key-up/button release and an honest actionable refusal if the user action was definitively rejected. No automatic replay, transport bypass, fractional-DPR special case or fresh Herdr input. Verify actual pointer and wheel actions against page event reports after asynchronous convergence on browser DPR1.25 and native DPR2; navigation away must never paint or target an old document. The previous post-loader pointer defect and full A04–A12 matrix remain open until specifically tested.

### Stream-first integrated native red control — current source, 2026-09-23

After worker A/B/C's first disjoint source pass plus Main's stream-effect TypeScript and capture-pinned/current-revision wheel fixes, `node --check browser-runtime/browser-helper.mjs`, `bun run typecheck`, focused browser wheel/presenter Vitest (4/4), `bun run build`, `cargo check -p cockpit-tauri`, and `cargo build -p cockpit-host --bin cockpit -p cockpit-tauri` passed. Disposable `scripts/verify/native_browser_acceptance.py --skip-build --native-only-open --nested-wheel --timeout 35` in session `native-b3013b772d38` showed first surface 674×188.47 CSS, page `innerWidth=674`, `innerHeight=188`, DPR 2, canvas 1348×376 and no startup frame warning. Six-second remote scroll reached authoritative `scrollY=3900`; the actual displayed canvas recorded **361** drawImage calls/6.0009s (**60.16 FPS**) and painted the green end marker. The **first nested wheel then failed**: at painted CSS x120/y100, remote page reported outer `scrollY=4080`, `nestedScrollTop=0`, `nestedWheelY=null`; expected nested target to scroll and outer page remain3900. This is an actionable native input miss, not a passing WEB-04/natively installed-app scenario. Trace/screenshot files retained at run-owned `/tmp/cnative-de58v6ck` until compact evidence copied; script stopped its gateway, Herdr, Niri, WebKit driver and fixture server. Read-only review identifies an uncoupled post-loader CDP input scale after asynchronous metrics override, and fixed initial `Page.startScreencast` max bounds on resize; worker A is correcting both without stopping the active stream. Rebuild and rerun exact nested/outer reverse paths, then test post-loader DPR1.25 physical browser pointer and private installed-prefix DPR2 before claiming input/fit.

Second private native attempt `native-50b68ef63ee1` (`/tmp/cnative-wpkrzk26`) also remained red on a **different exact boundary** after helper's fixed caps/input-mode edit: actual page CSS 674×188/DPR2 and canvas 1348×376, 361 native drawImage paints/6.0174s = 59.99 FPS during scroll, authoritative fixture finished at `scrollY=3900` and status `scroll complete` but final painted canvas stayed at old `scrolling`/white-header pixels for the 3-second marker wait. `WEB-04-native-final-marker-red.png` SHA-256 `7abca6c20ab9443f74c8fa7f74831e75e134335fe26042368ebe8d567c0dcfde` is the observed native screenshot. Fixture, gateway, compositor, WebDriver, Herdr and frontend were stopped, no default session mutated. Worker A is restoring a single trailing identity-checked settled-scroll capture; no green claim from FPS alone. Independently, before a fresh gateway/helper restart the same owned 1568×1009 Chromium UI displayed canvas 562×150 stretched to a CSS surface 562×823; `WEB-04-browser-stretched-red.webp` SHA-256 `e8db5d3546b99dd50d6e8447c621bba0d14ed9aefa96f27a45a00812632c78c5` shows that real incorrect UI. Source presentation assigned only `canvas.width`, never `canvas.height`, before draw; Main added descriptor-pinned height reset. Its recomplied frontend subsequently rendered page A at matching canvas 562×823/CSS 562×823 without stretching. On a fresh helper/browser expanded to **1342×823 CSS at DPR1.25**, authoritative page reports matched but bitmap stayed **1342×823**, not expected physical ~1678×1029 after 15 seconds; no alert. Helper A is adding a best-effort async post-resize density capture without stopping the usable stream. This is an additional red density criterion, not a WEB-04 pass.

Third rebuilt native run `native-2f69e167839e` (`/tmp/cnative-yofjs6hq`) shows the scroll-settle repair **did** paint final green `(0,167,75)` after **362** actual draws/6.01595s; no first-frame warning, canvas1348×376 at pane674×188/DPR2. Nested wheel forward +180 reached `nestedScrollTop=180`, outer `scrollY=3900`, a new canvas paint and `nestedWheelY=90` (CDP DOM event delta is DPR-scaled; actual CSS scroll distance remained180). Nested reverse −70 then reached authoritative `nestedScrollTop=110`, `nestedWheelY=-35`, but **no new native canvas draw within three seconds** and the screenshot still shows the forward-scrolled nested view. `WEB-04-native-nested-reverse-red.png` SHA-256 `46016de0f91a664b0eea5773a91f8ce34275b4d6b95f14295c0d7f581c291a69` is the actual WebKit red surface; script stopped all private services. Worker A is adding a debounced, identity-pinned post-wheel idle capture only when no screencast frame has published, because nested overflow can change pixels without changing the root screencast `scrollOffsetY`. Independent reviewer identified a separate P1: an unawaited resize screenshot can overtake a newer live frame and can overlap Chromium's temporary screenshot emulation with pointer dispatch; both guards are blocking before a green claim.

Fresh compiled private browser gateway at `127.0.0.1:40305`, explicit frontend DPR1.25/1568×1009, actual page A, Expand to surface1342×823, measured remote `innerWidth1342/innerHeight823/DPR1.25`, 18-second wait: bitmap stayed **1342×823**, not approximately1678×1029. UI said Live with no alert; the CSS-density stream is usable but full pixel density has not converged. `WEB-04-browser-dpr125-red.webp` SHA-256 `33e187aa3245242d99ca7f5503f08a205691a2d3a9c2747245a88ae5fcb267b9`. This disproves the first helper resize screenshot scheduling assumption; do not substitute the previous low-DPR browser screenshot or the native 674×188 first paint as full-width fit proof.

Fourth native run `native-2ae03f9107a1` (`/tmp/cnative-mx1xglzq`) using final screenshot-lock patch **regressed at the first paint**: fixture CSS674×188/DPR2, frame1 674×188 and live screencast frames2–5 **2560×714** (3.798 physical pixels per CSS pixel), not the required1348×376; validator rejected before input. The new constant `Page.startScreencast` maxWidth2560/Height1600 acts as a requested scale target for a small viewport, not merely a non-upscaling bound. `WEB-04-native-screencast-overscale-red.png` SHA-256 `67f49152f05e838dfe8b62c90adb69be6e270e86bacde110c40d44cf6523b22b` is the real native screenshot. The prior native capture at exactly1348×376 and this new out-of-bounds behavior were supplied to helper owner. All private processes cleaned. Final rebuilt browser at frontend DPR1.25 reproduced CSS-only1342×823 after 10 seconds on fixture A and briefly showed878×1286 for a 562×823 CSS viewport (1.5625× CSS, a doubled DPR multiplier), so current frame scaling is not yet accepted; installed lifecycle Close remains pending browser-fit repair.

Fifth native run `native-dc6b58ce91dc` (`/tmp/cnative-2vdnxxbh`) on the bounded-physical-cap patch: **first bitmap1348×376 at CSS674×188/DPR2**, **361 actual WebKit `drawImage` paints/6.003815s=60.13 FPS** across the real six-second page scroll, authoritative final `scrollY3900` and visible green `(0,167,75)` all passed. The native nested wheel forward+180 and reverse−70 both reached `nestedScrollTop180→110` and each emitted a new visible paint. The **third** discrete wheel, outer forward+160 at CSS y166 outside the 65px sticky inner scroller, did not produce a fixture root scroll within5s; fixture remained outer3900/inner110, UI Live/no alert, despite document `min-height:8000px`. The verifier aborted before the outer reverse and installed Close. Its private WebDriver/Niri/gateway/Herdr all stopped. To distinguish dropped frontend input from a rejected helper command, a throwaway clone `/tmp/cweb-stream-0Df4eS/installed_native_acceptance.py` now records actual Tauri browser command responses and DOM wheel events on the next run; product code is not weakened on this unproven cause.

The bounded-physical-cap helper in the private gateway `127.0.0.1:41143` briefly reached the desired **1678×1029** canvas for full-width CSS1342×823/DPR1.25 but, after 2.8 seconds, regressed to **703×431** while retaining the 1342×823 CSS surface. Actual screenshot `WEB-04-browser-resize-stale-red.webp` SHA-256 `c0197b3689ede1eeaf7956691223b14157bd3db6eff7de2182d98391c232ec77` proves an undersized bitmap stretched across the full pane, not physical-density fit. The screenshot's visible `562×823 DPR1.25` label is **load-time fixture text** (`fixture.py:12`), not evidence of stale post-resize page content. The authoritative fixture reported repeated `innerWidth=1/innerHeight=1` transient resizes followed immediately by1342×823 during the helper screenshot lane. On the initial narrow CSS562×823 view, canvas remained only562×823 after6.5s. Inference at this candidate: screenshot emulation and concurrent undersized packet admission were racing; any transient density alone is **not** a green fit check. Worker owner has both observations.

Screenshot-false + first CSS frame admission private Chromium candidate `127.0.0.1:40505`: real Open reached a usable initial canvas562×823, actual fixture A Load at `received_at=140414.795` and Expand resize at `140431.112` reported only1342×823/DPR1.25 and **no intervening 1×1 page resize**. After expansion the intrinsic canvas was **1678×1029** at CSS1342×823 and remained that resolution after another 3.4s, Live/no alert. The fixture's `#size` text stays at562×823 by design (set only on initial `load`), while authoritative resize confirmed the live 1342×823 viewport. This is a browser physical-density and no-1×1 observation for this candidate, **not** native nested/outer wheel proof or a campaign completion. An earlier traced native probe using a half-step strict-physical-only source (`SHA0ae096…`) never delivered its CSS first frame and timed out; that source was superseded by explicit CSS initial-frame admission before the green Chromium check.

Visual correction to the fifth native result: `WEB-04-native-halfwidth-red.png` SHA-256 `b0939c440b9fdce682d480d05f4e1468a25280d2dad34bd298782081f46ecc0c` shows **the page content fills only the left half** of the 674-CSS browser pane even though the bitmap is1348×376 and paints at60FPS. On the actual1352×1670 native window, row1300 has green fixture header at physical x650 and white at x700, with the canvas spanning roughly0–1352 physical pixels; fixture independently reported CSS innerWidth674 and h1 width650. Thus the earlier “DPR2 first bitmap” and left-edge final marker were **necessary but insufficient**, and the missing outer y166 wheel may be a direct consequence of this distorted page hit geometry. Run-only acceptance now samples fixture green at 90% of intrinsic canvas width after final scroll before attempting nested/outer wheels. Helper owner is experimenting with screencast maxWidth/maxHeight as **CSS**, not physical, while retaining the authenticated viewport and screenshot capture guard; no product acceptance claim until real image occupancy passes.

CSS-sized screencast cap experiment in real private WebKit `native-cdcec30dd795`: first intrinsic canvas674×188 at CSS674×188/DPR2, and `native-first.png` under `/tmp/cnative-qocxyuvp` visually filled the whole browser pane with the heading, status and nested box at normal CSS size. This fixes the earlier **half-width content geometry**, but the old immediate density gate correctly rejected a CSS-density bitmap. Run-only verifier was adjusted to retain the first usable CSS frame and wait seven seconds for an authenticated physical1348×376 frame (without weakening either criterion). The following run `native-271c8e7f0227` (`/tmp/cnative-9fpui0cc`) stayed674×188 for the entire seven seconds with no fixture resize side effects; abort before gesture, no native FPS/outer-wheel claim.

Disposable helper diagnostic source (not product instrumentation) in `native-2966b2149cdf` (`/tmp/cnative-bbs6loxh/browser-cdp-overrides.jsonl`) identified **a precise dropped physical frame**, not an absent high-density capture: CDP `Page.captureScreenshot` with `clip:{x:0,y:0,width:674,height:188,scale:1}` and `captureBeyondViewport:false` produced encoded JPEG **1348×376** at DPR2, matching the frozen requested viewport. One capture pinned a document generation replaced during navigation (`8021→8022`); the other returned while additional authentic screencast packets arrived, invalidating its exact frame-sequence proof before publication. Neither one-shot initial capture left a density correction armed for a later quiet interval, so the live canvas remained674×188 despite a valid physical JPEG result. Scope is an identity-pinned, trailing idle correction rather than bypassing stale-frame protection or accepting transient image dimensions. The disposable CDP trace confirms per-child requested override674×188/DPR2 and the real helper/browser fixture children; it was cleaned with its private run.

Preserved the scoped diagnostic as `WEB-04-native-physical-dropped-trace.jsonl` SHA-256 `883bb4282498107b470cee418e2e0bd05681415b623d035281037c921ee32402` (7,857 bytes, run-owned target IDs/fixture only). A trace records at most bounded sampled frame/capture lanes and native screenshot options, JPEG dimensions, process identity and post-capture geometry; it does not contain page contents or user paths.

Initial under-density-frame 120ms correction candidate `native-1e9a69e2bb48` (`/tmp/cnative-futj06c2`): after three initial CSS674×188 frames the helper delivered a **real1348×376 JPEG as frame sequence4** for the same CSS674×188 revision1. This proves the trailing correction overcame the startup capture race. Over the actual six-second root scroll the native canvas drew **361 frames/6.002038s=60.15FPS**, and the fixture finished outer scroll3900 with left marker `(3,167,70)`. Nonetheless the **final** latest frame returned to CSS674×188 (expected live-stream best-effort), and the 90%-of-canvas green header sample was `[255,255,255]` rather than `(0,168,75)`; real screenshot shows **content again compressed into the left half** of the full-width browser pane. `WEB-04-native-css-first-full.png` SHA-256 `7997e831456a813449391a5c779b6cc571405e81d5ff2297b0f9eeafdf68ffaf` is the visually full CSS-density page before the physical screenshot; `WEB-04-native-post-capture-halfwidth-red.png` SHA-256 `15df1b1f25bdb9194c7522eb9c23f36b3ea54b0f138986558b024921e6fd41ff` is the shrunk page after live frames resumed. Inference: CDP screenshot perturbs subsequent screencast content scale despite no reported page resize; an initial physical bitmap and FPS do **not** prove sustained visual fit. Must re-establish stream rendering geometry after screenshot, with no repeated scroll stop/start or stalled input. Native nested/outer wheel and installed Close remain untested on a visually fitted final frame.

### Uncapped screencast producer and exact post-wheel density diagnosis — 2026-09-24

The frozen initial CSS-sized `Page.startScreencast` caps prevented a 562→1342 CSS-pixel browser-pane expansion from emitting any admissible enlarged stream packet: the private Chromium remote page reported its new 1342×823/DPR1.25 layout, but the displayed bitmap stayed 562×823. Omitting both caps in source SHA-256 `ca4abcec43a59467e4feee3bec0df0309bc851d091ec8a050996d01ab273bc53` was tested through only a run-owned source-equivalent tracer copy, not the installed executable. Fresh real native WebKit `native-735248b2bda0`, `/tmp/cnative-xlxnulv9`, first presented CSS674×188 and then physical1348×376 at DPR2; its remote page independently reported CSS674×188. During the six-second page animation, 361 actual canvas `drawImage` presentations in 6.014377s gave 60.02 FPS and authoritative scrollY3900. The final screenshot `WEB-04-native-uncapped-full.png` SHA-256 `6769d0c8d09c8a07187ac622c4960e6cd16848c49032a08ff12c36ea730ddcb1` visibly fills the **entire** pane: both left and 90%-width right marker samples were `(0,167,75)`. Four actual WebKit wheel gestures produced nested scroll 0→180→110 and page scroll 3900→4060→3970, with a new paint for each, host scroll0, no alert and no fixture 1×1 resize. This positive is a browser-frame/input smoke, not installed native Close or the full WEB-03 resize/takeover matrix.

In private managed Chromium 1568×1009/DPR1.25, the uncapped producer plus bounded screenshot generated a 1678×1029 bitmap for expanded CSS1342×823 and held it for 3.3s, with no current-page 1×1 resize or alert. Actual pointer down/up reached remote `(140,140)`; nested/root wheel reached remote `(140,140)`/`(140,350)` with page scroll102 and nested45. The live page remained visually full-width, but after those gestures the intrinsic bitmap settled back to CSS1342×823: **post-input physical-density acceptance remains open**. The actual post-wheel view is `WEB-04-browser-uncapped-postwheel.webp` SHA-256 `22c2ed36f9577fe086639ca0565c2bcd435ac88549874c3a40ccca272fb9e14e`, and the bounded first source-trace is `WEB-04-browser-uncapped-postwheel-trace.jsonl` SHA-256 `bcf5fb30d926c245640da686fd58a7e56dce65fb4131703d6510ffdf71432155`. Through the actual browser UI, an unsent page-A region and note were saved; navigation A→B→Back A gave fixture loads for B and A and Notes1→0→0, no retained-warning/retry or old mark on return. This separately supports quiet draft retirement, not a claim about prepared/sent feedback.

The next run-only trace against source SHA-256 `25bfca2b514e8452990a5dcc8ce6246f430df9457c7101c573e33c4c2d01baee` isolated the remaining density failure before CDP screenshot: two post-wheel attempts at viewport revision3 had valid page binding, frame/arrival/input identity, but `Page.getLayoutMetrics` measured integer `scrollY=102` while the authenticated screencast baseline was fractional `102.4000015258789`; the generic 0.01 CSS-pixel geometry test refused both before `Page.captureScreenshot`. `WEB-04-browser-fractional-scroll-red.jsonl` SHA-256 `0ef6466af5bc07c1c48a7ca2cf6cbf5d10f66cc6cb6772221e343507e405cfbd` records the exact before/baseline mismatch and zero post-wheel screenshot sends. Fix only this bounded precision disagreement without weakening target/document/viewport/input identity or claiming that a CSS-density post-wheel frame is crisp. Worker owns the helper source; final packaged browser/native and installed-Close proof remain open.

An isolated no-reapply control removed only the screenshot's same-requested-metrics callback from a `/tmp` helper copy; product source was unchanged. In actual Chromium DPR1.25 this variant held a 1678×1029 bitmap for CSS1342×823 even 3.2s after the pointer/nested/root wheels. It is **rejected** because the real native WebKit `native-4db1451f307b` showed the opposite: CSS674×188/DPR2, physical bitmap1348×376 and 362 paints/6.014s (60.19 FPS), but the final remote page occupied only the **left half** of the native pane (green `(0,167,75)` left; white `(255,255,255)` at 90% width). `WEB-04-native-no-reapply-halfwidth-red.png` SHA-256 `7352517afc132d5919c2229365992ebb70f4f6ab042c5711f63f21f58dd1abb7` is the actual displayed counterexample. A physically sized bitmap/FPS or a Chromium-only improvement does not permit dropping the WebKit-required metrics restoration; preserve platform parity and solve the bounded stale-screenshot race without hiding the visual failure.

### Same-geometry animated-frame regression, actual browser red→green

Read-only review found that once a physical screenshot was accepted, the helper discarded *every* newer authenticated CSS-density screencast packet for the same exact viewport/document/scroll geometry. A private 1568×1009/DPR1.25 Chromium run against frozen pre-fix helper SHA-256 `5bb6c1f0b95144e42f708e3326724457d0855fca83c564daee5daa1b93a73547` opened the owned remote page at CSS1342×823 and waited for a physically sized1678×1029 canvas before arming its animation. During the next 3.2s the remote fixture's genuine `requestAnimationFrame` tick advanced **30→180**, while Cockpit's actual `canvas.browser-frame` `drawImage` count stayed **5→5** and the screen visibly remained at tick0. `WEB-04-browser-animation-frozen-red.json` SHA-256 `50a1f3abe9c65735738db8fa7fbf8c63bd763dab7e607f8d54df49e3de64a819` records both clocks/viewport; `WEB-04-browser-animation-frozen-red.webp` SHA-256 `c5cc72e07d300ed8f7df3b4fa4310cd0f11e43dfa74692a2878186d5c3f2c720` is the actual frozen browser view. A high-density screenshot is not acceptable if it freezes future live page content.

The helper owner removed only that same-geometry physical-marker suppression; newer frame bytes still require authenticated current page/geometry and bounded CSS or physical dimensions. Fresh source SHA-256 `186f9c651206cd92ae3d24998620a09eecd06a54e6e5e1c4ec4686de471771f0` repeated the private page with motion armed only *after* a MutationObserver saw the physical1678×1029 screenshot accepted three times. Remote tick again advanced **30→180**, and the actual presented canvas advanced **17→202 draws in 3.2s** (185 new, about57.8 FPS), with no alert and a visible later tick650 across the full-width page. `WEB-04-browser-animation-live-green.json` SHA-256 `c2dd0d169974cca4473f880e114a582989a76d1394d0f917c47c5dbf7bebddcc` and `WEB-04-browser-animation-live-green.webp` SHA-256 `bd326e1449d8e159ce641c007cfb153d358f43807692d4974ea2c4d08fc98958` preserve real output. The current stream can temporarily present CSS-density frames during animation; no future-content freeze is traded for a sharp stale still. Reviewer separately identified a pending held-input release/lease-handoff race under a hung screenshot; that source repair and packaged native Close remain open, so this evidence is not an increment completion.

### Native post-wheel density counterexample and held-input lease race

Final candidate helper SHA-256 `552acf44e67aff1dce9bbbe2b51cb3e2d8ee5ab9ab72d0fe01615e32ea5bc22e` in isolated WebKit `native-d8cdd1f9f647` first painted1348×376 for CSS674×188/DPR2. The remote page finished root scroll3900; WebKit painted362 frames/6.012s (60.21 FPS), and both left/right header samples were green across the whole viewport. All four nested/outer wheel actions changed the intended remote scroll offsets and painted again: inner0→180→110, outer3900→4060→3970. However, four seconds after those wheels, the actual native canvas was **674×188**, not1348×376. The run-owned trace recorded two physical1348×376 `Page.captureScreenshot` responses after the final scroll, with successful same-metrics reapplication, but subsequently admitted CSS-density packets displaced the sharp settled frame. This is a failed physical-density gate, not a passed native acceptance. Compact result `WEB-04-native-postwheel-css-red.json` SHA-256 `9fc42b6214f15f533266fcec2c3cadd6ea3d3ecb6178c901333362c9144c6b31`; actual native screenshot `WEB-04-native-postwheel-css-red.png` SHA-256 `96befd47ea1d95b7d27b4d18808ece3cd0d06336a4156eaf46f0d15040a0d849`. The private native gateway, Herdr, WebDriver, compositor and fixture exited; neither the default session nor installed app was touched. Worker owns the exact post-capture handoff repair, which must preserve authenticated changing CSS frames and avoid the previously reproduced animation freeze.

Separately, a throwaway direct-source harness extracted the old and current `retireView`/`releaseHeldInput` functions and supplied a never-resolving screenshot promise, held old-lease button at `(37,49)`, then synchronously retired that view and issued a new-lease down at `(99,99)`. Frozen pre-fix source SHA-256 `5bb6c1f0b95144e42f708e3326724457d0855fca83c564daee5daa1b93a73547` queued **only the new mousePressed**, leaving the old mouse held; candidate source queued **old mouseReleased at lease3 before new mousePressed at lease5**, with the new pressed state intact. This exercises actual extracted helper functions under simulated hung-CDP timing, **not** a native held-button physical gesture. Read-only review also found same-view `take_control` could race an awaited `release_control` and revoke the new lease; helper now revalidates state/controller/generation after awaiting old CDP dispatch. Reviewer inspected the current guard and found no remaining P1/P2 in this bounded path. Native Close and final helper-source runtime checks remain open.

### Final safe live-frame cutover; sharp settled bitmap still open

A one-packet, 100ms same-geometry CSS-frame drop briefly made a private native post-wheel bitmap settle at physical1348×376, but read-only review identified a **P1 data-loss counterexample**: that packet can be the sole genuine new marker painted after the screenshot, leaving the UI permanently stale. It was rejected and removed completely, not shipped. Increasing the number of otherwise identical screenshot/reapply attempts would repeat the measured CSS echo and has no demonstrated benefit. Final helper SHA-256 `552acf44e67aff1dce9bbbe2b51cb3e2d8ee5ab9ab72d0fe01615e32ea5bc22e` therefore admits every authenticated current CSS or physical frame and caps density refinements at two per exact geometry. The native `native-d8cdd1f9f647` red proof above matches **that exact final helper source**, so the 674×188 post-wheel bitmap remains an explicit unpassed WEB-03/WEB-06 quality criterion; the real native stream, full-width pixels, routed input and 60FPS remain passed independently.

Fresh private Chromium at 1568×1009/DPR1.25 loaded an owned remote animation page by the actual Page URL input, expanded the browser surface to1342×823, briefly presented physical1678×1029 frames, and then settled at CSS1342×823. After arming remote `requestAnimationFrame`, the remote page's posted tick advanced30→180 at CSS1342×823/DPR1.25; Cockpit's **actual canvas `drawImage` count** advanced26→220 in3.25s (194 new, about59.7FPS), and a later real screenshot showed animated tick560 filling the entire pane with Live browser view and no alert. This is a positive **live-frame/full-width** check and a negative **sharp settled bitmap** result, not a complete physical-density pass. `WEB-04-browser-final-live.json` SHA-256 `07e9bf964e982d0f1d50c57ca5d0ac994156d2a84e7f4e3bdd91e8ec5ec06e82`; actual `WEB-04-browser-final-live.webp` SHA-256 `75b5242e885fdef3456217e7b9e49c4535043b84f67ce6b6d54dfefe7a32185c`. `node --check`, frontend `bun run build`, focused browser/presenter tests 4/4, and `cargo build -p cockpit-host -p cockpit-tauri --bins` passed after reverting the unsafe frame drop. Full WEB-04 A04–A12 and native sharpness remain open.

## Accepted OBS-042 native density discriminant — 2026-09-24

Source `50b65ef186538795f07281cb9ec08647a5cb57ab`; product worktree clean at selection. The pre-existing `tasks.json` edit and `.audit/` are protected. WEB-05's product/evidence was committed in `0b0578498327b0038928f5bb323cc5414a6d95f8`, its disposable fixtures were cleaned and its overlapping locks released to queued. Main now owns WEB-04 (`app-shell`, `browser-ui`, `browser-helper`, `client-transports`); WEB-06's completion dependency remains, but density diagnosis may safely inform both. This bounded increment covers OBS-028/029/031/033's initial native fit/quiet continued painting and WEB-04's geometry/input precondition only; A04–A12, WEB-05 A14–A21/A24, WEB-06 resource matrix, WEB-08 security and PERF-01 remain open.

Current source path: `browser-runtime/browser-helper.mjs` applies requested CSS/DPR through `applyRequestedViewport`, captures measured `window.innerWidth/innerHeight/devicePixelRatio`, publishes authenticated JPEG descriptors through `enqueueFrame`, and schedules bounded density screenshots. `src/app/browser/framePresenter.ts` verifies frame envelope/identity and decode, while `BrowserPane.tsx` paints descriptor image dimensions and gates location-sensitive input against the confirmed frame. `scripts/verify/native_browser_acceptance.py` checks a private WebKit first canvas, fixture CSS/DPR, physical bitmap density, actual paint timestamps, wheel and cleanup. Prior run proved the first canvas 674×188 for a 674×188 CSS surface at DPR2, before annotation checks; prior WEB-04 traces also show a physical screenshot accepted then displaced by an authenticated CSS-sized screencast. Neither an immediate first-frame strict-density assertion nor a successful frame descriptor alone distinguishes eventual async convergence from permanent under-density.

Decision: retain the first usable authenticated same-target frame while native fit converges; never drop a sole newer real page frame, relabel dimensions, or change target/document/epoch/lease authorization to manufacture density. First extend only the **run-owned native diagnostic path** in the existing harness to sample initial canvas/fixture and actual canvas bitmap dimensions over a bounded convergence interval, with stage-specific trace flags and first/last paint timing, without changing its physical-DPR acceptance or treating a CSS frame as final success. If the trace isolates a specific stalled correction or mismatched capture/paint admission, accept a separately scoped source repair in the owning helper/presenter/UI file. A failed discriminant is evidence, not permission to apply an unmeasured screenshot loop or stall the stream. No user installed application, active/default session, shared profile, arbitrary OS input or protected path is touched.

Verification: one uniquely named private Herdr/Niri/WebKitDriver/fixture root using the current built frontend, host and Tauri binary with `--native-only-open --trace-cdp-overrides --trace-capture-lanes --trace-frame-publication`; record source/binary hashes, fixture CSS/DPR, bitmap size/paint sequence before and after settling, owned helper PID(s), emitted descriptors, screenshot dimensions, page scroll and final visible status. If a repair follows, rebuild once and run a *new* disposable native control plus browser page at DPR1.25: initial usable frame, eventual true physical bitmap without obscuring latest content, actual wheel/scroll repaint and quiet static state, no duplicate stale warning, and owned Close cleanup. An isolated hostile/stale document descriptor must remain rejected. Do not repeat the previous native test unchanged merely to reconfirm its red result. Record exact negative outcome if density still fails, keep WEB-04 active, and do not claim WEB-05/native or WEB-06 completion from this slice.

### First discriminant and accepted admission/consumer split

`scripts/verify/native_browser_acceptance.py --skip-build --native-only-open --trace-cdp-overrides --trace-capture-lanes --trace-frame-publication --trace-density-convergence --timeout 35` used private Herdr `native-5bac8417bff5`, root `/tmp/cnative-kj7t4a8p`, isolated Niri DPR2, WebKit and fixture `127.0.0.1:41003`. First real frame was 1348×376 over CSS674×188.47; the fixture independently reported 674×188/DPR2. Initial five-second stationary sample remained physically dense; six-second animated scroll reached 3900 and 361 real `canvas.drawImage` paints in 6.006s (60.10 FPS), with final green marker. The trace recorded **two live same-target helper PIDs**, `2697411/32690586` and `2697465/32690625`, each producing at least 256 screencasts and 256 frame descriptors; both recorded physical 1348×376 screenshot results. This successful run does not invalidate the previous native DPR1 red or prove post-scroll density/one-owner resource acceptance. The initial trace invocation failed before launching product because the opt-in source anchor had stale indentation; the repaired run completed and cleaned its owned gateway, driver, compositor, Herdr and fixture. All identifiers are disposable; the raw diagnostic JSON and screenshot remain under its `/tmp` root until compact evidence is retained.

Source shows a concrete admission-to-publication gap in `BrowserHelperSupervisor::open`: two concurrent opens can both pass `existing_view_id` before either inserts its first managed view. Prior per-association startup reservation removed the duplicate in one run but degraded actual native paints to 1.16 FPS and was **reverted**; merely serializing startup is therefore not accepted. Two exclusive code reviews/possible bounded repairs may proceed: Host worker owns only `crates/cockpit-host/src/browser_helper.rs` and existing focused host tests, using a key `(association_key,browser_incarnation)` that reserves startup without holding registry locks over awaits; it must preserve initial frame/barrier and follower grant, avoid serializing established frame delivery and return without a speculative fix if it cannot account for the prior throughput counterexample. UI worker owns only `src/app/browser/BrowserPane.tsx` and existing focused component tests; identify whether same native view issues redundant overlapping open requests due effect identity churn or mount/remount, and remove only confirmed duplicate opens without suppressing legitimate separate observers, hide/show, target navigation or stream-close cleanup. No wire/API change or cross-file edits. Main owns integration and the run-owned native/browser verification fixture; neither writer runs builds/tests/runtime/formatters or commits while writing. On integration require one authenticated owner/helper per association, a separate observer with its own grant, >15 actual native painted FPS while scrolling, static density/fit convergence, no stale frames or lost input, and private Close cleanup. If either candidate recreates low FPS or zero paint, revert that source change and retain diagnosis, not a process-count-only win.

### Integrated one-helper red and next bounded density repair

The browser stream-effect fix removed `associationChanged` (a render-mutable boolean) from dependencies; Main moved its previous-key update into effect setup so a genuine association switch still retires old draft/stream state. Frontend build and five focused BrowserPane/presenter tests passed. Read-only lifecycle review found no introduced P1/P2. A **new** private native run against this built frontend (`native-86ee8a30ead1`, `/tmp/cnative-whm97xd4`, fixture `127.0.0.1:46703`) observed exactly one helper at the first canvas and a usable full-width 674×188 CSS frame, but its bitmap remained **674×188 at DPR2 for the full five-second convergence sample**; the original native gate failed before animation. Both old and new runs cleaned only their private descendants. Do not count one helper as success.

The opt-in trace narrows the cause: the sole helper published a true 1348×376 screenshot as frame sequence 2 **for the prior document generation**; the new page later published CSS-sized frames at the new generation. At the new generation CDP returned physical 1348×376 screenshots (sampled `screenshot_accepted` count 4 overall), but no physical frame was published after the later CSS frame. The density capture pins `expectedStreamArrivalSequence` and frame sequence; a newer screencast packet arriving while the screenshot is in flight makes the screenshot stale. The current two-attempt timer arms only from screencast delivery or resize, so a stale final attempt after the last stream frame has no trailing idle retry. This is a concrete async-correction lifecycle failure, not an absent physical CDP bitmap or proof that suppressing newer CSS frames is safe.

Accepted narrow repair: in `browser-runtime/browser-helper.mjs` density-refinement scheduling only, re-arm **one bounded identity-pinned trailing correction** after a stale density attempt when the current target/document/viewport still needs physical density and no newer input/resize/scroll was requested. Use current capture baseline and re-evaluate sequence/geometry at retry time; do not reuse stale bytes, poll static pages continuously, suppress authenticated stream packets, or relax frame/lease/epoch gates. Stop on successful physical publication or retired identity; bound total snapshot work per exact geometry and cancel timers on stream detach. Revalidate with a fresh private native run measuring first usable frame, async DPR2 convergence, >15 actual FPS after page scroll, latest painted content and Close; Chromium DPR1.25 browser frame/resize/navigation control follows. If the screenshot still races or native throughput collapses, retain red evidence and re-plan at the earlier stage rather than shipping speculative fallback.

### Native one-helper density ordering after two failed repair attempts — 2026-09-24

The first helper scheduling repair (three instead of two density attempts and a stale identity-pinned idle re-arm) did **not** satisfy the strict DPR2 first-frame and settled-scroll criteria. Separate browser readiness repair now waits for the open stream command handle before reporting `Live browser view`: a first real native navigation had submitted the URL while the frame was already visible but the command handle was still connecting; a focused regression exercises attached + painted frame before the open promise resolves. Frontend build and six focused BrowserPane/presenter tests passed after that change. Neither is accepted as a completed density repair.

Native run `native-b8ee7fed36f0` (private `/tmp/cnative-wy7_wzat`) navigated through the actual Page URL field, painted true 1348×376 over CSS674×188.47/DPR2 at first observation, used **one helper**, and painted 361 frames/6.001s = 60.16 FPS while the fixture scrolled to 3900; its five-second post-scroll samples ended at **674×188**. A stricter post-scroll assertion and run-only density-result trace were added without relaxing the original first-frame gate. `native-de3687867e3e` (private `/tmp/cnative-ggve0r21`) painted 1× first, corrected to 2× in .204s, but still failed the unchanged strict first frame. An opt-in diagnostic continuation preserves the failure while observing scroll rather than claiming a pass. `native-081c74c893bd` (private `/tmp/cnative-tj_0k1pr`) met first DPR2 and ~61 FPS yet finished its five-second post-scroll sample at CSS resolution. All disposable Herdr/compositor/gateway/WebDriver/fixture descendants in these runs were cleaned.

**Distinguishing producer/consumer trace:** `native-11328524095c` (private `/tmp/cnative-2vl50hen`, cleaned) showed accepted physical CDP captures published by the sole helper and received by the native client as frame sequences **368, 370, 372**, each with matching target, document, viewport revision 362, scroll 3900 and physical1348×376 dimensions. The producer then published and the client received **CSS674×188 frames 369, 371, 373** at the *same* target/document/viewport revision/scroll only 9–13ms after each physical frame; the native canvas painted them and remained CSS-sized for five seconds. The page was live (~60.13 drawn FPS during scroll), with no JS alerts. This rules out physical screenshot rejection, frame socket loss, or image decode as the *settled* defect; increasing snapshot budget merely repeats the alternation. Strict first-frame also failed in this run (1× then 2× in .204s).

The **single run-only helper-copy experiment** `native-8772ce0469b2` (private `/tmp/cnative-dt71qaqw`, cleaned) omitted only the post-screenshot identical CDP metrics override and instead measured the actual DOM viewport while retaining the existing geometry verification. Override sends dropped to the initial committed plus one noncommitted send, yet physical **367, 369, 371** were still followed by CSS **368, 370, 372**, and final canvas remained 1× after five seconds; first-frame was 1× then 2× in .205s, scroll remained ~60.11 drawn FPS. Thus redundant restoration is **not** the sole cause. It does **not** prove the CSS packets are duplicates: a real same-geometry page update may have arrived after a physical capture. No production frame suppression, identity relaxation, or acceptance reduction is justified. After two failed density repairs, stop editing that criterion; consult the bounded read-only advisor on a discriminator for CDP frame capture timestamps/content and keep WEB-04 in progress/uncommitted pending a safe ordering design. The original status ledger and WEB-05/06/08/FLOW/PERF/ACCEPT criteria remain open.

### Focused shared-frontend proof and advisory boundary

An independent Chromium session against owned Herdr `polish-_r9lsrhp` (`/tmp/cpol-_r9lsrhp`, gateway `127.0.0.1:34389`, fixture `127.0.0.1:38663`) opened the actual browser toolbar, navigated through its Page URL form, observed the authoritative tab title `Polish annotation fixture` and confirmed URL, rendered the real remote page in a 513×764 canvas over its full 512.9×764 CSS surface, and clicked its actual remote button: visible counter **0→1** with `Live browser view`. The Close action persisted: a fresh Cockpit tab for the same disposable session offered `Open browser`, not `Close browser`. Browser tab and the fixture's recorded processes were closed/stopped; the user's session was untouched. The first automation `fill` appended the new URL to existing text and navigated to a Not found path; it was classified as an **automation input-selection error** rather than a product navigation failure after explicitly selecting the Page URL input, typing physical keystrokes, and observing the corrected authoritative navigation and page pixels. This focused pass does not satisfy the complete WEB-04 A04–A12 matrix, native OS input or density.

Read-only advisor `DensityOrderingAdvice` confirmed the producer/consumer trace proves delivery order and density but **not pixel acquisition order**. Reject suppressing the later CSS frame based solely on matching geometry or arrival time: a live page may paint newer content at the same viewport. Its recommended discriminant is a run-only, deterministic, large raster generation marker decoded from neighboring physical screenshot and CSS screencast JPEGs, with one quiet capture and a controlled intervening mutation; source clocks need explicit calibration before any cross-mode comparison. A trailing newer generation demands preserving live content and investigating an engine-supported DPR-sized stream, not stale screenshot promotion. The advisor did not approve a product repair, and its opinion is not acceptance.

### Native raster-order discriminant; density repair remains open

Run-only `native-1b4933873a22` (`/tmp/cnative-dahrh2k4`, fixture `127.0.0.1:44047`) used a disposable helper copy and a fixed 120×80 CSS-pixel marker in the private page. Bounded JPEGs from the accepted screenshot and published frames were decoded at the same marker location using Pillow. After scroll 3900, capture-1 and physical frame 372 were 1348×376/G0; subsequent CSS frame 373 was 674×188/G0. Capture-2 was 1348×376/G0; immediately after its response the controlled page mutation returned G1; physical frame 374 still showed G0, while the next CSS frame 375 showed **G1** at the identical target/document/revision/scroll. Capture-3 and physical frame 376 showed G1, followed by CSS frame 377/G1. Trace records mutation at monotonic 161744678ms, physical 374 at 161744682ms, and CSS 375 at 161744689ms; generation colors decoded as G0 `(239,0,241)` and G1 `(0,240,239)`. This proves the trailing low-density frame can be genuinely newer content, not always a duplicate; suppressing it solely on viewport identity or arrival would lose a real update. It does not establish that every trailing CSS frame is newer or identify an engine-supported full-density streaming transport.

The unchanged native first DPR2 gate passed in this run; the private fixture scrolled at 361 actual canvas paints/6.006s (60.10 FPS), but the five-second settled sample ended at CSS674×188 over CSS674×188.47/DPR2. The diagnostic intentionally reported `ok:false`; no product repair follows without a new stream design. All owned helper, WebDriver, compositor, gateway, Herdr, HTTP servers, and private tabs were stopped. Next check: determine whether WebKit can deliver DPR-sized continuous screencast or whether fresh physical captures can preserve newer page content at the required animation rate without discarding authentic CSS frames.

The next run-only engine capability check, `native-569ffd51499d` (`/tmp/cnative-dy5c7suq`, cleaned), set `Page.startScreencast` `maxWidth`/`maxHeight` to the DPR2 physical bounds in a disposable helper copy. WebKit accepted the stream but kept emitting **674×188** JPEGs over CSS674×188/DPR2, including during the six-second scroll (~60.11 actual canvas paints/s) and immediately after each physical1348×376 screenshot (sequences 369→370, 371→372, 373→374). The first canvas was 1× and the five-second settled sample stayed 1×; diagnostic `ok:false`. A larger screencast size ceiling cannot fix physical density on this observed engine. Next discriminant is snapshot-only throughput under real scroll/input, not another CSS suppression or density retry.

Run-only throughput probe `native-e12874c2a23a` (`/tmp/cnative-57b98bpx`, cleaned) made **sequential** `Page.captureScreenshot` requests while the real CSS screencast remained active. During actual six-second native-driven scroll it decoded **180** physical 1348×376 JPEGs over **5.965s** (~30.18 captures/s), and the native canvas painted **183** times over the animation (~30.47 FPS). The capture lane could satisfy a >15 FPS throughput budget under this fixture; it did not publish the probe JPEGs or establish their ordering relative to later CSS frames. The diagnostic was intentionally red, not an acceptance pass. Next: a disposable **screenshot-only** ordered playback prototype with screencast stopped, one capture in flight, fresh target/geometry checks, and native input/scroll/quiet-frame observations. Do not adopt continuous polling or change product capture architecture until that full path proves acceptable and idle/resource behavior is understood.

**Checkpoint decision:** the three-attempt/rearm product timer was reverted to the two-attempt baseline after its two failed native density repairs. The stream-effect ownership fix and painted-frame/readiness gating in `BrowserPane` remain: actual disposable browser UI navigation/click/Close passed, and the earlier private native runs observed one helper instead of duplicate starts. Focused BrowserPane/presenter Vitest **6/6**, `bun run build`, `node --check` on restored helper, and verifier `py_compile` passed after the revert. These are a verified lifecycle/readiness subrepair and repeatable diagnostic harness, **not** WEB-04 completion or native physical-density acceptance. Do not reinterpret the run-only snapshot throughput as authorization for permanent capture polling; the original WEB-04 A04–A12/browser/native matrix and native density requirement remain open. With no currently justified bounded event-driven physical capture repair, release WEB-04's locks at a clean checkpoint to advance independent required campaign tasks, retaining this exact blocker and discriminant for resumption.
