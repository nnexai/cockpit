# NATIVE-01 — owned macOS bundle installation

## Accepted plan

Main/Astra accepts this plan on 2026-09-21 against `6593cf4ecba87687c809ee9f3d2a295c757db787`. RUN-01 is complete. Current owned campaign edits remain protected. Original `tasks/NATIVE-01.md` criteria 1–6 are retained; OBS-013 explicitly assigns actual macOS execution to user verification. Lock: installer.

Inspected current InstallPaths, build arguments, schema-1/2 receipt loading, ownership checks, per-file atomic publication and uninstall. Current macOS flow incorrectly shares Linux --no-bundle/binary installation. Read issue #5's proposed patch as evidence; adapt it, do not copy its ownership assumptions.

Design: Darwin builds an app bundle (`--bundles app`) and installs `target/{debug,release}/bundle/macos/Cockpit.app`; Linux keeps --no-bundle and current binary/desktop behavior. --reuse and --debug select the same artifacts as a normal build. Add an explicit disposable bundle-destination option; default to the user's Applications location, and choose a deterministic prefix-local Applications location when --prefix is used. Print resolved targets. Never use sudo or force-stop a running process.

Validate Info.plist application identity and its declared executable, including safe relative path, containment and executable mode. Copy the whole bundle preserving permitted internal relative symlinks and modes; reject unsupported file types and escaping links. A receipt must bind the exact destination and a deterministic content/type/mode/link-target manifest, not only the main executable. Schema migration may preserve known old ownership but never adopt an already-existing foreign bundle or CLI path.

Publication: stage completely in the destination filesystem, verify the staged manifest, and journal the exact owned staging/backup/target identities before replacing anything. Directory backup/publish renames are NOT an atomic exchange. On handled failure restore a verified previous installation where possible; on interruption retain an explicit recoverable pending record, never a successful receipt. Subsequent recovery must verify journaled manifests/paths before moving or removing anything; modified/unknown states are refused. Include launcher/CLI publication in the success/rollback accounting so no successful receipt describes mixed generations. Use a bounded installer-specific transaction, not a general framework.

Update/uninstall checks all receipt-owned bundle content and modes before mutation and refuses foreign/modified artifacts. Preserve user config, borrowed paths and running processes. Keep schema-1/2 Linux receipt support and existing CLI-launcher behavior. No signing/notarization/auto-updater work.

## Ownership

Worker owns `scripts/install-native.py`, `scripts/test_install_native.py`, and installer-specific package command changes only if essential. Main owns documentation integration because WEB-07 changed `docs/native-install.md`; return precise proposed doc changes, do not edit that file concurrently. Do not edit Tauri runtime, browser helper or shared protocol. Python LSP is unavailable. Inspect existing installer test conventions and Tauri bundle identity before implementation. Skip every test/build/formatter/service/install/runtime command and commits during the writing wave.

## Verification and blockers

Per user OBS-011, long validation remains deferred. Original macOS checks are retained in [MACOS-HANDOFF.md](MACOS-HANDOFF.md): fresh install/two-build update and launched identity; wrong identity/foreign/modified refusal; publication failure with recognized previous or explicit recoverable state; safe migration/uninstall/config retention; installed startup/resource discovery. Their status is **user verification—not executed**, not passed.

OBS-013 supersedes the original missing-macOS blocker. Reachable delivery requires implementation, read-only ownership/publication review, final Python behavior checks, the real owned Linux prefix install/update/uninstall scenario, explicit macOS handoff and an owned commit. Do not acquire/emulate a runner or substitute Linux for Darwin proof. All verification destinations remain owned disposable roots; no installed user app or default Herdr session is touched.

## Implementation and bounded evidence

Darwin now uses a schema-4 bundle-only layout: the graphical launcher targets `Cockpit.app/Contents/MacOS/cockpit-tauri`; fresh installs contain no redundant raw GUI copy. Linux remains schema 3. Verified schema-1/2/3 Darwin raw binaries retire through journaled before=file/after=absent transactions with rollback, not direct deletion. This follows the bounded Astra launcher consultation; Main retains verification ownership.

The read-only installer review found and the worker repaired consistent lexical launcher identity, canonical-parent overlap checks, missing-owned-bundle refusal before deletion, and platform-neutral binary-only test fixtures. Prior ownership, staging cleanup and transaction findings were repaired as well.

Final delegated verification after those repairs: `TMPDIR=/tmp/csg-a3e9b950 python3 -m unittest discover -s scripts -p test_install_native.py` — **15 tests passed** (0.055s). Expected refusal stderr: `kept unrecognized path: binary`.

Real Linux CLI scenario, all successful: fresh `--reuse --debug --prefix /tmp/csg-a3e9b950/native-cli-smoke-final-20260921` install; installed `cockpit-cli --help`; repeat update; `--uninstall`; removal of that uniquely owned prefix. HOME/XDG paths were owned fixture paths. No service/GUI was launched. Evidence: [NATIVE-01-linux-evidence.json](NATIVE-01-linux-evidence.json). The existing debug binaries were exercised as installer artifacts, not asserted to include subsequent campaign source edits.

`docs/native-install.md` describes platform build/layout, XDG isolation and transaction limits. Its unrelated WEB-07 hunk is excluded from this increment's commit. No throwaway implementation or build artifact is added to the repository. macOS runtime results remain explicitly unexecuted; the adjusted delivery closes only with the owned installer commit recorded in `tasks.json`.

## Accepted OBS-032 Linux installed-process freshness addendum — 2026-09-23

Baseline `96f79c81768428413027c59bbc12a127bb8a0c93`; macOS runtime remains user-deferred, and Linux prefix smoke previously proved only install/update/uninstall and CLI, not a graphical process exit. User now requires a new `native-install` followed by a new launch to use current Cockpit code/assets, and closing Cockpit to leave **no Cockpit-owned backend/service** running (Herdr session persists). Their stale backend theory is a hypothesis: do not label it the cause of flicker/small image without process/build evidence. This native Linux lifecycle acceptance belongs jointly to NATIVE-01/WEB-06/ACCEPT-01, not an excuse to stop their active app.

Exclusive worker C owns `scripts/install-native.py`, `scripts/test_install_native.py`, `src-tauri/src/lib.rs`, and if proven necessary `crates/cockpit-host/src/browser_runtime.rs` or `browser_helper.rs` (worker A owns only helper JavaScript; worker B only frontend/core browser drafts). Read-only inspect the installed launcher/receipt and running process facts if useful, but never signal, replace, attach to, uninstall, overwrite config for, or navigate the user's installed application. Trace actual Linux launcher target, source build/native embedded asset identity, whether native process starts a separate Cockpit gateway, ownership of Chromium/helper children, and `RunEvent::ExitRequested` shutdown path. Correct only a reproduced ownership/lifecycle error; no new daemon, watcher or global kill-by-name. Herdr stays independent, an unrelated sentinel browser/helper cannot be killed, and a failed helper shutdown cannot strand the GUI indefinitely. If installer is already correct, report that with evidence rather than changing packaging for a rendering symptom. Skip builds, tests, services and commits during writing.

After integration, install a freshly built distinct artifact **only to a disposable prefix** with isolated HOME/XDG, launch it through that prefix's real `.desktop`/launcher against a uniquely named disposable Herdr session, compare running executable inode/hash and frontend asset identity with the just-installed artifact, use the app's actual Close action, and enumerate only run-owned Cockpit gateway/helper/Chromium descendants and sockets until reaped; the private Herdr server must remain available. Relaunch after a second distinct disposable build/update to prove no stale process or resource mix. Inspect installed user files read-only at most; never run `native-install` on the real prefix or kill a PID without proven ownership. A source-tree Tauri run or installer unit test does not pass this criterion. Record exact identities, close result, sentinel and cleanup in this run, keeping original macOS handoff untouched.

### Integration review P1: bound each exact-owned close, not the whole receipt pass

Private native scroll fixture `native-b3013b772d38` reached correct first page CSS 674×188 at DPR 2/1348×376 canvas and **361 actual draws/6.0009 seconds = 60.16 FPS**; its first nested wheel went to outer page (scrollY 4080, `nestedWheelY=null`), so this is red for input despite good stream motion. The run-owned WebKit/Niri/gateway/Herdr processes stopped. Read-only reviewer `StreamNativeReview` found a separate close P1: a new fixed five-second wrapper around `BrowserService::shutdown` can cancel its *second* exact-owned receipt mid-close (each Playwright CLI close has a 15-second internal bound), leaving Chromium alive and a `Closing` receipt while GUI exits. Worker C removed the whole-service 5s wrapper and stops the owner listener before the browser receipt pass while retaining the exclusive owner lock; the current 1024-receipt sequential worst case remains impractically long. Main accepts a narrow exclusive extension to `crates/cockpit-core/src/browser.rs::BrowserService::shutdown`: process already validated loaded receipts with at most four active close+store operations via existing Tokio facilities under the existing `operation_lock`/`shutting_down`; attempt and persist **every** receipt even if another fails, preserve original-order first error where possible; do not add a new wire/API shape, unbounded fan-out or blanket process kill. Core browser draft file is separately owned by the UI worker, no other writer touches browser.rs. Rebuild once after all follow-ups and then run installed-prefix GUI Close with at least two run-owned associations plus private Herdr remaining; do not call a source-only shutdown check proof.

### Installed Linux Close: WebDriver teardown is not window Close

First disposable debug install of source SHA-256 `552acf44e67aff1dce9bbbe2b51cb3e2d8ee5ab9ab72d0fe01615e32ea5bc22e` used `/tmp/cinstall-stream-vQqZ6f`, not the user prefix. The run-only verifier initially identified the installed app by its source-build `cockpit-tauri` process name, which is wrong for the installed `cockpit` executable. After switching to exact `/proc/<pid>/exe` inode matching, a private **native-owner** run (no independent gateway) proved the installed hash `370fc51e14fdaf3c3ae0ffd4c9eb0c3a78da9db0fac3bf39bc9309d430291a3e`, inode462920 and owner-socket ownership, but WebDriver `DELETE /window` terminated all three GUI/WebKit PIDs while leaving `owner.sock` behind after15 seconds. `NATIVE-01-installed-close-red.json` SHA-256 `47961a0767957249618ae96947ca26780e5d9cd5388311301cbc6e3b42742211` preserves those exact identities. A preceding attempt with a separately started browser gateway was an **observer-role verifier error**, not proof that native owned that gateway's socket.

Read-only `NativeCloseAdvisor` reviewed Tauri's `ExitRequested { code: None }` callback and owner unlink ordering; its advice to discriminate actual Close-event delivery rather than guessing a new hook was accepted. A **temporary, unshipped** private trace path verified the installed binary's startup marker; repeating WebDriver `DELETE /window` emitted no Tauri close/exit event before process termination. In contrast, `niri msg action close-window --id 2` targeted the **only** private Cockpit window (`app_id=cockpit`) in the isolated compositor. Tauri delivered `CloseRequested → Destroyed → ExitRequested(None) → prevent_exit → browser shutdown_ok → app.exit(0) → ExitRequested(Some(0)) → Exit`; its exact owner socket and all three PID/start-tick identities disappeared while the private Herdr session remained alive. `NATIVE-01-os-close-event-trace.txt` SHA-256 `5c9d9728b021b26e63cc487477e390bf2031f25e8dfe587360a0c05f492731b5`, actual installed WebKit screenshot `NATIVE-01-debug-owner-close-green.png` SHA-256 `68ff2511d2eb574919794b4d035aa2176e43cee369c219730ad6bc819c34cb53`. The temporary Tauri trace code was removed completely before packaging; **only a real compositor Close**, not WebDriver session deletion, can close this Linux-native acceptance gate. Uninstrumented debug and second distinct release install/update checks follow.

The **uninstrumented** rebuilt debug executable again matched its installed SHA-256 `370fc51e14fdaf3c3ae0ffd4c9eb0c3a78da9db0fac3bf39bc9309d430291a3e` at running inode471917. Fresh private native-only session `native-e90eec058b4f` opened the browser via its real Tauri UI; page CSS674×188/DPR2 first reached1348×376 bitmap, then actual WebKit drew361 frames/6.0083s = 60.08 FPS while root scroll reached3900 and the entire page header was green at both edges. Real routed inner wheels moved 0→180→110 and outer 3900→4060→3970, each followed by a canvas paint, with no visible alert. Niri Close of exactly its owned window id2 reaped the native/WebKit PID-start-tick triplet and native-owned `owner.sock`; separate run-owned Herdr remained alive. `NATIVE-01-debug-owner-close-final.json` SHA-256 `8181a29ecf8f3b4a0c0148cff7a4cf3844242e2d33b08e9812233f679e6f853d` and actual WebKit `NATIVE-01-debug-owner-close-final.png` SHA-256 `98497ca363e18210684bb90367f3f83cdc1cf7e38ae05988aa6f8d9b549fd191`. The settled bitmap after wheels was only CSS674×188 despite correct full-width content; WEB-03 physical-density follow-up is **not** closed by this shutdown pass. Distinct release build/update evidence follows.

### Installed release update and first-Open cancellation regression

The first *distinct* release build updated only `/tmp/cinstall-stream-vQqZ6f`; the installed executable SHA-256 `ba7e84c33c42a5a0804376491df4a6b7198b9ee8f990e523ad6d46ac855dfe1c` matched `target/release/cockpit-tauri`, and WebKit loaded its embedded `tauri://localhost/assets/index-CzXuBxoL.js` rather than the debug HTTP frontend. In three private native-only sessions, the first real command-palette Open intermittently left the terminal visible for 12, 20, and 35 seconds even though the exact Space association receipt was `open` with a CDP target. In the 12-second red case, the Workbench request ref advanced 5→10 while the session, selected Space and epoch matched before/after; one more Open click displayed the already-open association. A fourth run's first click succeeded, proving this was intermittent rather than a persistently missing release asset. [NATIVE-01-release-open-race-red.json](NATIVE-01-release-open-race-red.json) and the retained red screenshot record the failure; the prior green release screenshot does **not** erase it.

`src/app/App.tsx` now invalidates an in-flight browser action when the selected Space, session, or session epoch changes, **not** merely when Herdr sync leaves/re-enters `live`. The response still must match the same exact session/Space; while sync is unavailable, the view remains display-only. After a second real release build/update, installed SHA-256 `b3791212bc14b834c3f226701d66baa7510461b74178ad3c8a4b341a9096e44e` matched `target/release/cockpit-tauri`, and each run loaded embedded `tauri://localhost/assets/index-Bx3kSYin.js` (dist SHA-256 `04a409ae93e0e2b02661287e26271a9b97e6c495908861c50847c9cfa93f116f`). Three fresh, uniquely named native/Herdr sessions (`native-76c6f535606e`, `native-8ca384e674c0`, `native-8edd43394404`) each completed its **first** Open, fixture navigation, full-width DPR2 first bitmap 1348×376 over CSS674×188, 361 measured native canvas draws/6 seconds (60.11, 60.06, 60.11 FPS), all four nested/outer wheels and paints, then a real private Niri window Close. Each exact installed GUI/WebKit PID/start-tick triplet and native-owned `owner.sock` was gone; its separate private Herdr remained alive. Full run results: [run1](NATIVE-01-release-sync-guard-run1.json) SHA-256 `d16e111f3858319627575ac0069ddb5ced11705cb161227259fe54201f6658df`, [run2](NATIVE-01-release-sync-guard-run2.json) `507e51249b21fc508777211101ae070ddfd40583605dfb7d444c4db4d32d6542`, [run3](NATIVE-01-release-sync-guard-run3.json) `1c94f4f2c88e3d1da8932d7f7ec44f7866eb88d2d7611d0717e188937a095c09`; [first physical frame](NATIVE-01-release-sync-guard-first.png) SHA-256 `f4a2d59d4db1755bdd32b37025725ad13a2d5633ad2a082722370eeedef05156`, [post-wheel visible frame](NATIVE-01-release-sync-guard-window.png) `b2f5ce4031baf9df454661186e21ce18b78101e13cfce02119a1cb3d5d393f0e`.

This verifies two distinct disposable installs and real native Close of one exact association per run, **not** close of two concurrent associations or macOS execution. Settled native screencast still replaces physical pixels with CSS-density frames after wheels; high-DPI sharpness remains red. Neither a WebDriver window deletion nor a successful screenshot alone is recorded as proof of complete NATIVE-01/WEB-06 acceptance.
