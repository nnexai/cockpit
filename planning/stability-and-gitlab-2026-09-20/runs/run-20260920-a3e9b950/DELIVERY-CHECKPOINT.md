# Final batch checkpoint

The final authenticated workflow completed after correcting only the owned gateway's D-Bus routing. `final-workflow-surface.json` records MR1 resolution, explicit reviewed effects, setup operation `23316d57-9d6a-49ca-af63-ff8a5386d362`, workspace w2, source import, dirty-worktree removal refusal, and unchanged sentinel. Main inspected the reviewed-effects screenshot. There were no remote mutations during this final workflow. Comment/reopen/refresh-conflict, successful paste and clean teardown were not executed; the retained dirty checkout prevented reuse of the fixture branch.

## Cleanup executed

- Closed the main owned browser with `cockpit browser close --space w3` and explicit csg-a3e9b950 socket/config: response `closed`, profile retained.
- Native CLI owner was already absent (`browser_owner_timeout`); the exact owned Playwright session `cockpit-1db31ad63008fe154ef21cb6` then reported it was not open. No unrelated browser was closed.
- Stopped csg-gateway, csg-flow-gateway, csg-final-fixture, csg-herdr, csg-herdr-nativefinal, csg-herdr-flowfinal, csg-webdriver and csg-display through their owned process handles. Native app/Vite were already stopped by their worker.
- Retained `/tmp/csg-a3e9b950` evidence, profiles and worktrees, especially the deliberately dirty workflow checkout and companion, rather than force-deleting them. `final-resources.json` records stopped session authorities.
- Retained the owned GitLab issue2/MR1/source branch for macOS handoff. No merge, approval, protected-branch write or remote deletion.
- Six OBS-008 HOME files remain unchanged, per explicit user authorization.

All writing workers are settled. No source edit was made for the disproven precision hypothesis. Full campaign completion is deliberately not claimed: `BATCH-FINAL.md` names the remaining browser scroll barrier and native annotation acknowledgement failures and exact unexecuted scenarios. The final status checkpoint records the real implementation commit, releases active locks, and preserves all 25 task identities and both deferred provider tasks.
