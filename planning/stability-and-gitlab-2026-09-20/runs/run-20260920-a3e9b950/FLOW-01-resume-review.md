# FLOW-01 continuation — linked-checkout Review identity, 2026-09-24

This is a new runtime record. It supplements, and does not replace, [`FLOW-01.md`](FLOW-01.md) and [`FLOW-01-resume.md`](FLOW-01-resume.md). The parent task remains open. No product files, tests, build outputs, campaign ledger, planning authority, or commits were changed by this worker. No build/test/format/lint was run.

## Browser actual-UI reproduction

- Private root `/tmp/cflow-reviewresume-20260924`; unique Herdr session `flowreviewresume20260924`; parent Space `w1` at `/tmp/cflow-reviewresume-20260924/repo`, initially committed `f1f4e5183fe29e5c7c56544262393851bad04f24` on `main`; private browser gateway `127.0.0.1:36371`; repository/worktree/companion/state roots and `HOME`/XDG are under that root. The private Herdr socket was passed explicitly to the gateway. A real supported installed Pi agent `pi-flow` was started idle in `w2:p2`, terminal `term_65c36f91537b63`, selected child tab `w2:t2`; no prompt or Enter was sent.
- Cockpit's real browser **Set up a task Space** reviewed and created owned child Space `w2` **FLOW Review Resume**, branch `flow-reviewresume-task`, operation/companion `8babd18a-3021-414a-9e9d-e21e6c750fa8`, generation 1. Checkout `/tmp/cflow-reviewresume-20260924/worktrees/repo-8babd18a-3021-414a-9e9d-e21e6c750fa8`; companion `/tmp/cflow-reviewresume-20260924/companion/8babd18a-3021-414a-9e9d-e21e6c750fa8`; manifest state `completed`, generation 11, workspace `w2`, tab `w2:t2`, pane `w2:p2`, and both worktree/companion explicitly recorded as owned. Browser hierarchy visibly showed parent `repo` → child `FLOW Review Resume`.
- The worktree is an actual linked Git checkout, not a nested ordinary directory: `git rev-parse --show-toplevel` returned the child path; `--git-common-dir` returned parent `/tmp/cflow-reviewresume-20260924/repo/.git`; `--git-dir` returned parent `.git/worktrees/repo-8babd18a-3021-414a-9e9d-e21e6c750fa8`; branch `flow-reviewresume-task`; HEAD `d129cfb06edeb2b60916594f7074a6a8e7ac0297`; status was clean at the identity check. The checkout `.git` file SHA-256 was `c09ba7e3a44d2b4899de87c0eb77876789cf183702ddb456bce4d7ae8f9bd127`.
- Actual **Commands → Open Review right** from the selected child terminal produced a visible inline error rather than opening Review. Captured browser POST `/api/v1/sessions/flowreviewresume20260924/review/open` body: `{"pane_id":"w2:p2","binding_id":"sha256:c7410190e56436d5c029e4767d2bba175e9e9b4aa169f561c811011047136969","repository_id":"repo-f322fb25adcde05764625444c5a7facf88f6f6b0bad4f8cfc6e3d12dd9b85fbf","direction":"right"}`. Actual response: HTTP 503 `{"code":"context_root_not_authorized","message":"selected repository is not the source pane's current checkout"}`. A safe UI Refresh followed by a fresh authoritative presentation GET preserved the same pane binding and roots; another Review-open produced the same refusal. No Review diff/comment workflow is claimed for this run.
- The exact presentation GET response had `extension:null`, `renderer:null`, `confidence:"none"`, `can_open_review:true`, and `default_root_id:"companion:8babd18a-3021-414a-9e9d-e21e6c750fa8:54:616242"`. It contained a repository root at the child checkout with `repository_id:"repo-63a58721a721bc27cd6411a14b32b7bf650fc47178a3b983384c480efe200fb7"`, and a companion root whose `repository_id` was the parent/setup ID `repo-f322fb25…` and `checkout_path` was that same child checkout. Herdr's live `api snapshot` confirmed `w2/t2/p2` focused and Pi cwd/foreground cwd equal to the linked child checkout. Thus the actual request was not stale after Refresh: it selected the companion root's parent ID while the normal terminal's live checkout resolved to the child ID.
- Source localization sent to Main: `src/app/paneRenderers.ts:149-156` inspects the pane, chooses `default_root_id` for Review when that root kind is repository or companion, and sends its `repository_id`; `crates/cockpit-core/src/context.rs:908-929` resolves the normal pane's actual cwd with `RepositoryCatalog::discover_checkout` and rejects a differing ID. `crates/cockpit-core/src/context.rs:995-1025` chooses a companion as default root for an ordinary terminal with an associated companion. `crates/cockpit-core/src/repositories.rs:400,914-938` hashes root, Git common dir, and checkout into the ID, so parent and linked child checkout IDs differ. This is a confirmed browser-visible linked-checkout Review defect; Main owns the scoped source repair.

## Separate native Review attempt

- A first native attempt incorrectly reused the browser fixture; it was stopped and explicitly excluded from native evidence. The valid native attempt used separate private root `/tmp/cflow-native-reviewresume-20260924`, short isolated XDG/Herdr configuration `/tmp/nrr24/c`, unique Herdr session `nrr24`, native-owned Niri/WebDriver process tree, and private frontend static server on port 5173. It ran the real `target/debug/cockpit-tauri`; no user/default Herdr session, profile, socket, repository, or browser was used.
- Native Herdr parent Space `w1` `Native-FLOW-Parent`, repository `/tmp/cflow-native-reviewresume-20260924/repo`; native Cockpit UI setup created owned child Space `w2` `Native FLOW Review`, operation `7bf4f12d-4f46-40f9-8d65-2fbf3229dd51`, branch `native-flow-review`, checkout `/tmp/cflow-native-reviewresume-20260924/worktrees/repo-7bf4f12d-4f46-40f9-8d65-2fbf3229dd51`, companion `/tmp/cflow-native-reviewresume-20260924/companion/7bf4f12d-4f46-40f9-8d65-2fbf3229dd51`. A read-only link to the installed Reviewr plugin was placed only in the private Herdr registry. The run-owned child received a committed branch marker, staged+unstaged `overlap.txt`, and untracked `untracked.txt` (no parent or non-fixture data was touched).
- The real native **Commands → Open Review right** opened the graphical Review surface, but the captured native screenshot showed **Files 0** with the changed child checkout, blank diff, and `Loading comments…`; no changed file/diff/comment content was visible. This is a native parity failure/open criterion, not a pass. Native screenshot `FLOW-01-resume-review-native.png`, 26,504 bytes, SHA-256 `5166e8661a3ea2d0d5b7095a586489dd6444d777c6d9467e5d27296c70a471f7`.
- Browser screenshot `FLOW-01-resume-review-browser.png`, 149,515 bytes, SHA-256 `bd8f30dc9f8def5c1a77237c114512ce01852535240a2fb9d39e14fae4568289`, shows the actual browser selected owned child/pane and inline Review refusal.
- Port 5173 and WebDriver port 9519 were released: owned WebDriver session deleted; native WebDriver, Niri, static frontend server and private headless Herdr server stopped; socket `/tmp/nrr24/c/herdr/sessions/nrr24/herdr.sock` absent; no listener on 5173/9519; no matching native launcher, Tauri, WebDriver or private Niri processes remained. Only the native disposable Git/config/data root `/tmp/cflow-native-reviewresume-20260924` and private registry `/tmp/nrr24` were removed. Main was directly notified that 5173 is released. No native build was run by this worker.

## Remaining FLOW-01 criteria

Prior browser evidence in `FLOW-01.md` documents browser Review staged/unstaged/branch/untracked comparisons, browser line/file comment revision retention, source refresh, prior browser/native Context-to-real-Pi paste, changed-source refusal, dirty-owned refusal, and clean owned teardown. That evidence is not converted into new passes here. This run does not prove a linked *borrowed* child Space hierarchy, native Review scope/comment parity, paste target-change-before-send, lost-response/unknown-receipt reconciliation or explicit duplicate-risk acknowledgement, pending intent across process/server restart, source/media/Mermaid/search breadth, or full TUI hierarchy/focus parity. After Main's browser-green review, its managed tab was released, the private gateway exited, private Herdr server socket disappeared, and only the worker-owned disposable browser root `/tmp/cflow-reviewresume-20260924` was removed. `ss` showed no listener on gateway port 36371 or native ports 5173/9519; no matching `flowreviewresume20260924`, gateway, or Tauri process remained. Main was told the fixture was clean. Do not treat this record as FLOW-01 completion.

## Rebuilt browser Review check after Main's producer repair

- Main rebuilt the frontend as `index-CwwUE_r2.js`. Reloaded the same owned Chromium tab/session and reused the same disposable Herdr fixture/gateway; port 5173 remained free during this browser-only verification.
- Repeated actual **Commands → Open Review right** from selected linked child `w2:t2`, source `w2:p2`. The request `POST /api/v1/sessions/flowreviewresume20260924/review/open` returned HTTP 200 (request `request-541`, 1,864-byte response), and the authoritative response verified Review pane `w2:p3`, terminal `term_65c3752ebef514`, `extension:\"review\"`, `renderer:\"review\"`, confidence `verified_launch`, and `default_root_id` equal to the child repository root `repository:repo-63a58721a721bc27cd6411a14b32b7bf650fc47178a3b983384c480efe200fb7`. The same response still lists companion root identity `repo-f322fb25…` separately; it no longer controls the normal terminal's Review checkout selection. UI no longer painted the 503 refusal.
- Selected Review comparison **Branch**, entered explicit base `main`, and pressed Enter. Actual Review UI painted one changed file `branch-only.txt`, added line `BRANCH_ONLY_FLOW_REVIEWRESUME`, and `Files 1` / `Changed files 1`; the authoritative `/review/snapshot` and `/review/file` requests returned HTTP 200. Screenshot `FLOW-01-resume-review-browser-green.png`, 179,237 bytes, SHA-256 `d702d490c92f476a50b2142ef980b20c7ffe6ac48d9fdd7af975a62433843358`.
- This verifies the repaired browser linked-checkout Review launch and one real branch diff, not Review line/file comments, other scopes in this fixture, a native green result, or FLOW-01 completion. The recorded request response contains the selected root identity; the browser request-body capture is not retained for this second 200 response.

## Fresh isolated native run against rebuilt frontend — core diff, WebKit visual incomplete

- This attempt used a new native-only disposable root `/tmp/cflow-native-green-20260924`, short isolated XDG/Herdr registry `/tmp/ng24/c`, unique session `ng24flow`, private HOME/XDG/config/state/cache, a private Niri compositor, WebKitWebDriver on 9519, and a static frontend service on 5173. The app was actual `target/debug/cockpit-tauri` loading Main's rebuilt dist; the browser fixture above was not reused. The installed Reviewr plugin was linked only into this private Herdr registry.
- Native Cockpit setup created parent Herdr Space `w1` `NativeGreenParent` and owned linked child Space `w2` `Native Green Review`, operation `1391137f-70a4-4dd8-a44b-2f36bd14a543`; branch `native-green-review`; checkout `/tmp/cflow-native-green-20260924/worktrees/repo-1391137f-70a4-4dd8-a44b-2f36bd14a543`; companion `/tmp/cflow-native-green-20260924/companion/1391137f-70a4-4dd8-a44b-2f36bd14a543`. The run-owned `branch-only.txt` marker `NATIVE_GREEN_BRANCH_DIFF_MARKER` was committed at `65fca411b95eb5c1c4b4fe7a62632d7f3fd3e39a`; file SHA-256 `68a55028f5ddf961184f060bf0fdb5cd9703906a4d8171808e70cd2c1e06a0fd`.
- Native **Commands → Open Review right** created an actual Review pane. The initial painted scope was **All local changes**, `Files 0` (expected because the branch marker was committed). After selecting **Branch**, entering explicit `main`, and clicking the real **Refresh** control, the WebDriver command returned HTTP 404 `invalid session id`, message `session deleted because of page crash or hang`. No painted branch file, diff, or comment editor state was observed; no native green UI claim follows. Native WebDriver exited status 1. No native app stderr or core dump was present in the private fixture or service output, and the launched child Tauri PID was not exposed by WebDriver; Niri log showed only our expected SIGTERM during cleanup. This is a still-open native rendering/automation acceptance failure; do not retry unchanged without a changed diagnosis.
- The owned core review store preserved authoritative response `/tmp/cflow-native-green-20260924/state/review/snapshot-f5aa2c5c-60e4-4848-a346-90fdae8e2ead.json`: pane `w2:p4`, repository `repo-3a87472e559414bc9b491491af6091cd7d67917755f7d1c465d72ff854af1fbe`, checkout above, comparison `branch`, base revision `17681db91491d30c5b689fe27bd6cfd6680c6e6f` (`main`), head `65fca411b95eb5c1c4b4fe7a62632d7f3fd3e39a`, one `branch-only.txt` added with 1 addition and no diagnostics. This proves core calculation/receipt only, **not** the required native rendered result. A raw copy is `FLOW-01-resume-review-native-branch.json`, SHA-256 `97ed887f9bf6cf8443f742278bed571ce73516127b1cd74fb4d635009a8822a7`.
- Cleanup: the WebDriver session was already invalidated by the page crash; owned WebDriver, Niri, frontend, and headless Herdr services were then stopped; `ss` showed no listener on 5173 or 9519; no matching run-owned native launcher/Tauri/WebDriver/Niri process remained; `/tmp/ng24/c/herdr/sessions/ng24flow/herdr.sock` was absent. Only `/tmp/cflow-native-green-20260924` and `/tmp/ng24` were removed. Main was notified promptly after port release. No product build or test was run by this worker.
