# WEB-08 payload-isolation evidence — 2026-09-24

Scope: bounded browser-gateway probes, including an initial test-mode pass and the live-runtime attempt recorded below. This does **not** complete WEB-08 or any native/browser hostile-negative acceptance row. No product, test, or status files were changed. No provider or pre-existing user Herdr session was touched.

## Owned fixture and cleanup

- Private root: `/tmp/web08-payload-20260924` (0700); config/state/data/cache and Playwright profile directories were all 0700. Browser fixture: `127.0.0.1:43872/page.html`; built gateway: `127.0.0.1:43871`, using `target/debug/cockpit serve --test-mode --bind 127.0.0.1:43871 --static-dir dist`. All test requests were run from an independently named Playwright CLI browser session `web08payload` (browser PID 3742812). The gateway had no live Herdr adapter or BrowserRuntime in test mode, so it could prove gateway body limits and browser origin behavior, but not view/helper defenses.
- Shutdown: closed `web08payload`; stopped the exact owned fixture PID 3742082 and gateway PID 3740015. Verified all three PIDs absent and `ss` showed no listeners on 43871/43872. Removed `/tmp/web08-payload-20260924` after evidence creation; it contained only this worker's fixture/profile/config/state/cache data.
- The **initial test-mode fixture** intentionally had no private Herdr session, managed browser incarnation, helper, active view, frame grant, second view, or observer. The separately isolated live session and owner cleanup are recorded below.

## Runtime results

| Probe | Negative result / exposure | Authorized positive after it | Status |
|---|---|---|---|
| Ordinary external browsing control | — | Chromium navigated to `https://example.com/`; actual page title `Example Domain`. | Pass for this ordinary remote navigation control only. |
| Remote-page JavaScript `GET /api/v1/status`, `POST /api/v1/browser/view/command`, `POST /api/v1/browser/view/open` from fixture origin `http://127.0.0.1:43872` to gateway `http://127.0.0.1:43871` | Each `fetch` rejected in page JS with `TypeError: Failed to fetch`. Chromium console identified missing `Access-Control-Allow-Origin`; the JSON POSTs failed their CORS preflight. The page did not receive a response/body. | The ordinary external navigation control above succeeded; then the same-origin Cockpit gateway loaded as title `Cockpit`, and `/api/v1/status` returned HTTP 200. | Confirms the tested cross-origin JS response barrier and that navigation/gateway remained usable. It does not prove a real browser-runtime mutation was attempted or refused. |
| Remote-page WebSocket to `/api/v1/browser/view/events/not-a-view` | WebSocket error; Chromium console reported handshake HTTP 403. No event payload was received. | Same-origin Cockpit page loaded and same-origin `/api/v1/status` returned 200. | Pass for this unauthenticated invalid-view event stream attempt only. |
| Same-origin browser-command body > 6 MiB (6 MiB + 1 byte) | `POST /api/v1/browser/view/command` returned HTTP 413, `Failed to buffer the request body: length limit exceeded`. | Following status request returned HTTP 200 and remained on Cockpit. | Pass for gateway request-body limit in test mode; no active view command path was attached. |
| Same-origin view-open body > 128 KiB (129 KiB) | `POST /api/v1/browser/view/open` returned HTTP 413, same bounded body-limit message. | Following status request returned HTTP 200 and remained on Cockpit. | Pass for gateway JSON body limit in test mode. |
| Page JS `fetch(file:///etc/passwd)` and `fetch(http://127.0.0.1:9222/json/version)` | Both failed with `TypeError: Failed to fetch`; no file/CDP response was available to page JS. | Gateway page remained loaded with title `Cockpit`; same-origin status positive had already succeeded. | Limited negative only. The loopback CDP endpoint was not active; this does not prove a live managed profile/CDP boundary. |

Request/response measurements above are from actual page JavaScript in the named browser, not direct curl or a mocked browser response. The worker did not persist reusable grants or credentials; none were issued in this test-mode fixture.

## Source-review-only bounds (not runtime proof)

- `crates/cockpit-host/src/browser_view.rs` source limits (in addition to the test-mode body probes): 128 KiB default JSON, 6 MiB command body, and 128 KiB frame-credit text. Live probes below exercised body/credit limits, but not server-to-client malformed JPEG or descriptor validation.
- `browser-runtime/browser-helper.mjs` source limits: 64 KiB helper WebSocket payload, 32 peers, dialog/blocker strings sliced at 4096, inspected text/excerpt at 1024 and accessible name/locator component at 256. Live probes below exercised the title/locator/dialog/slow-peer/file/download paths stated there; no native UI-rendering claim follows.

## Remaining assigned rows / disposition

- Reused `WEB-08-resume.md` Host/Origin and forged/pre-auth frame observations; this live run additionally tested a binary first message and a modified view grant. Grant replay and genuine expiry remain inconclusive in the prior record.
- **Untested:** malformed/oversized JPEG and server-to-client frame envelope injection; extreme descriptor dimensions/area originating in the helper; genuine metadata-event flood/lag closure; genuine expiry/replay; held-key/button lost-client release; permissions; symlink/traversal sentinels; feedback file permissions/write/readback; CSP/native-origin; extension/pairing/Show and saved-feedback/A25 cutover; native surface.
- File chooser and synthetic download blockers were exercised only on this private browser; permission prompts and externally selected/local file contents were not. A tested download cancellation left the association unable to open a view until lifecycle handling/recovery; the exact observed sequence and unresolved state ambiguity are in the live section.
- The prior Host/Origin run's file-mode and symlink checks in `WEB-08-resume.md` remain partial evidence; this run did not repeat them.
- No product change was made. One download-associated view-open failure was reproduced and reported to Main; it is a confirmed runtime finding, not repaired by this worker. This record does not claim full WEB-08 acceptance.

## Live private gateway and managed-browser matrix

- Separate root `/tmp/web08-payload-live-20260924` (created 0700); isolated XDG/HOME/config/state/cache; Herdr session `web08-payload-20260924`, Space `w1`, pane `w1:p1`; session API/client sockets lived under that root. Gateway `127.0.0.1:35761`, fixture `127.0.0.1:35762`, private CDP endpoint `127.0.0.1:41399`; port 5173 and the native surface were not used. Cockpit state root was 0755 inside the private 0700 parent; browser state/profile/helper/association subtrees were 0700 and the materialized helper/owner lock and Herdr API/client sockets 0600. Profile `DevToolsActivePort` was 0644 inside the 0700 profile/root.
- Herdr PID 3767787, gateway PID 3769391, fixture PID 3776007, managed Chromium incarnation `pid=3776853:start=34940288:receipt=…` (receipt hash omitted here), owned association `1faab3a53f0ba925a0b5d1a3`. A private Chromium page used target `9C6CA7AD350CBAB299080ED184A80C41`. The frame grant was obtained only from the live event snapshot and used in memory for the first frame WebSocket message; no grant value was persisted or printed in this evidence. The first probe mistakenly looked for a grant in the HTTP open response (which omits it) and timed out; changed to the documented event snapshot→grant handshake, producing a real frame.
- **Authorized frame positive:** repeated live HTTP view opens returned 200; event subscriptions delivered the matching attached identity/epoch; binary `IBFV` v2 messages arrived and were credited. Captures included 800×600 / 7,291-byte JPEG (7,387-byte packet), 800×600 / 12,883-byte JPEG (12,979-byte packet), and a remote-page 13,810-byte JPEG (13,906-byte packet). Descriptor sequence advanced and title/control probes did not suppress these subsequent authorized frames.

| Hostile input / blocker | Runtime refusal or exposure | Authorized positive after the case |
|---|---|---|
| Binary JPEG-looking bytes before the view grant | WebSocket closed 1008 `frame grant required`; no frame delivered on the rejected connection. The frame endpoint accepts credits only after the first grant, so this is not a test of injecting a JPEG into the server-to-client lane. | Fresh view received and credited 800×600 frame, 7,291-byte JPEG. |
| Modified/forged grant on an open private view | WebSocket closed 1008 `Browser frame grant no longer belongs to this stream`; no frame delivered. Grant value omitted. | Fresh view received and credited 800×600 frame, 7,291-byte JPEG. |
| Wrong frame credit sequence after one valid frame/ack | WebSocket closed 1008 `invalid frame credit`. | Fresh view received and credited 800×600 frame, 7,291-byte JPEG. |
| Command body 6 MiB + 1; view-open JSON body 129 KiB | Each route returned HTTP 413 `Failed to buffer the request body: length limit exceeded`. | A new 800×600 authorized frame succeeded after each case. |
| Typed page text 65,537 bytes (over the 64 KiB command limit) | HTTP 400 `invalid_browser_view_command`, `browser view text exceeds bounds`; it did not reach the page/helper. | Fresh authorized 800×600 frame succeeded. |
| Concurrent control flood: 64 over-limit typed-text requests | All 64 returned HTTP 400; maximum response body 84 bytes, 181 ms total in this run. This is a control-validation flood, not a metadata-event flood. | New authorized 800×600 frame succeeded. |
| Viewport width 2,561 at maximum height 1,600 | HTTP 400 `invalid_browser_view_open`, `browser view viewport dimensions exceed frame bounds`; the invalid view was not attached. This tests request viewport bounds, not a malicious helper-sourced descriptor/area. | New authorized 800×600 frame succeeded. |
| Hostile DOM locator/name/text: element id, `aria-label`, and text each 100,000 characters | Read-only point inspection returned HTTP 200 accepted, 3,208-byte JSON; text and excerpt were 1,024 characters each, name 256, locator 257 including `#`. It did not return the full hostile values. | New authorized frame succeeded (12,883-byte JPEG). |
| Page title changed to 65,536 characters | Browser automation observed the full title in Chromium, but a fresh attached view snapshot still exposed the previous 30-character title. A subscribed view received no `targets_changed` title event within 8 seconds. No giant title was observed in forwarded metadata; dynamic title freshness/bounds are not proven by this stale result. | A later view after the title mutation still produced and credited a frame; no stall observed. |
| Slow frame peer (received frame but withheld credit) with an observer view | Slow view held frame sequence 25 (12,979 bytes) unacknowledged. Concurrent observer received the same sequence/bytes and acknowledged it. | Observer and a further fresh view both received valid 800×600 frames; slow peer did not stall the other view. |
| Page JavaScript from `http://127.0.0.1:35762` attempted GET status, JSON POST browser-view command and browser-action lifecycle, plus WebSocket to an invalid view at gateway `127.0.0.1:35761` | All page fetches rejected with `TypeError: Failed to fetch`; WebSocket reported error. No API body was exposed to page JS. Chromium console showed CORS failure in the earlier test-mode attempt; the live probe output confirms failure but does not claim which gateway check rejected each live request. | Same managed browser navigated to `https://example.com/` with title `Example Domain`; a fresh authorized view then received/credited a 13,810-byte JPEG frame. |
| Oversized JavaScript alert (20,000 characters) | Live blocker event kind `dialog`, `message_length=4096`, cancellable; matching dialog-dismiss command returned HTTP 200 accepted. Automation eval exited 0 after dismissal. | Fresh authorized frame received and credited (7,291-byte JPEG). |
| Owned page file input `.click()` | Live blocker kind `file_chooser`, 51-character message, cancellable. Matching cancel returned HTTP 200 `unsupported` capability `file`; no path was selected/read. A subsequent view snapshot had no blocker. The older Playwright attachment retained its own stale “File chooser” modal state; a fresh CDP attachment to the same owned browser did not, and the view/frame path remained live. | Snapshot showed `blocker=null`; fresh authorized 800×600 frame received and credited (15,638-byte JPEG). |
| Synthetic data-URL download (`WEB08_DOWNLOAD_MARKER`) | Live blocker kind `download`, 56-character message, cancellable; matching cancel returned HTTP 200 `unsupported` capability `download`. No matching download marker was found anywhere under this run-owned root. | **Recovery anomaly:** immediately after cancellation, fresh view/open returned HTTP 400 `browser_view_association_unavailable`. This sequence repeated twice. On first occurrence, a later browser/action status (run **after** the failed view/open) said connection `open`; an explicit browser/action open returned 200 and a subsequent authorized frame succeeded. On second occurrence, browser/action open could not identify one stable target after test-owned duplicate tabs; after closing only those owned duplicate tabs, open returned `outcome_unknown`. Main was notified. I did not infer that the later status preceded the original failure or retry the uncertain open. |

## Download receipt order, teardown, and final gaps

- At Main's request, I recorded the order precisely: the original download→cancel was followed immediately by failed view/open; only then did the one status request report `connection=open`. There was no immediate status→view/open recheck before an explicit open on that live receipt. I did **not** create new tabs to repeat that sequence. After the owned browser association was explicitly closed for cleanup, a changed check did status once (`connection=closed`) and immediately view/open without browser/action Open; open remained HTTP 400 `browser_view_association_unavailable`. This closed-state result does not resolve the live-receipt ambiguity. Main received this caveat.
- Browser/action close returned HTTP 200, connection `closed`, and removed the managed Chromium process (PID 3776853 absent). Then stopped only owned Herdr/gateway/fixture PIDs 3767787/3769391/3776007. Verified all four PIDs absent, no process command line referenced this private root, `ss` had no listeners on 35761/35762/41399, and the Herdr API socket was absent. Main's worker message records the download/view recovery finding without claiming a source fix.
- Unrun here: actual helper-sourced malformed/oversized JPEG and binary envelope/dimension/area injection; true metadata-event flood/lag; permissions; lost-client held keys/buttons while a grant is active; traversal/symlink sentinel; feedback/file permission and saved-readback; actual CSP/native-origin parity; A25 cutover and extension/pairing/Show runtime checks; native hostile matrix. These remain acceptance gaps despite the positive frame controls above.
 
## Changed-diagnosis download recovery recheck — 2026-09-24

- Repeated the live download→Cancel case in a new private root `/tmp/web08-payload-recheck-20260924` (0700), XDG/HOME/config/state/profile, Herdr session `web08-payload-recheck-20260924`, Space `w1`, fixture `127.0.0.1:36142`, and the current workspace gateway binary `target/debug/cockpit` at `127.0.0.1:36141`. Port 5173 was free at allocation; this fixture sent nothing to it.
- Browser/action Open initially succeeded and a pre-download BrowserView opened. A same-origin synthetic `data:text/plain` download produced a cancellable `download` blocker (message length 46). Its matching Cancel returned HTTP 200 with result `rejected`. Immediately afterward, with **no** browser/action Status or Open between Cancel and the next request, one fresh `browser/view/open` returned HTTP 400 `browser_cli_failed`, message `Playwright CLI exited with status exit status: 1`. No authorized post-cancel frame was established. No retry was made.
- Without querying Status/Open after the red result, inspected only the run-owned durable association receipt’s non-secret fields: `state=open`, `intent=none`, same private session/Space, and a target ID was present. This does not demonstrate the disconnected-receipt revalidation path; the response failed earlier as `browser_cli_failed`, unlike the prior `browser_view_association_unavailable` observation. I did not record any frame grant, helper ticket, CDP endpoint, or reusable credential.
- Teardown used browser/action Close (HTTP 200, `connection=closed`); managed browser PID 4050932 exited. Stopped only this run’s Herdr PID 4044904, fixture PID 4048694, and gateway PID 4049686. Verified those PIDs absent, no listeners on 36141/36142 and no sockets under this private root. Port 5173 acquired a listener only after this fixture’s run; it was not this worker’s process and was left untouched.
- This changed-diagnosis attempt did not satisfy the requested authorized-frame positive; result is an unresolved red runtime outcome, not a fix or WEB-08 completion.

## CLI/CDP liveness diagnosis after download cancel — 2026-09-24

- Fresh private root `/tmp/web08-payload-cdpdiag-20260924` (0700), HOME/XDG/config/state/profile, Herdr session `web08-payload-cdpdiag-20260924`, Space `w1`, fixture `127.0.0.1:36242`, current workspace gateway `target/debug/cockpit` at `127.0.0.1:36241`. Port 5173 was free at allocation and was not used. Initial browser/action Open and pre-cancel view/open succeeded. A synthetic download emitted a cancellable `download` blocker (message length 37); one matching Cancel returned HTTP 200, `unsupported`, capability `download`.
- Immediately after Cancel, ran the same managed-session CLI liveness command, `playwright-cli -s=cockpit-876cd11a46047845ebe26d13 cookie-get __cockpit_liveness_probe__`: exit 1, signal null, stderr empty, no spawn error. Bounded stdout (captured up to 4 KiB; complete output was shorter):

  ```text
  The browser 'cockpit-876cd11a46047845ebe26d13' is not open, please run open first

    playwright-cli -s=cockpit-876cd11a46047845ebe26d13 open [params]
  ```

- Without any browser/action Status/Open or retry after Cancel, made only read-only requests to the private profile’s pinned CDP endpoint from `DevToolsActivePort` (port 46499). `/json/version` returned `Chrome/153.0.8010.47`, protocol `1.3`, with its browser WebSocket path matching the pinned file path. `/json/list` still returned page target `D879C89C9441296EFE5BCD97D42B04A2` at `http://127.0.0.1:36242/` (`WEB08 liveness diagnostic`) and two `chrome://omnibox-popup.top-chrome/` browser-UI targets. The WebSocket URL itself was not recorded.
- After those observations, the durable private receipt still said `state=open`, `intent=none`, session/Space `web08-payload-cdpdiag-20260924`/`w1`, and target present. Both processes were alive: Herdr daemon PID 4073857, start ticks 35292640; managed Chromium PID 4075995, start ticks 35295314. Thus the observed CLI session was “not open” while the pinned browser endpoint, page target, durable open receipt, daemon, and browser process remained live; no grant or credential was captured.
- Cleanup: browser/action Close returned HTTP 200/`closed`; then stopped only owned Herdr PID 4073857, fixture PID 4074595, and gateway PID 4075451. Verified all four PIDs (including browser PID 4075995) absent, no listeners on 36241/36242, and no sockets under this private root; removed the root. This is a liveness diagnosis, not a recovery attempt or WEB-08 completion.
