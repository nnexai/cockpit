# Consolidated stability delivery — 2026-09-21

Baseline: `163ee85fba8c883f80b5b24871de84bd210a7823`. Implementation/evidence commit is recorded in `tasks.json` by the subsequent checkpoint. This is the user's OBS-015 combined implementation/review/verification batch. The final user direction was to end within ten minutes; no additional broad matrix was launched. **This is not full campaign acceptance.** Known failures below remain open rather than being relabeled passes.

## Delivered implementation

- Browser focus/handoff, retained drafts and pending annotation intent, durable feedback operation identity, explicit rejected versus unknown outcomes, local editor event ownership, and bounded input queues.
- Capture-time geometry checks, bitmap/URL release, bounded decode/frame credits, independent observers, exact frame ACK/discard, controller/tab/dialog authority, grant expiry and scoped helper retirement.
- Browser/native cancellation, late-open cleanup, dependency diagnostics, private helper transport boundaries, and durable JSON publication.
- Context/Review scroll and source/diff continuity, compact resource presentation, terminal frame-grid handling, and conservative comment-paste acknowledgement.
- Final Main repairs: pointer down/up click count is at least one; note/editor/blocker/recovery controls do not bubble into remote pointer capture; alert acceptance does not pass null to Playwright; forced pointer release cannot synthesize a click; Retry awaits association reconnection and actually reopens the failed view stream.

## Executed checks

See `final-checks.md` for exact package commands and earlier integration failures. Frontend build and host/native binaries passed. The integrated frontend wave reported 158 tests; final affected App suites (`bun run test src/app/App.test.ts src/app/App.integration.test.tsx`) passed 58/58 after the Retry repair. The initially mistyped `.test.tsx` filter found no tests; it was corrected, not counted as a pass. Latest frontend bundle: `index-CH8RRJN1.js` (326 modules).

Rust: core 150, providers 28, host 12, Herdr 92, protocol 13: **295 passing tests**. These totals describe executed suites, not every original acceptance matrix. A temporary native-coordinate precision hypothesis test passed without a production change and was removed; no precision fix is claimed.

## Actual browser proof — Main

Authority: session `csg-a3e9b950`, gateway `127.0.0.1:37619`, fixture `127.0.0.1:37629/?case=web-final`, Space w3/tab w3:t2; no user-active Herdr session used.

1. Physical click regression: fixture pointer down/up previously produced no click; after click-count repair the hit counter advanced. A later 100-click run advanced it from 1 to 101.
2. Physical region draw -> note editor -> `MAIN-SAVED-REGION` -> physical Save. Before repair the surface captured the editor's pointer and Save never upserted; afterward the editor closed and the authoritative draft contained the exact comment (revision 3). `main-browser-note-fixed.png`.
3. Actual PNG capture `5e9b6fd0-8a45-48cd-864b-e8ae6c09d0c3`, SHA256 `248f43ac9f31f3915106616e0cfbf28c4b3e9357eb8743a6294eb1d024c88f49`: `final-web-capture.png`. Main visually inspected page pixels, region and exact note, without application chrome.
4. No eligible agent: operation `browser-feedback-5e9b6fd0-8a45-48cd-864b-e8ae6c09d0c3` was definitively rejected, target null, acknowledged IDs empty, pending count 1. UI reported rejection and retained note/marks/capture. This does not prove successful agent paste.
5. Animated fixture produced four different presented-canvas signatures and alternating header pixels. Animation then stopped.
6. Following owned gateway restart, old Retry reconnected only the association and left the stream failed. The repaired Retry returned `/browser/view/open` 200 and visibly restored `Live browser view`; older drafts remained listed. Fresh fixture navigation succeeded.
7. Physical page Alert -> visible `alert: Owned alert sentinel` -> physical Accept -> blocker disappeared and fixture reported `alert-dismissed: 1`. `main-browser-recovery-dialog.png` was visually inspected.
8. Actual page wheel moved remote scrollY 0 -> 280 -> 440 while host scrollY stayed 0. **Subsequent nested-wheel input stalled behind `Showing last confirmed frame`; no nested-scroll event was observed. This remains unresolved under WEB-04/WEB-06.**

Performance: `main-browser-latency.json`, 100 physical clicks, same monotonic automation clock until changed presented canvas pixels, no exclusions. p50 49.486 ms, p95 50.46987 ms, maximum 333.416 ms. Includes automation overhead. This is not the five-minute resource plateau, whole-app sustained performance, or full PERF-01 acceptance.

## Delegated real-surface and boundary proof

- `final-browser-surface.json`: terminal marker survives zoom/restore; Review selection/source-diff scroll continuity; resource issue/MR rendering; typing and resize observations. Its original click/Save/recovery failures are superseded only by the precise Main checks above. Originally claimed nonexistent screenshots were removed; do not infer screenshots from those historical names.
- `final-protocol-boundaries.json`: exact controller ACK/discard, invalid Host/Origin close 1008, unauthenticated timeout, observer mutation denial, alert acceptance, grant expiry/retirement and forced pointer release without click. Corrected continuously ACKing controller progressed through sequences 1,2,3,5,6,7 while observer withheld credit. Earlier slow-observer failures were harness ACK errors, not concealed product passes.
- `final-native-surface.json`: actual isolated Linux Tauri startup, existing-directory setup, Herdr terminal marker/zoom state, native inline frame/channel and hide/show observations. `native-replay-fresh-frame.png` contains real native canvas pixels. Native OS coordinate injection produced no fixture event; WebDriver actions were unsupported. Synthetic pointer capture requires instrumentation and is explicitly **not physical-input proof**.
- Native DOM-only region replay persisted an annotation, but UI reported `Annotation save was not acknowledged; retry it before closing`; note/capture completion remained unavailable. **Native acknowledgement is unresolved under WEB-05/NATIVE-02.** IPC instrumentation was unavailable because Tauri invoke/ipc descriptors are non-writable (`native-ipc-annotation-probe.json`). No speculative numeric or equality change was shipped.
- GitLab resource/import evidence already exists in `GLAB-resource-ui-evidence.json/png`. Final workflow evidence is recorded separately when returned. An isolated workflow gateway initially inherited the private compositor D-Bus address, preventing CLI keyring access; Main corrected only its D-Bus credential-service routing while retaining owned HOME/config/state. No token was read or copied.

## Coverage and issue disposition

Original 25 required task identities remain. OBS-014 permits targeted checks, not invented passes. SYNC/terminal/view/source/browser/security improvements are delivered; their task records point here. WEB-04/06 scroll recovery, WEB-05/native annotation acknowledgement, successful same-tab agent paste, the unexecuted broader workflow/security/input matrices and sustained PERF criteria are explicitly not all accepted. ACCEPT-01 therefore remains incomplete.

A01–A25 remain owned as mapped in ACCEPTANCE.md; A23 stays excluded. This batch is not a blanket A01–A25 pass. #6 remains partial: GitLab issue/MR implemented, GitHub PR/Jira deferred. #3 concerns the current inline implementation, not a resurrected extension. #9–#12 receive the browser/terminal/cancellation repairs but are not claimed fully closed while the failures above remain. No remote issue was automatically closed from partial evidence.

macOS remains **user verification—not executed**, per OBS-013; `MACOS-HANDOFF.md` retains the original platform checks. No emulation or substitute macOS claim.

## Safety and cleanup

All runtime work used named disposable authorities under `/tmp/csg-a3e9b950`. Final cleanup stops owned app/gateway/helper/fixture/Herdr/display resources, preserves evidence/profiles/owned worktrees for diagnosis, and performs no remote branch deletion/merge/approval. Owned GitLab issue 2/MR1 and their dedicated branch remain available for the macOS handoff. User explicitly authorized retaining the six OBS-008 HOME files unchanged; no speculative restoration or further HOME write.

The subsequent checkpoint records final service cleanup and commit identity. The umbrella goal is not marked complete while the named failures remain.

## Resumed fixed batch — 2026-09-24, authorized after `8c6b1e7`

Execution resumed from the 17 done, WEB-05 Main-owned in-progress, six queued and three deferred ledger records. This is one new implementation pass across the **remaining** FLOW-01, WEB-04/05/06/08, PERF-01 and ACCEPT-01 outcomes, not another WEB-05 subset or a campaign-completion claim. Protected existing work: the unstaged `crates/cockpit-core/src/browser/drafts.rs` change, unstaged campaign ledger/instructions/observations/helper edits and `.audit/`; do not stage foreign changes or touch the user's active/default Herdr session, installed app, browsers or profiles. Recent owned commits `776d1d3`, `fe7452c`, `d5da745` and `8c6b1e7` exist. Earlier Chromium/native captures and exact PNG/receipt evidence remain valid for their unchanged paths; current changes to draft retirement and any frame/input paths invalidate only their dependent scenarios.

Implementation ownership, with existing wire shapes and Herdr authority fixed: Main owns `BrowserPane.tsx`, the preserved `drafts.rs` edit, generated/protocol changes if indispensable, this run evidence and ledger integration. Browser-runtime worker exclusively owns `browser-runtime/browser-helper.mjs` and `src/app/browser/framePresenter.ts` for input coordinate, newer-frame/admission, bounded decode and lifecycle behavior; no BrowserPane or host edits. Host-boundary worker exclusively owns `crates/cockpit-host/src/browser_helper.rs` and the relevant core browser runtime association/lease boundary for confirmed owner/observer/security and shutdown defects; no helper JS, drafts, protocol or UI edits. Local-workflow worker exclusively owns Context/Review/project/comment source paths outside App/BrowserPane and shared browser paths, addressing source-confirmed retention/ownership gaps; existing behavior needing only acceptance gets no speculative rewrite. Main integrates once; all writers skip checks and runtime acceptance. Security, performance and integrated acceptance are in this batch, not permission to mutate protected resources or add product scope.

Frozen **single consolidated verification pass**, after all implementation and integration: (1) static `git diff --check`, `cargo fmt --all -- --check`, `node --check browser-runtime/browser-helper.mjs`, `bun run typecheck`, `bun run build`, affected browser/App Vitest and focused core draft/feedback tests, and `cargo build -p cockpit-host --bin cockpit -p cockpit-tauri`; (2) one run-owned browser-gateway Chromium journey at desktop and minimum width, plus one isolated real Linux-native WebKit/Tauri journey, exercising first frame/resize/scroll/hover/animation and quiet recovery with measured CSS/DPR/bitmap/presented pixels, click/key/ownership, annotation authoring/retirement, saved PNG/feedback and exact no-Enter recipient, with truthful failure states; (3) an isolated Context/Review setup→comment/refresh/paste→borrowed/dirty refusal/owned cleanup path plus Herdr TUI authority; (4) bounded hostile Origin/grant/lease/frame/page-to-loopback negatives and an authorized positive control on the owned environment; (5) sustained mixed browser/native resource, idle, observer and renderer measurements against the existing RUN-01 method, followed by the integrated issue/resource reconciliation. Reuse past evidence where code/runtime assumptions are unchanged; unexecuted deep A04–A25 cases remain explicitly unverified. Failed build gates leave dependent runtime checks unrun, not exercised on stale binaries. After this gate starts: **no source or harness repairs and no rerun**; finish independent safe checks, classify failed/unrun, clean every owned root/process/port/socket with verified exit, commit only verified owned changes, report and pause the incomplete goal.

Implementation integrated before the gate: WEB-05 draft-store retirement no longer interprets a changed target or helper generation as proof of page navigation; explicit same-target document-change UI retirement still discards old marks without warnings. An authenticated target's Chromium loader identity is retained within its helper across tab selection, while a changed loader receives a fresh generation. BrowserPane persists a dirty editor before local tab selection, avoids discarding another tab's marks, and opens the newly selected tab's draft after its target event. The native/browser frame producer and presenter worker found no safe additional sharpness-only edit under OBS-043; FLOW-01's current source had no established unfixed defect, so its open criteria remain actual-surface proof. WEB-06 host serializes same-incarnation helper admission through first-frame publication and materializes the shared packaged file under a separate short lock. Named read-only risk review identified tab-switch/helper-restart draft loss and cross-incarnation temporary-file leakage; Main corrected both before verification. No worker ran tests or launched product resources. The gate below remains unchanged.

### Resumed batch consolidated gate (one pass; no repair/rerun)

- PASS: `git diff --check`; `node --check browser-runtime/browser-helper.mjs`; affected `bunx vitest run src/app/browser/BrowserPane.test.tsx src/app/browser/framePresenter.test.ts src/app/App.integration.test.tsx` (3 files, 34 tests); `cargo test -p cockpit-core browser::drafts::tests` (9); `cargo test -p cockpit-core browser_feedback` (4); `cargo test -p cockpit-host authority_guard` (2); `cargo build -p cockpit-host --bin cockpit -p cockpit-tauri` (warnings only).
- FAIL: `bun run typecheck`, three TS18047 errors at `src/app/browser/BrowserPane.tsx:872–874`: `incoming.document` possibly null. No post-gate source edit. `cargo fmt --all -- --check` reports formatting drift across numerous files outside the owned Rust edits; no broad formatting changes. Campaign `campaign.py check` reports this modified evidence file differs from HEAD; this is administrative evidence-drift, not a behavioral pass.
- PASS, bounded **host-only** real-surface acceptance on one uniquely named disposable Herdr session `hostgate-ab44fa02d79e`, root `/tmp/cockpit-owned-hostgate-kfrfel5k/hostgate-result.json`: Herdr created workspace `w1`; gateway's correct-origin status returned 200, hostile Origin returned 403 and hostile Host returned 403; gateway opened an owned browser to an owned HTML fixture and **two concurrent** authorized browser-view admission requests both returned 200, distinct view IDs, matching browser incarnation/document generation and first-frame sequence 1, bitmap 700×460 at CSS 700×460 DPR 1.0. This checks independent WEB-06 first-frame/admission and only the enumerated Origin/Host subset of hostile controls; it does **not** prove frame/grant/lease/full hostile matrix or UI/natively rendered ownership.
- Owned-process reconciliation from the host-only run: recorded Herdr and gateway parent PIDs `3364681`, `3364715`, and six additional root-isolated descendants; after termination root-isolated PID inventory was empty, both parents exited 0, gateway and fixture TCP ports refused connections, Herdr Unix socket refused connection. No user/shared process was stopped.
- UNRUN (dependent): `bun run build` needs the failing TypeScript gate, so no current frontend artifact exists. The frozen Chromium browser-gateway desktop/minimum journey, native WebKit/Tauri interaction and image-quality journey, full Context/Review authoring/paste/borrowed/dirty journey, full hostile matrix, sustained 335-second mixed browser/native resource/idle measurement, and ACCEPT-01 complete real-surface signoff would consume stale frontend/runtime; they were not run. Prior captures are not proof for changed source. No issue is marked complete from the narrower test/host results; this batch stops without a verified product commit. Further repairs require explicit user authorization.

## Explicitly authorized repair batch after failed gate

The user explicitly requested finishing the work despite the reported TypeScript failure. This is a **new** fixed repair batch, not a retroactive rerun of the prior gate. Scope: resolve the known nullable-document frontend compile error and any already identified source defects in the same FLOW-01, WEB-04/05/06/08, PERF-01 and ACCEPT-01 outcomes; retain the previous batch's safe draft and admission edits, protected foreign work, Herdr authority and unchanged wire contracts. Main integrates; no extra implementation worker is needed for the three-line known blocker. No broad refactor or speculative sharpness fix.

Freeze checks **before edits**: affected-source `git diff --check`, `node --check browser-runtime/browser-helper.mjs`, `rustfmt --check --edition 2024` on the two owned Rust files (workspace-wide formatting drift already measured), `bun run typecheck`, `bun run build`, the affected BrowserPane/framePresenter/App Vitest set, focused core draft/feedback tests, host authority tests and Rust host/Tauri build; then one uniquely named disposable Herdr-backed Chromium gateway journey (desktop/minimum, annotation/tab return, save/feedback, grant/security) and one isolated native WebKit/Tauri browser/Context/Review/input/frame-quality journey; a sustained 335-second mixed native resource run using the existing harness; exact ownership cleanup with recorded PIDs/ports/sockets. Reuse previous host-only 200/403, parallel-admission and first-frame proof unless changed assumptions invalidate it. If a check fails, finish safe independent planned checks; no post-gate product/harness repair or rerun. Commit only verified owned changes, then reconcile incomplete criteria honestly.

### Repair batch single gate — observed so far

- PASS: `git diff --check`, owned two-file `rustfmt --check --edition 2024`, helper `node --check`, `bun run typecheck`, `bun run build` (fresh frontend), affected Vitest 34/34, core draft tests 9/9, core feedback tests 4/4, host authority tests 2/2, Rust host/Tauri build (warnings only). The prior TS18047 failure is resolved; no source edits occurred after this gate began.
- PARTIAL/FAIL: native annotation journey `python3 scripts/verify/native_browser_acceptance.py --skip-build --annotation --element-boundary --nested-wheel --timeout 35` ran in `native-1d75da20ffe5` (`/tmp/cnative-xzv5ckdz`): first live frame, 362 drawImage paints/6.016 s (~60.17 fps), authoritative final scrollY 3900, region and note saved at revisions 2/3, intentional basket clear yielded Notes 0; nested Element refusal and valid h1 selection rendered. **Timed out waiting for authoritative nested/document scroll** in the planned nested-wheel step, so the full native journey failed. Initial native bitmap measured roughly 1× CSS at DPR 2; quality acceptance remains unproven. Harness's pointer transport is DOM PointerEvents, not physical native OS input. Its Herdr/gateway/WebDriver/compositor parents returned and root-isolated PID inventory was empty; fixture and frontend TCP ports, gateway port and Herdr socket all refused connections.
- PARTIAL/FAIL: owned Chromium `chromium-cc5e51cb45fb`, root `/tmp/cockpit-owned-chromium-ohbilkw7`, fresh gateway `127.0.0.1:45517` and fixture `127.0.0.1:35521`: visually rendered first page, in-frame button click updated to “fixture clicked”; authored and saved a region note, selected a second page (Notes 0), returned to the first (Notes 1, note and mark visually intact). **Send annotations failed with “Capture draft is no longer current”** (`browser_draft_identity`), with Retry offered. Source `drafts.rs:204–227` marks all other identities stale on opening a second tab; `prepare_capture` at `:523–527` refuses stale, even though `open(draft_id)` can display its contents. No capture/PDF/PNG/recipient completion was proved and no retry occurred. At width 360 the UI retained the selected mark and, after resize convergence, showed a bitmap 449×159 against CSS 358.9×126.7 at observed DPR 1.25; initial transient scaled paint was not counted as settled proof. The current disposable endpoint exposed “Open Review right” as disabled because its supported extension is not installed and enabled: full Context/Review journey is a missing prerequisite, not passed. Owned Chromium root PID `3392222` exited; all 19 root-matched fixture/browser/helper PIDs were gone; gateway, fixture and Herdr ports/socket were released. Reused unchanged prior host 200/403 and concurrent first-frame acceptance from the previous gate.
- PASS, independent native-only sustained workload: `native-6202d60a8a2f`, `/tmp/cnative-ahvbyzx7`, `--skip-build --native-only-open --trace-cdp-overrides --trace-capture-lanes --trace-frame-publication --trace-density-convergence --sustain-seconds 335 --idle-resource-seconds 90 --hide-resource-seconds 30 --timeout 35`. Actual native UI opened the owned URL; 19,419 Canvas drawImage paints / 335.012 s = 57.97 fps, authoritative scrollY 3900 and final green marker. During 90 s static idle, zero paints; during 30 s hidden, zero paints; Show restored the same URL/marker. First and post-scroll settled bitmap density was CSS674×188 at DPR2 despite accepted CDP DPR2 override and a transient captured physical1348×376 frame; full-density steady presentation remains **failed/unproven**, not a quality pass. Raw 458 one-second samples: `/tmp/cnative-ahvbyzx7/sustained-samples.json`, SHA256 `1c38368544ef72e0566f8a9d36e09dced8ebf51762e9e2188ef5530bb03c4e13`. Post-30s active 306 samples kept nine tracked processes: total PSS 462,261→537,634 KiB, median 484,545 KiB, p95 536,542 KiB, least-squares **+319.28 KiB/s**, median 64% CPU of one core, p95 68%; WebKit web-content stable identity `3388810/33929599` grew 196,161→273,867 KiB (+288.34 KiB/s), network `3388784/33929590` 23,150→33,775 KiB (+37.11 KiB/s). Memory plateau **failed**; idle reduced PSS to 536,071 KiB, with no retained-growth clearance. All recorded Herdr/gateway/Niri/WebDriver parents returned; independent root-isolated PID check was empty and fixture/5173/gateway TCP and Herdr Unix socket all refused connections. This measurement is a bounded browser workload, not the full PERF-01 mixed ten-tab/Review workload.

Disposition at gate stop: known TS failure repaired and compiled, but browser capture after switching tabs is a product regression, nested wheel timed out, native steady density and active PSS plateau remain unsatisfied, and the isolated endpoint lacks the Review extension. No valid full delivery or campaign completion claim; no product commit of this integrated unverified batch. Existing foreign modifications remain untouched. A further source fix and fresh acceptance pass require a separately authorized batch, not an automatic retry under this one.

## Explicit resumed implementation batch after second gate

The user explicitly resumed the unfinished campaign and authorized fixing the observed defects without asking for another ordinary-error approval. Retain the earlier failed-gate evidence and owned edits; protect all foreign campaign changes and `.audit/`, and never touch default/user Herdr or browser state. This batch covers the **full remaining FLOW-01, WEB-04/05/06/08, PERF-01 and ACCEPT-01 outcomes**: stop marking a different tab's draft stale, prove capture/delivery on return, resolve nested wheel/density/lifecycle defects at their owning source, investigate the measured WebKit retained-memory growth without a blind throttle, and finish the workflow/security/acceptance paths where a real isolated prerequisite is available. Main owns UI `BrowserPane.tsx` and its tests, integration, runtime and this evidence; core-draft worker exclusively owns `crates/cockpit-core/src/browser/drafts.rs`; helper worker exclusively owns `browser-runtime/browser-helper.mjs` and `src/app/browser/framePresenter.ts`. No worker runs builds/tests/harness or touches another worker's files. Existing host admission changes stay unchanged unless independent source evidence requires a fix.

Frozen checks after one integration pass: `git diff --check`, direct Rust formatting check of owned Rust files, helper `node --check`, `bun run typecheck`, `bun run build`, affected browser/App Vitest, focused core draft/feedback and host authority tests, `cargo build -p cockpit-host --bin cockpit -p cockpit-tauri`; a single owned Chromium UI journey through first/second tab, saved annotation, capture PNG and definitive feedback/error state at desktop and minimum width; owned Linux-native WebKit/Tauri first-frame, nested wheel, region/note/capture and presentation-density journey; bounded owned Host/Origin/grant/lease checks and real local Context/Review authority when installed in disposable space; 335-second active/90-second idle/30-second hidden native sample with paint/FPS, object/process/PSS slopes, post-scroll and reopen; verify all owned PIDs and ports/sockets exited. Reuse unchanged previous evidence for exact controls. No post-gate source/harness repair-and-rerun; any blocked criterion remains explicitly unverified and no unverified integrated commit.

Integration before the gate: confirmed `retire_obsolete` was the tab-switch capture blocker; removed open-time cross-identity staleness, retained exact browser-incarnation binding and explicit same-target stale/discard authority, and gave the new A→B→A capture regression valid inline provenance. Density refinement now permits intervening live frame arrivals only while document/binding/viewport/input and geometry remain pinned; it does not suppress newer stream frames. Prior native combined `--element-boundary --nested-wheel` left the UI's Element tool selected, while `BrowserPane.onWheel` intentionally routes only in Browse. The existing fixture now explicitly selects Browse before wheel, matching a real user action. This corrects a harness precondition, not product wheel handling. OBS-043 permits lower-resolution streaming to retain smooth current content; DPR2 is measured and improved when safe, **not** an unconditional completion requirement. No unbounded owned helper queue/decoder allocation was established as a WebKit PSS cause, so no speculative memory suppression was added. Prior FLOW-01 browser Context/Review and exact no-Enter feedback proof on unchanged paths can be reused; current native Review parity is still unproven. The prior accepted native receiver at `/tmp/cnative-dgfdgryu/codex` is available for the owned positive native agent path; it is not a model or user's active pane.

### Resumed batch consolidated gate — observed results, no product rerun

- PASS: `git diff --check`, two-file `rustfmt --check --edition 2024`, helper `node --check`, `bun run typecheck`, fresh `bun run build`, affected Vitest 35/35, `cargo test -p cockpit-core browser_feedback` 4/4, host `authority_guard` 2/2, and `cargo build -p cockpit-host --bin cockpit -p cockpit-tauri` (warnings only). FAIL: `cargo test -p cockpit-core browser::drafts::tests` 8 passed/1 failed: newly added tombstoned assertion in `stale_client_revisions_and_consumed_ids_cannot_resurrect_removed_marks` expected `browser_draft_identity`, received `browser_draft_provenance` at `drafts.rs:2163`; its context lacks inline provenance. A→B→A test passed in the same run, but the complete focused suite is red. No source/test/harness edits or test rerun after starting this gate.
- PASS, single real Chromium gateway journey: private session `tabreturn-b06051599370`, root `/tmp/cockpit-owned-tab-return-ug_em_tu`, gateway `127.0.0.1:34691`, owned fixture `127.0.0.1:41467`, dedicated Chromium root PID 3484474. First fixture rendered, region authored and note `Authoritative retained first-tab note b06051599370` saved; second page selected with Notes 0; returning to first restored Notes 1, region and exact text in rendered pixels. Send annotations produced authoritative `capture-c12ea68c-c1ad-4e6b-af81-1f57615020c0.json` and actual PNG 449×1312 under this root, same annotation ID/URL/note and one pending ID. Its delivery receipt is **rejected**, no agent target (`no eligible agent target is available in this Space`), acknowledged IDs empty; the UI showed Saved feedback recovery and Retry rather than consuming it or the old `Capture draft is no longer current` error. Thus tab-return capture is fixed on a real gateway, but positive agent delivery for this tab-switch path is unrun; the prior unchanged native recipient path is proved below. At 360px outer width, no horizontal document overflow and first-tab/recovery controls remained visible, but stacked split layout and recovery controls left only ~35px of canvas height at 760px high; do not call that a fully usable minimum-height browser.
- PARTIAL/FAIL, single combined native WebKit/Tauri run `native-fd9a7d67ce5d`, `/tmp/cnative-9p0a_j0b`: live page and 674×188 CSS-density bitmap at DPR2; authored region revision 2, saved exact native note revision 3, then captured PNG 1348×376 and pasted 2,860 bytes to a run-owned Codex-named raw PTY fixture, bracketed frames 1 and **no trailing Enter**. Herdr accepted operation `browser-feedback-fc0c9505-4e91-4b1e-a19c-04ec570db971`, UI reported `Pasted to codex · Enter not sent`, pending count 0. Later synthetic native pointer did not start the fixture's scroll after capture, so the run timed out waiting for scrollY 3900; nested wheel and subsequent Element acceptance in this run were **not reached**. Failure screenshot shows Browse selected, note and pasted notice, page still `waiting for routed native pointer`. The synthetic pointer route is not a physical OS wheel. No repair or rerun within this gate.
- PASS, bounded independent hostile authority/grant probe in `security-9a682f23966b`, `/tmp/cockpit-owned-security-x1ld3ql1`: positive correct Host+Origin GET status 200; hostile Origin, hostile Host, duplicate Origin and duplicate Host returned 403 `invalid_request_authority`. An authorized owned browser view returned first frame sequence 1 and a fresh frame grant; while an events WebSocket lease delivered `attached`, a forged wrong-view frame grant closed 1008 `invalid browser frame grant`, and the authentic grant on the same retained view returned a binary frame (9,856 bytes). This is only that bounded matrix, **not** WEB-08's full credential/lease/hostile page/malformed frame/filesystem security acceptance. Herdr/gateway/fixture private parents exited, root-isolated PID inventory was empty, gateway/fixture TCP ports and Herdr socket refused connection. Chromium gateway and failed native session likewise had zero owned processes, exited browser root and refusing dedicated TCP/Unix endpoints at cleanup.
- REUSED unchanged earlier FLOW-01 browser Context/Review local source/line-comment/refusal/re-preparation/browser reload proof and prior host concurrent admission. This batch's bare private fixture has no Review extension; current native Review/borrowed/dirty lifecycle and full hostile matrix remain unverified. No parent campaign issue is complete from these partial results.
- PASS, independent native-only 335-second animation/90-second idle/30-second hidden run `native-6240c388e8aa`, `/tmp/cnative-ek2xcsni`: actual native UI navigated the owned page; 19,397 canvas `drawImage` presentations / 335.004 seconds = 57.90 fps, authoritative scrollY 3900 and painted green end marker. Static idle and hidden intervals had zero paints, and Show returned to the same URL and visible marker. Initial/reopened canvas was 674×188 at DPR2; a post-scroll settled 1348×376 bitmap was observed, with `image_quality.acceptance=not_evaluated`; OBS-043 permits current lower-density frames, so density alone is not a fail. Raw 458 samples `/tmp/cnative-ek2xcsni/sustained-samples.json` SHA256 `2125a83be01f9b080640c4c934211a578e81f0ca628b830e028fd1bd0ad5930d`. **FAIL PERF-01/WEB-06 resource plateau:** after 30-second warm-up, 306 one-second samples across nine process identities: total PSS 448,455→541,702 KiB, linear slope **+349.80 KiB/s** (range 448,455–546,366); WebKit web content 196,191→274,847 KiB, **+280.21 KiB/s**; WebKit network 23,027→33,562 KiB, **+36.73 KiB/s**. Static-idle 90 samples still showed 525,885→536,415 KiB (measured +162.83 KiB/s), even though painting stopped. These are process PSS, not proof of a specific JavaScript leak. Only bounded fixture animation is exercised, not PERF-01's ten tabs, terminal+Review mixed workload or 100-response latency sample. Herdr/gateway/compositor parents returned 0, WebDriver stopped, root-scoped PID inventory empty, and fixture/5173/gateway TCP ports and Herdr socket refused connection.

Disposition: the A→B→A real capture and exact native agent paste are concrete improvements, but the added Rust assertion is red, combined native input/wheel acceptance failed before wheel, sustained WebKit PSS has no plateau, and full FLOW-01/WEB-06/WEB-08/PERF-01/ACCEPT-01 matrices are not proven. No further source change or product rerun after this batch gate; **no verified product commit** of the integrated unverified edits. All tests and runtime checks above are per-scenario, not campaign completion. Existing foreign work and default/user resources remain untouched.
