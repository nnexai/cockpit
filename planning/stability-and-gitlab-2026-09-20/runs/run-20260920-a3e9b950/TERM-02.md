# TERM-02 — selected terminal lifetime

## Accepted just-in-time plan

ACCEPTED Main/Astra openai-codex/gpt-6-astra, 2026-09-20. Dependency TERM-01 complete at 4faf2030361411480571fb3cc7a05cea0f41afa9, ledger checkpoint c360e68. Current product/source baseline is that commit; baseline clean after owned evidence/status commit. Lock app-shell + terminal. Main owns runtime/integration and shared authority/ledger files. Independent VIEW-01 may edit only context/review-owned files, never App/TerminalPane or shared transport/protocol files.

Inspected: full TERM-02 brief; current authority UI research/parity skill; App PaneView readiness and Workbench projection/history/renderPaneLayer; TerminalPane construction, data/resize/wheel handlers and attach/cleanup ladder. LSP references resolved TerminalPane to App and its behavioral tests. Observation #12/#9 is ground truth; source confirms every history projection mounts TerminalPane and only incoming/unpainted defers attachment. On leaving selected tab, old projection loses controlAllowed and opens an observe stream because deferTerminal becomes false. Server refusal is correct.

Chosen repair: eliminate long-lived mounted projection history. Retain at most current projection plus the immediately prior committed projection only while incoming selected panes finish the existing synchronous fit/readiness handoff. Do not retain any old projection sharing current pane IDs (avoid duplicate component identity). Cached outgoing handoff is attachment-ineligible; deferTerminal must be true whenever !incoming, in addition to the existing incoming unpainted sizing barrier. Existing effect cleanup closes/aborts stream and timer; outgoing controls/focus remain guarded by incoming. On readiness, outgoing PaneView unmounts and disposes xterm/listeners/observer. No long-lived hidden xterm or stream; metadata/render-view state continues in existing usePaneRenderers, not mounted terminal history. Keep true same-pane renderer binding stability. Remove obsolete MAX_CACHED_PANES only if solely used by deleted history.

Owned implementation: src/app/App.tsx projection retention and PaneView wiring; src/app/TerminalPane.tsx only if an actual lifetime gap needs repair; existing App.integration.test.tsx and TerminalPane.test.tsx behavioral cases. No CSS error suppression, no server changes, no retry expansion, no generic snapshot/renderer abstraction. Optional transient outgoing xterm has zero attachment and disappears when incoming sizing commits; measure actual duration/peak count. If it lasts beyond a bounded sizing handoff or blanks a previously healthy frame, stop and replan rather than weakening criterion 6.

Ordered recipe: Main first records red-capable 10-tab baseline trace; worker removes history and enforces attachment eligibility, adds a regression proving stream closure/no hidden reopen/revisit input and keeps existing handoff tests meaningful. Worker skips all formatting/build/test/service/commit commands. Main integrates, typechecks/builds and runs focused behavioral tests; lifecycle reviewer checks selected/current/retained identities; Main executes browser and real Linux Tauri channel scenarios. No exported API change expected; if needed run LSP references and migrate every caller before edits.

Coverage and exact proof: (1) ten one-pane tabs, sixty rapid GUI switches with terminal requests/selected identity/alerts traced; no hidden attempt. (2) exactly selected visible stream count after settle and deliberately hidden direct attach retains pane_not_visible. (3) revisit prior tab, send owned marker and observe its own output, same Herdr order/focus. (4) repeat sixty via actual native UI with invoke/channel traces. (5) count live stream lifetimes, DOM xterm hosts and bounded outgoing handoff before/after settle. (6) sample successive frames while switching under output; retain prior painted healthy view until sizing commits, no cached takeover/input/resize. (7) guarded TUI/snapshot compares selected Space/tab/pane and process continuity. Also create split to test multi-pane visibility, ownership loss, reentry during output and cancellation separately per transport. Original server gate, semantics and required browser/native surfaces remain unchanged.

Resources: reuse guarded csg-a3e9b950 session/root for TERM-02 only; Main may add ten tabs/split only within owned workspace. Existing fixture Herdr process stopped; validate guard/hash before launch. Own gateway port 37619 only after free check. Browser automation isolated; native uses owned headless GNOME/private bus/WebKitWebDriver from TERM-01, with TAURI_WEBVIEW_AUTOMATION=true and custom-protocol packaged build. VIEW-01 runtime is serialized after TERM-02; only implementation is parallel. No active/default session, installed app, personal browser or remote tracker mutation. Main owns teardown or durable transfer.

Escalate if required retention semantics cannot be preserved by bounded current/outgoing projection, if shared transport changes become necessary, or source baseline drifts. Strong Main replans; worker does not broaden slice. Accepted plan covers all seven criteria but proves none yet. Cheapest capable configured implementation worker follows this resolved recipe; Main retains final review/verification/commit.
