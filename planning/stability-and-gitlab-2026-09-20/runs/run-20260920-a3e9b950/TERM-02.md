# TERM-02 — selected terminal lifetime

## Accepted just-in-time plan

ACCEPTED Main/Astra openai-codex/gpt-6-astra, 2026-09-20. Dependency TERM-01 complete at 4faf2030361411480571fb3cc7a05cea0f41afa9, ledger checkpoint c360e68. Current product/source baseline is that commit; baseline clean after owned evidence/status commit. Lock app-shell + terminal. Main owns runtime/integration and shared authority/ledger files. Independent VIEW-01 may edit only context/review-owned files, never App/TerminalPane or shared transport/protocol files.

Inspected: full TERM-02 brief; current authority UI research/parity skill; App PaneView readiness and Workbench projection/history/renderPaneLayer; TerminalPane construction, data/resize/wheel handlers and attach/cleanup ladder. LSP references resolved TerminalPane to App and its behavioral tests. Observation #12/#9 is ground truth; source confirms every history projection mounts TerminalPane and only incoming/unpainted defers attachment. On leaving selected tab, old projection loses controlAllowed and opens an observe stream because deferTerminal becomes false. Server refusal is correct.

Chosen repair: eliminate long-lived mounted projection history. Retain at most current projection plus the immediately prior committed projection only while incoming selected panes finish the existing synchronous fit/readiness handoff. Do not retain any old projection sharing current pane IDs (avoid duplicate component identity). Cached outgoing handoff is attachment-ineligible; deferTerminal must be true whenever !incoming, in addition to the existing incoming unpainted sizing barrier. Existing effect cleanup closes/aborts stream and timer; outgoing controls/focus remain guarded by incoming. On readiness, outgoing PaneView unmounts and disposes xterm/listeners/observer. No long-lived hidden xterm or stream; metadata/render-view state continues in existing usePaneRenderers, not mounted terminal history. Keep true same-pane renderer binding stability. Remove obsolete MAX_CACHED_PANES only if solely used by deleted history.

Owned implementation: src/app/App.tsx projection retention and PaneView wiring; src/app/TerminalPane.tsx only if an actual lifetime gap needs repair; existing App.integration.test.tsx and TerminalPane.test.tsx behavioral cases. No CSS error suppression, no server changes, no retry expansion, no generic snapshot/renderer abstraction. Optional transient outgoing xterm has zero attachment and disappears when incoming sizing commits; measure actual duration/peak count. If it lasts beyond a bounded sizing handoff or blanks a previously healthy frame, stop and replan rather than weakening criterion 6.

Ordered recipe: Main first records red-capable 10-tab baseline trace; worker removes history and enforces attachment eligibility, adds a regression proving stream closure/no hidden reopen/revisit input and keeps existing handoff tests meaningful. Worker skips all formatting/build/test/service/commit commands. Main integrates, typechecks/builds and runs focused behavioral tests; lifecycle reviewer checks selected/current/retained identities; Main executes browser and real Linux Tauri channel scenarios. No exported API change expected; if needed run LSP references and migrate every caller before edits.

Coverage and exact proof: (1) ten one-pane tabs, sixty rapid GUI switches with terminal requests/selected identity/alerts traced; no hidden attempt. (2) exactly selected visible stream count after settle and deliberately hidden direct attach retains pane_not_visible. (3) revisit prior tab, send owned marker and observe its own output, same Herdr order/focus. (4) repeat sixty via actual native UI with invoke/channel traces. (5) count live stream lifetimes, DOM xterm hosts and bounded outgoing handoff before/after settle. (6) sample successive frames while switching under output; retain prior painted healthy view until sizing commits, no cached takeover/input/resize. (7) guarded TUI/snapshot compares selected Space/tab/pane and process continuity. Also create split to test multi-pane visibility, ownership loss, reentry during output and cancellation separately per transport. Original server gate, semantics and required browser/native surfaces remain unchanged.

Resources: reuse guarded csg-a3e9b950 session/root for TERM-02 only; Main may add ten tabs/split only within owned workspace. Existing fixture Herdr process stopped; validate guard/hash before launch. Own gateway port 37619 only after free check. Browser automation isolated; native uses owned headless GNOME/private bus/WebKitWebDriver from TERM-01, with TAURI_WEBVIEW_AUTOMATION=true and custom-protocol packaged build. VIEW-01 runtime is serialized after TERM-02; only implementation is parallel. No active/default session, installed app, personal browser or remote tracker mutation. Main owns teardown or durable transfer.

Escalate if required retention semantics cannot be preserved by bounded current/outgoing projection, if shared transport changes become necessary, or source baseline drifts. Strong Main replans; worker does not broaden slice. Accepted plan covers all seven criteria but proves none yet. Cheapest capable configured implementation worker follows this resolved recipe; Main retains final review/verification/commit.

## Revised accepted hydration handoff

ACCEPTED Main/Astra, 2026-09-20, after the first real post-edit run. The bounded-history repair removes hidden hosts/errors, but fit-only readiness exposes an empty selected terminal before its frame arrives. One fresh revisit measured 518ms from action to content; two identical control WebSockets opened 1ms apart, only the second survived, and its upgrade/full frame followed about 196ms later. This is fresh failure evidence, not a passing criterion 6. The initial automated hidden-attach flags are invalid because they compare against a lagging event-stream snapshot; correct that proof to retain DOM intent, accepted focus and snapshot observations separately.

HandoffAdvisor independently reviewed the bounded source/trace. Main accepts: attachment eligibility is current authoritative visible tab/layout plus live epoch, not painted status; success is first valid full-frame hydration, not fit/open; outgoing must be inert; readiness must survive continuously active layout changes but never detach/re-entry; derive effect dependencies from effective control/observe mode to remove the observed same-mode reopen. No shared focus/transport authority rewrite, timeout success, retry expansion, or server exception.

Revised recipe: let eligible incoming xterms attach after their existing synchronous fit even while the outgoing cover remains painted. Defer outgoing and unconfirmed tab/Space transitions; preserve already-visible split observers during pane-only focus confirmation. Keep PaneView readiness paired with attachment deferral, reset it on actual detach, and reject old activation readiness when props change. Publish ready from a cancellation/generation/renderer-guarded first-full-write callback; current genuine failure/closure also settles the cover into its real failure UI. Remove fit/open-only readiness. Use effective terminal mode rather than separate control booleans as the attachment-effect dependency. Make retained PaneViews inert and pointer-ineligible. Continuously attached same-pane layout changes can re-report current readiness without a second stream.

Retention policy is explicitly revised from sizing-only to one detached, frozen outgoing projection until the eligible incoming frame hydrates or a genuine current failure is shown. This retains all seven original criteria; it does not claim a one-refresh-cycle duration or a latency repair. Measure the actual retained-frame lifetime and peak renderer count on browser/native, including rapid reversal and split/zoom. If parsed contents still yield an exposed blank paint, require render completion with renderable incoming geometry; do not add arbitrary delays. A stalled/focus-failed transition must remain visibly pending/actionable rather than masquerading as live old content. Main owns these shared App/TerminalPane repairs and focused regressions before repeating proof.

Native discrimination: the full-write-only barrier still exposed one empty-content animation frame in 333 samples, although the browser run had zero empty frames in 336. Main therefore applies the already accepted conditional render barrier: after the first guarded full write, register `onRender`, explicitly refresh the fitted viewport, and settle readiness only from that render; dispose the listener on every retirement/failure. Empty terminal contents remain valid—there is no nonempty-text product predicate.

Native instrumentation correction: Tauri's `__TAURI_INTERNALS__.invoke` is non-writable/non-configurable, so the first wrapper recorded no channel calls and is not channel-lifetime evidence. Observe the writable `window.fetch` IPC boundary instead; record only command, pane/mode, returned stream ID, cancellation and safe error fields, never invocation-key headers. Verify host retirement with the existing `stream_not_found` response for canceled IDs. HandoffAdvisor also confirmed requested-control-only mode plus state/ref catch-up guard avoids the ownership-derived auto-reclaim trap; existing conflict/lost intent stays retired until explicit local takeover. Focus-confirmation flush preserves already-owned queued input without replaying dispatched commands.

User steering, 2026-09-20: make the debugging loop efficient and browser-first with Playwright CLI. Main attached named CLI session `csg-web` to the sole owned Chromium page at the guarded gateway, rooted CLI artifacts under the fixture directory, and closed the native app/input probe. Remaining shared-frontend scenarios and VIEW-01 failures are exercised and repaired on the web surface first. Native is reserved for native-specific behavior and final required acceptance; no scope or acceptance criterion is waived.

Extended browser layout probe, not a pass: creating a split and typing succeeded, but rapid zoom/restore exposed persistent blank output in the continuously mounted new pane. Raw pane-read history retained the marker and shell PID 524548; local visible xterm buffer was blank at base/viewport 169. A narrower trace/replay confirms a frame/local-grid mismatch can lose a row, but does not yet establish the complete blanking cause. OBS-010 assigns this output/grid/zoom work to required TERM-03 (its explicit original scope), after SYNC-01. TERM-02 continues its original selected-tab lifetime, split-tab switching, ownership-retirement and TUI criteria; it does not claim the extended zoom experiment passed. No required campaign criterion is waived, and further native debugging remains deferred under the user's browser-first direction.

## Acceptance results

All seven original TERM-02 criteria pass for the exercised Linux/browser fixture. Compact measurements are in [TERM-02-evidence.json](TERM-02-evidence.json); raw artifact hashes and build provenance are in [TERM-02-artifacts.json](TERM-02-artifacts.json).

| Criterion | Result | Evidence |
| --- | --- | --- |
| 1 | PASS | Final code, ten one-pane tabs, 60 Playwright switches: zero hidden attempts/errors, 365 sampled frames all with healthy visible content, peak 2 hosts, one UI socket/host at rest. Baseline had ten retained xterms and nine hidden alerts. |
| 2 | PASS | One-pane and split-tab runs settle to exactly one/two corresponding UI streams. Deliberate hidden WebSocket request still returns HTTP 503 `pane_not_visible`; server validation is unchanged. |
| 3 | PASS | Revisit while output continues reaches `TERM02-OUTPUT-60`. Latest post-loss marker is dispatched once, only to w1:p1, and appears as one echo plus one result in authoritative history; absent from the other pane. |
| 4 | PASS | Real Linux Tauri/WebKitGTK: 60 one-pane switches, 333 healthy-visible samples, one stream/host at rest; four retired IDs return `stream_not_found`. Additional split sweep: 328 healthy-visible samples, two streams at rest; six retired IDs return `stream_not_found`. |
| 5 | PASS | One-pane peak 2 hosts/rest 1; split peak 3/rest 2 on both surfaces. At most one detached outgoing projection is retained, including both hosts when that projection is a split. No cached subscription survives retirement. |
| 6 | PASS | Final single/split browser runs have no frame without healthy visible content. Real ownership loss leaves observation-only state. While the next focus request is held and the viewport shrinks, retained hosts are inert and send no input/resize/mouse/scroll or attach request. Positive WebSocket command capture observes 42 input commands, so the negative result is not an empty observer. A real `tab_not_found` focus failure remains visible on the committed pane and Retry succeeds. |
| 7 | PASS | Fresh, separately named guarded TUI captures bracket additional native and browser sweeps. Space/tab/pane order, selected w1/w1:tA/w1:pC, and layouts match before/after. Shell PIDs w1:p1=379000, w1:pA=379027, w1:pC=524548 are unchanged. TUI output contains the same native/split markers. |

The retained object is one frozen visual projection, not a promise to finish within one display refresh. Maximum sampled inert run: 910.6 ms in the final one-pane browser sweep, 929.2 ms in the split sweep. Native visible-old-pane intervals estimate 569 ms from selected-tab samples; this is not an independent inert-attribute timer. All retained runs finish. This increment repairs lifecycle and visible continuity, not transport latency.

Native keyboard proof used the owned compositor, not synthetic DOM keyboard events: 46 trusted keydowns and rendered `TERM02-NATIVE-INPUT`. The first private-pointer attempt triggered GNOME's hot corner; dismissing its overview after pointer activation within the same live RemoteDesktop session resolved input routing. No product permission/focus change was made. WebDriver DOM clicks were used for native tab actions; physical keyboard proof is a separate exercised path.

## Review, verification, and cleanup

`bunx vitest run src/app/TerminalPane.test.tsx src/app/App.integration.test.tsx src/app/review/ReviewPane.test.tsx src/app/review/ReviewViewer.test.tsx`: 61 passed. `bun run build` and native custom-protocol build passed. Existing bundle-size/Rust warnings remain. VisibilityReview found no actionable patch-introduced lifecycle findings after tracing detach, first-render readiness, canceled writes, and effective control intent; Main corrected the stale sizing-only comment. The reviewer did not independently rerun runtime proof.

Removed obsolete one-off browser/geometry/failure runners after saving their raw evidence. Retain the named Playwright session, generic visibility runner, native-channel runner and isolated keyboard helper for remaining campaign consumers; they are outside the repository. Closed only the owned pilot/split panes w1:pB and w1:pC after their captures. Stopped both oracle TUIs and input helpers; deleted owned native automation sessions. The shared owned server, gateway, browser and private compositor remain available for subsequent tasks. All six protected integration hashes remain unchanged since the containment correction. OBS-008 reconciliation and OBS-010 terminal zoom/output repair remain required; neither is resolved by this increment.

No standalone changelog exists. This run record documents the changed transition contract; existing Herdr authority/UI constraints are intentionally unchanged. Independent VIEW-01 working-tree changes remain outside this commit.
