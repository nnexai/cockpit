# WEB-05 — browser-page annotation navigation and quiet recovery

## Accepted user-priority increment — 2026-09-23

Baseline `96f79c81768428413027c59bbc12a127bb8a0c93`; WEB-01/02/03 are recorded done, but WEB-05's A14–A21/A24 and native matrix remain blocked. User OBS-030 explicitly supersedes the earlier WEB-05 requirement to retain/review abandoned annotations after ordinary page navigation: old, **unsubmitted browser-page marks/notes** should be discarded automatically when their target/document is replaced, with no “old annotations,” stale draft, capacity or retry notice on returning to the browser. The user's preference concerns page-scoped browser annotations, not Context/Review comments or already sent feedback. A save or paste with unknown remote outcome must not be replayed or misreported as discarded, and an already prepared/persisted capture must retain its same exact bytes and operation identity for honest recovery if still outstanding. OBS-031 further requires transient frame/viewport mismatch and normal navigation progress to remain visually quiet; genuine rejected, unavailable or uncertain user actions remain actionable inline. This is not blanket error suppression.

Exclusive worker B owns `src/app/browser/BrowserPane.tsx`, `src/app/browser/framePresenter.ts`, their relevant existing tests and browser styles, and `crates/cockpit-core/src/browser/drafts.rs` only if automatic durable old-document tombstone is required. No other worker writes those files. The existing `BrowserDraftRecovery` CAS discard/tombstone and `mark_stale` path are the canonical store; do not add another draft registry or API shape. On authoritative target/document change clear obsolete local overlay, editor/selection and notices immediately; serialize/cancel pending local writes before safely discarding run-owned old-document draft revisions, prevent stale async callbacks from recreating or painting marks, and ensure subsequent list/open does not expose old draft. Preserve association-owned saved feedback and pending capture receipts, avoid silent loss of an uncertain submitted operation, and never erase an unrelated target or other owner's work. On mismatched same-target viewport, display the best available authenticated frame fitted to the surface while existing control signals converge; use pinned descriptor for hit-testing/composition, and refuse only genuinely untrusted/obsolete target/document/epoch or unsafe input rather than raising a status on a single frame drop. The helper worker owns transport correction separately; unchanged wire is the shared boundary.

After integration, use one disposable browser gateway and one Linux-native WebKit fixture to author a region plus note, navigate A→B→A through actual Cockpit controls, observe authoritative document generations and painted pages, and verify old marks/Notes count and stale-warning/Retry UI are absent in B and on returning A. Also exercise same-document hash/history without deleting a still-current draft, failed navigation without losing current marks, concurrent/in-flight draft save without resurrection, and an actual rejected/unknown feedback action preserving honest recovery. Browser and native captures/action results are required; tests/typecheck alone do not close WEB-05. No active/default Herdr session, installed user app/profile, foreign repository or GitLab mutation. If durable tombstone conflicts with an uncertain send, preserve its receipt but keep obsolete local marks hidden; report the exact negative case rather than silently clearing a submitted operation. Original WEB-05 authoring/capture/delivery criteria not explicitly superseded remain attached for later acceptance.

## Accepted user-observed browser chrome polish — OBS-034–038

After the OBS-028–033 stream/native-close increment is proved and committed, implement exactly the five user-observed browser chrome defects in the existing `BrowserPane.tsx` and `browser.css`; worker ownership is exclusive and disjoint from `App.tsx`'s OBS-039 global Pane-menu removal. At a full 1568×1009 actual browser view and the private native WebKit surface, selecting or creating a mark opens a **small editor visually adjacent to that mark**, anchored through the *actually displayed* frame descriptor and surface dimensions and clamped within the viewport instead of the current wide bottom-left floating panel. A saved note stays attached to the mark; editor focus, Escape/Save and document navigation preserve current draft/uncertain-delivery rules. Do not put editing UI in remote page DOM.

While the browser annotation surface (not a textarea, toolbar, other input, composing IME or terminal) owns the keyboard, Delete removes only the selected mark through the existing revision-safe `removeAnnotation` operation and must not send remote Delete; without selection/annotation ownership it must not consume the page's ordinary Delete key. Keep the **existing trash control**: ordinary click removes the selected mark, Control-click clears *all unsent marks on the currently displayed document* through the existing `discard_draft` CAS and opens a clean same-document draft; it must be usable with annotations even without selection. Do not add a separate list, confirmation dialog, second icon, blind iterative remove, or discard a prepared/saved/pending capture or uncertain feedback; reject an unsafe bulk clear with one actionable local explanation. Clear/remote result and returning A→B→A must agree, including in-flight annotation mutations and revision conflict. The Send annotations affordance stays the same operation but becomes **one compact aligned icon button** with accessible name/title and no visible wide caption. Browser-tab close X is a separately operable, accessible control visually *inside its own tab chrome* without moving selection or closing neighbors. Avoid overriding the existing shared browser toolbar pattern or adding CSS for new UI surfaces.

Prove actual browser keyboard ownership, selected/delete vs no selection, Control-click bulk clear with draft recovery, mark-anchored editor near both middle and viewport edge, compact toolbar/tab close hit targets and navigation repaint through a disposable Herdr session and actual 1568×1009 browser screenshot/action→authoritative response→rendered state. Repeat the changed visible layout in private native WebKit; do not assert source-only snapshots as UI proof. Keep previous campaign WEB-05 A14–A21/A24 and accepted quiet-navigation scope open until individually exercised. No installation to the user's prefix, mutation of active session, blanket socket/process kill, or next increment's edits before stream/native Close commit.

### Private Chromium draft-retirement runtime check — in-progress stream batch

Run-owned Herdr server/session `cweb-stream-20260923`, workspace `w1`, Cockpit gateway from `/tmp/cweb-stream-0Df4eS`, fixture `127.0.0.1:37681`, dedicated managed Chromium tab at **1568×1009 CSS and explicit DPR1.25**; no active session/browser/install was touched. Through the actual Browser UI, opened page A, expanded to 1342×823 CSS surface, drew one region, saved note “Navigation draft must disappear,” and saw the annotation label, Notes 1 and the acknowledged editor close. Entered B through Page URL: fixture recorded page B `load` with 1342×823/DPR1.25; after `document_changed`, Notes 0/zero annotation rects/editor absent, no alert or retry. Clicked actual Back; waited for authoritative page A URL **and actual canvas pixel RGB (221,238,254,255)** at (12,12), Notes 0/zero annotation rects, toolbar “Live browser view,” no alert. Painted screenshot `/tmp/omp-sshots-158b4f4031108197.webp`. The tab title/URL advances before the document event, so a transient first inspection at B showed the old A rect until the authoritative generation transition; do not treat a tab URL as proof that the new document frame is already painted. This proves only a simple acknowledged-draft A→B→A path in Chromium. In-flight failed CAS retry, same-document history, failed navigation, unknown feedback and WebKit native still need explicit checks; original broader WEB-05 criteria remain open.

Durable browser A-return screenshot in this run: `WEB-05-browser-A-return-green.webp`, SHA-256 `c93ea6ba562b6fc13c81d28332e8e27c15a6184aa121df76679d3aef726f2f21`.

Additional same disposable browser gateway checks: authored another page A region and saved “Retain through same document”; owner draft-inventory POST `browser/drafts/recovery` returned draft `6a244d24-d644-4ef5-bc19-457aa7c1f741`, document generation `1790196548954`, revision 5 and that exact annotation text. Actual Page URL navigation to `/A#same-document` retained the same marked label/Notes 1 **and the identical backend draft ID/generation/revision**: hash history did not discard the same document. Actual URL submission `javascript:alert(1)` returned HTTP 400 `invalid_browser_url`/“unsupported or unsafe scheme”; page URL reverted to `/A#same-document`, original mark/Notes 1 remained and the toolbar eventually displayed that actionable rejection. No remote script ran. These verify one same-document and one definitively rejected navigation, not concurrent write or uncertain feedback.

### Verified OBS-034–038 browser chrome increment — 2026-09-23

- The editor now follows the selected mark through the displayed frame transform and clamps inside the actual surface, including after user textarea resizing; selecting a different mark first saves unsent note text. Escape/Close restore browser-surface focus. Selected Delete and ordinary basket click remove only that mark; unowned Delete still reaches the remote page. Control-click on the existing basket performs a serialized same-document CAS discard and opens a clean draft, without a Notes list or confirmation dialog. Send is icon-only with its accessible name; tab X is inside its tab hit target. The durable store refuses to discard a draft still referenced by a prepared or pending capture, without introducing a second draft registry or changing wire shape.
- Regression was red before the prepared-capture guard (`prepared_capture_preserves_its_draft_against_discard` returned `Ok(())` instead of refusal), green after. `cargo test -p cockpit-core browser::drafts::tests` passed 4/4, `cargo build --bin cockpit` passed, and the final `bun run build && bunx vitest run src/app/App.integration.test.tsx src/app/browser/BrowserPane.test.tsx src/app/browser/framePresenter.test.ts` passed 28/28. Two read-only reviewers identified stale editor selection, unpaired Delete, and prepared capture deletion risks; all were repaired in this increment.
- Actual Chromium in disposable session `uiwave-5d1ccc9377aa`, 1568×1009/DPR1.25, painted adjacent middle and edge editors, saved/switched notes, physically resized the textarea while controls stayed reachable, verified selected Delete and Control-click against draft recovery inventory and remote keyboard events, then reopened the browser on the final bundle and removed only the second of two marks by ordinary basket click. `WEB-05-OBS034-038-green.json` records precise dimensions, durable IDs/revisions and negative controls; `WEB-05-OBS034-browser-{toolbar,middle,edge,resized}.png` are actual browser captures.
- A fresh private Tauri/WebKit debug window ran final frontend `index-ss06VAGP.js` at 676×835/DPR2: the 320×132 editor was focused and clamped next to the real mark in a 674×188 surface; Tauri draft recovery acknowledged mark revision 2 and saved note revision 3, then painted the saved label. No top Pane, separate Notes list, or verbose Send remained, and the integrated tab X was 28×28 with no border. `WEB-05-OBS034-native-{editor,saved}.png` preserve visible before/after; private Niri Close reaped the native descendants and owner socket. A prior disposable installed release with the same BrowserPane changes also passed its annotation/chrome smoke; the final debug WebKit run covered the later app-shell menu reflow change. No user's installed prefix or active Herdr session was touched.
- This bounded chrome result does **not** mark WEB-05 complete: its original A14–A21/A24, native keyboard/bulk controls, uncertain feedback/send behavior, and accepted quiet-navigation matrix remain to be exercised. The separately observed intermittent WEB-04/WEB-06 DPR2 density failure (`WEB-04-native-chrome-density-unstable.json`: 674×188 and 1348×376 alternate at the same viewport revision) also remains open despite the successful final native UI run.

## Accepted next bounded increment: native page-scoped draft retirement — 2026-09-24

Baseline `e2d1a30f4a04c2f96a2c05d757a5d3d6512b0f1c`, WEB-05 active under Main's `browser-ui`, `browser-helper`, `browser-store` and `comment-delivery` locks. Previous Chromium A→B→A plus same-document hash and rejected URL checks are recorded above; the missing independent real surface is native Tauri/WebKit. This increment covers only unsubmitted, page-scoped A18/navigation cleanup and OBS-030. All original A14–A21/A24 authoring, capture, composition, feedback, multi-client, capacity and failure-retention criteria remain WEB-05-owned; an unknown send, pending/prepared capture or Context/Review comment is **not** an abandoned page draft.

No product edits are planned unless a native reproduction proves a specific defect. `BrowserPane.tsx`'s existing `retireDraftsFor` queues CAS `discard_draft`, filters late mutation responses, and gates stale `openDraft` callbacks; `crates/cockpit-core/src/browser/drafts.rs` guards prepared/pending capture references. If a repair is necessary, Main owns those files and re-runs the affected focused gate; no wire change, new registry, retry loop, unrelated rendering fix or installed-user-prefix mutation. Use existing `cockpit_browser_draft_recovery` IPC for authoritative draft IDs/revisions rather than assuming painted disappearance equals deletion.

Fresh, uniquely named `wnav-native-*` Herdr session, isolated HOME/XDG/config/socket/fixture/Niri/WebKitDriver, ephemeral localhost ports and debug native binary embedding final frontend `index-ss06VAGP.js`; do not attach to the active/default user session. Through actual native Browser controls: navigate to private A, create region and save unsent note; record target, document generation, draft ID/revision and painted mark. First submit a rejected unsafe URL and a same-document hash: require the current mark, ID and note to remain. Navigate A→B: require B's painted page and authoritative new document/target, zero old marks/editor and no stale/Retry notice; inventory must lack old unprepared draft ID. Back to A: require fresh A page/pixels and still no old marks, note or warning. Negative control: a prepared/pending capture must not be tombstoned by CAS discard; use an isolated core regression or safe native preparation without sending if available, and report exact coverage instead of silently treating it as discarded. Confirm native process/owner-socket cleanup while independent Herdr stays alive until its own run-only teardown.

Evidence: actual native screenshot before navigation and on B/A return; compact JSON with UI actions, Tauri draft recovery responses, page identity, rejected URL status, same-document hash, saved note, cleanup, exact frontend/binary identity. Positive and negative require UI plus backend agreement. If WebKitDriver loses its session, a native density frame oscillates, or an external prerequisite prevents one check, preserve the observed failure and do not claim native parity; do not repeat an unchanged failing approach. No GitLab/agent send, protected branch, user's installed app, or default Herdr resource is in scope. Main accepts this plan; no unresolved assigned-slice design decision remains.

### Verified native page-scoped draft retirement — 2026-09-24

- On source `e2d1a30f4a04c2f96a2c05d757a5d3d6512b0f1c`, `python3 /tmp/wnav-native-ZVpoDC/native-navigation.py --skip-build --native-only-open --quiet-navigation --timeout 30` exercised fresh, private Herdr `wnav-native-0e3c4e2767c3`, localhost fixture `127.0.0.1:46593`, Niri and WebKitDriver against `target/debug/cockpit-tauri` and frontend `index-ss06VAGP.js`. It submitted the initial Page URL through the native UI, drew a region with separated DOM pointer events after confirming the transient drag, saved an unsent note through the actual inline editor, and checked Tauri IPC `cockpit_browser_draft_recovery` plus visible WebKit paint. The page A mark was draft `cd20fcba-dd01-4c40-a831-a715a75d45fe`, revision 3, document generation `1790212864180`, note visible and header pixel `(221,238,255,255)`. Three final window screenshots and exact binary/frontend/capture hashes are in `WEB-05-native-navigation-green.json`.
- Submitting `javascript:alert(1)` displayed the actionable unsafe-scheme rejection and retained the current URL, mark and draft. Submitting A#same-document retained that same draft ID, document generation and note, revision 5. Submitting B loaded the independently reported orange fixture, painted `(255,225,201,255)` from its source `(255,225,202,255)`, produced document generation `1790212865700`, showed no mark/editor/notes/alert/retry and left **no** drafts in authoritative Tauri inventory. Native Back loaded blue A again at generation `1790212865966`, still no old marks/notes/retry; inventory held only a new clean draft `a3304d87-6f55-449b-992d-3a0381dee803`. The old draft did not silently return. The temporary session's fixture/static servers stopped, WebKitDriver stopped, and private Niri/gateway/Herdr exited 0. Browser Chromium navigation proof at lines 19–25 remains independent.
- The native W3C WebDriver pointer-actions endpoint returned HTTP 500 unsupported, so this proves the WebKit UI handler and Tauri store via separated DOM pointer events, **not native OS-pointer delivery**. An initial uninstrumented paired event failed to admit a mark; a later staged event confirmed its transient SVG before release. The unsafe-rejection text is in `role=status`, not `role=alert`, and remote painted flat-orange JPEG readback differed one blue channel from source; the throwaway harness predicates were corrected, not product code. A successful same-document hash left the prior unsafe rejection in the header until B: queued `OBS-040` to WEB-04, without claiming an error-free hash status. The existing intermittent 674×188 versus 1348×376 canvas at 674×188 CSS/DPR2 also appeared across isolated runs and remains WEB-04/WEB-06; no density or full native parity claim follows from this navigation result.
- This bounded A18/OBS-030 navigation subset is verified without a product edit. WEB-05 stays active under Main's existing locks. A14 freehand/element authoring, A15/A16 inspection boundaries, A17 pinned PNG, A18 composition/retry/capacity/concurrency, A19 exact lookup/ack, A20/A21 recipient/paste and A24 races/payloads remain unexecuted; the prepared/pending guard had its focused core regression in the prior committed increment but not a complete native capture lifecycle. Next selection: just-in-time plan a separate positive and negative capture/delivery increment against the settled source, with unique private session and actual PNG/receipt authority, then continue remaining criteria. Do not stage protected `.omp/*`, `.audit/`, or the unrelated earlier edits already present in `tasks.json`.

## Accepted next bounded increment: captured PNG integrity and no-recipient retention — 2026-09-24

Baseline commit `825405527bc42fc1bed287007b669bd3b53f0bbe` with active WEB-05 Main ownership/locks (`browser-ui`, `browser-helper`, `browser-store`, `comment-delivery`), settled WEB-01/02/03 dependencies and prior native-navigation evidence above. `tasks.json` has pre-existing unstaged modifications and is **not** wholesale-staged; `.omp/*` and `.audit/` remain protected. This increment covers only A17's successful region+note PNG on one normal viewport per real surface and A20/A21's **negative no-eligible-agent delivery**; it also observes a non-consuming A19 lookup twice without claiming the complete exact acknowledgement/retention contract. A14 other tools, A15/A16 DOM boundaries, A17 multiple DPR/zoom, A18 failure/retry/races, A19 idempotent old/new ack and closed-view fetch, A20/A21 successful focus/paste/unknown outcome and A24 capacity/multi-client/payload review remain explicitly attached to WEB-05. No legacy migration/A23, new API, fake recipient, blanket install, active Herdr mutation, protected branch or production GitLab writes.

Current source contract: `BrowserPane.tsx` `captureImpl` obtains `localLocation()` plus `capture_prepared`, verifies accepted frame sequence/target/epoch/document/viewport, composes PNG from only `canvas.browser-frame` and saved marks/comments, persists `save_capture` against original draft revision and only then invokes `onFeedback`. `App.tsx` forwards stable operation ID through `sendCapturedFeedback`; a rejection leaves saved capture and unsent annotation work recoverable. Rust store remains the authority for exact saved bytes and feedback identity. Use existing HTTP `/api/v1/browser/feedback` and `/feedback/image` or native `cockpit_browser_feedback` and `cockpit_browser_feedback_image`, plus draft recovery list. No product edit is planned; if capture integrity or retention fails, return the exact red action/response and Main serializes a narrowly scoped repair after both readers settle. Do not invent a second registry, recapture under one ID, auto-retry an unknown send, or clear marks because the destination is absent.

Two independent **read-only-product** verification slices run concurrently with exclusive resources and proof files. Browser slice owns only its uniquely named disposable Herdr server/session, isolated HOME/XDG/socket/gateway/localhost fixture, harness Chromium tab/profile and `WEB-05-capture-browser-*` run captures/JSON; native slice independently owns its own unique Herdr/session/HOME/XDG/socket/Niri/WebKitDriver/fixture and `WEB-05-capture-native-*` captures/JSON. The browser slice uses actual canvas Region drag, inline note Save and Send button, verifies HTTP saved lookup twice (same capture/annotation ID and pending count), decodes stored PNG, checks page pixels, mark/comment pixels and chrome exclusion, captures visible rejected/no-recipient state with mark/notes still displayed, and checks draft recovery; verify prior successful UI/browser URI changes are not introduced. Native slice uses the actual Tauri/WebKit controls, separately dispatched pointer events only after confirming transient SVG because WebKitDriver W3C pointer action is unsupported, inline note Save and Send, then native IPC lookup twice and PNG image bytes, same positive and no-recipient negative assertions; record source/canvas DPR mismatch instead of calling density fixed. Each slice starts a blank private fixture, records binary/frontend identity, capture provenance, full PNG SHA-256/size/dimensions, source/captured sample locations, original/current draft revisions and any pending/delivery receipt; no personal user sessions.

Only Main writes the shared `WEB-05.md`, OBSERVATIONS/ledger or product files and integrates evidence after both slices settle; workers skip formatters, linters, builds, tests and commits. A real saved PNG plus duplicate read without consumption and visible no-agent retention is the bounded positive+negative gate; a successful request without rendered feedback failure and post-save image authority is not proof. Preserve actual failures rather than lowering the gate. Native/browser layouts may expose the independently open OBS-028/029/WEB-04/06 density/input defects; name and queue them, do not mask as capture success. Main reviews each result, validates independent screenshot/PNG/state evidence, runs only affected focused checks if product code changes, then commits this verified increment and appends its full SHA to the active task ledger. Plan accepted by Main; no assigned-slice design decision remains.

### Verified captured PNG and no-agent saved-feedback subset — 2026-09-24

- Browser worker `CaptureBrowserProof` used only private Herdr `polish-45_xi88j`, Cockpit gateway `127.0.0.1:34017`, color-coded fixture `127.0.0.1:37281` and managed Chromium. The actual Page URL, Region drag, color `#1769aa`, inline note Save and Send produced capture `e5d58853-263f-4637-b189-8a40736bd8c5` from draft `4215b72c-7775-4649-97b6-255f9eea5c48` revision 4. Two HTTP feedback lookups preserved the same pending annotation ID `17c34cf0-44cd-4149-9cdc-33ab7077986a` and count 1; the image endpoint returned a 408×682 PNG (103,042 bytes; SHA-256 `0e62ce1774bc7d7214d63d4ce7480fe2efdec48b1453255686bae8dad04a814b`). I viewed the copied PNG: page-blue, gold and mint fixture pixels and the blue region/label are present, without Cockpit tab/address/tools. The browser screenshot visibly states “no eligible agent target is available in this Space” plus a rejected-feedback retry path while showing the region/label; no eligible agent or acknowledgement was introduced. `WEB-05-capture-browser-evidence.json`, `WEB-05-capture-browser-saved.png` and `WEB-05-capture-browser-no-recipient.webp` are the durable proof. Its private services/tab and temporary root were stopped/removed.
- Native worker `CaptureNativeProof` used separate private Herdr `native-613cd3b23fa8`, Niri/WebKitDriver and fixture `127.0.0.1:39321`. The actual native Region, note Save and Send yielded capture `676c69ba-579c-4a7c-89db-659ad6894864`, annotation `96d4bee7-2ee8-428e-9b81-f71297349a94`, saved page+frame provenance target/document `73E391FFEF53ADEA10837E95AB0E1D8F`, frame sequence 11, viewport revision 1. Two Tauri feedback IPC reads kept pending count 1 and the same capture; Tauri image IPC returned a 674×188 PNG (52,597 bytes; SHA-256 `ec0fa6c7415db7ee3edeb4f7d18c69dda382a48ac36e0e1d97d7c6741799cd5e`). I viewed the copied PNG and WebKit no-agent screenshot: real fixture pixels, red region/note with 1,337 red overlay pixels and no host chrome; the native banner says “Feedback delivery was rejected; retry the same saved capture.” `WEB-05-capture-native-result.json`, `WEB-05-capture-native-captured.png`, `WEB-05-capture-native-no-agent.png` and before/after native screenshots preserve exact surface evidence. WebKit W3C pointer actions remain unsupported: separate DOM pointer events with confirmed transient SVG were used, not OS pointer proof. At the same 674×188 CSS surface/DPR2, the first canvas was 1348×376 but the before-send canvas was 674×188; WEB-04/06 density remains open. Private fixture/static servers stopped, WebKitDriver stopped, and Herdr/gateway/Niri exited 0.
- Both raw backend PNG files independently matched their recorded SHA-256. Earlier native *throwaway harness* attempts were not product passes: one read after the first sent action raced the asynchronous capture save (first feedback lookup empty, second populated), and two wrongly expected a no-agent error only in the header when the actual native delivery banner visibly reported the rejection; a first note wait was too narrow. The final result waited the authoritative saved capture and rendered banner; raw exploratory base64-heavy files and service logs were removed after preserving this diagnosis. No worker changed product code, ran tests/builds/formatters or committed.
- `OBS-041` is the required limitation, not a fabricated no-agent editability pass: after the saved capture, both current *editable draft* inventories advanced to revision 5 with zero annotations while the no-agent screen still showed the old overlay; the same mark/note are durably retained in the pending immutable feedback capture. This proves successful one-viewport A17 PNG, A19 repeated non-consuming lookup, and negative absent-recipient saved-byte retention on browser/native, **not** hide/reopen/editor recovery, multiple DPR, successful recipient paste or the remaining A14–A21/A24 matrix. WEB-05 remains `in_progress`. Next action: accept a bounded OBS-041/A18/A20 recovery plan, exercise hide/reopen and selected-edit/remove of the pending saved capture with exact ID/PNG checks, and repair any misleading/unrecoverable transition before closing the no-recipient criterion. Preserve `tasks.json`'s pre-existing unrelated edits when recording this increment's commit.

## Accepted next bounded investigation: OBS-041 saved feedback after owner remount — 2026-09-24

Baseline `083fc7da5b9acd2e02b393838aed6758b8754cdb`, WEB-05 still Main-owned and dependency-ready under existing browser UI/helper/store/comment locks. The two independent actual capture proofs above establish immutable PNG and pending feedback, but both current draft inventories were empty while the old mark remained rendered. Do not assume this is data loss: the saved pending capture contains the original annotation ID/comment and image. Conversely, do not treat a stale in-memory SVG as proof the mark remains editable or can be retried after remount. Original WEB-05 A18/A20/A21 recovery/destination criteria and no duplicate retry remain open; A14/A15/A16/A17 multiple sizes/A19 exact ack/A24 are untouched in this diagnostic increment. No product/API change or user session mutation is authorized to workers.

Two **read-only-product** verification slices, browser Chromium and Linux Tauri/WebKit, own disjoint uniquely named Herdr sessions, isolated HOME/XDG/socket/ports/fixtures and separate `WEB-05-OBS041-{browser,native}-*` run artifacts. Reuse the known successful region/note/capture pattern from `WEB-05-capture-*-*`: actual Page URL, Region drag (native separate DOM pointer events, no OS input claim), note Save, Send with no eligible agent, wait for exact pending capture and image hash. Positive control: repeat lookup/image before/after every action proves the frozen ID/PNG/note persist and no feedback is acknowledged or automatically replayed. Negative control: click/select the *painted* saved mark, attempt to edit its note and explicitly Save or remove it through UI; capture the visible success/refusal and authoritative editable-draft revision/annotations after the action. A UI success claim without a matching durable edit is a defect; an explicit refusal with the exact frozen capture retained is honest. Then leave the Space without discarding saved feedback (create a second **private** Space if needed), return through actual Cockpit controls to remount BrowserPane, and report visible pending-recovery/retry affordance, mark/editor state, Send enabled state, repeated capture lookup/PNG hash, and any focus/status failure. Do not click a Retry that could send to a real recipient; no fixture agent is created. If the UI offers only a destructive close/discard, stop and record its guard rather than bypassing it; a safe separate-Space switch is preferred.

Workers own only their distinct `/tmp` and `runs/.../WEB-05-OBS041-browser-*` or `...native-*` evidence; they skip builds, formatters, linters, tests and commits and do not edit `BrowserPane.tsx`, core drafts/delivery, protocol, shared `WEB-05.md`, observations or `tasks.json`. Main integrates actual responses/screenshots, compares them with `BrowserPane` component-local pending-delivery refs and core `apply_capture_receipt` consumption, then accepts a **new bounded repair design** only if a real loss/misleading action is proven. Any repair must preserve immutable saved bytes, stable operation identity for rejected versus unknown delivery, selected new edits, prepared/pending capture guards and quiet ordinary navigation; no blind retry or global change to feedback semantics. Native/browser surfaces and authoritative store both required after a repair. Observation acceptance here is a diagnosis with actionable positive/negative evidence, not a WEB-05 completion or invented successful remount path. Main accepts the read-only verification plan; assigned slices have no unresolved design decisions.

### Observed OBS-041 action and handoff failure — 2026-09-24

- Browser `polish-_t0imugi`: after real Send/no-agent, capture `cde8806a-e1b6-4346-b5fb-5e63d28c0699` retained pending annotation `80cd3d04-a237-420a-b9da-fdcf8d26202c`, original note and 408×682 PNG SHA-256 `7c1bfecc237db140f77fca34ae0d4aca3f75f63eda6e9a5e3f8383f6311722c6`; editable draft advanced revision 4→5 with **zero** annotations. The still-painted region opened the original-note editor. Explicit Save left the new note only in that editor, displayed “Retained annotation changes need review,” and did not alter draft or frozen capture. The resulting local retry intent blocked the normal browser handoff: after creating a second private Space and returning to the first, only “Review retained browser work” with Retry/Discard appeared; BrowserPane, URL, editor and Send were absent, and Open browser did not restore them. Repeated HTTP feedback/image lookup retained exact ID, pending count 1, note and PNG hash; no paste or acknowledgement occurred. See `WEB-05-OBS041-browser-evidence.json` and its owned screenshots/PNG.
- Native private WebKit `native-1f9f400dc7aa` independently reached the same zero-annotation revision-5 branch after Send/no-agent. Attempted note Save left the frozen pending capture `47464af2-9f6a-4073-839f-64b1a99f51fb` untouched (PNG SHA-256 `40caab3626db80c03f5298d2167f2958682f39ffc9dcebac0f414e611aad8cf0`), and actual Space switch/return exposed the same browserless Retry/Discard guard without invoking either action. An independent native run `native-8254c81bb9f4` exercised the *other* valid receipt branch: the core retained a newer draft annotation with original ID `c6d7cb7f-1dd7-48e1-b64c-a7d4ea4d3353`; its later note Save advanced draft revision 4→6 while the pending capture `15516c80-5a81-47a6-a8c5-d47675c7eac7` kept the original frozen note and PNG SHA-256 `5b71186e4e76c2b9bd44451cc93b7b6ea96bc235c37b885a1fa98929a85f0b23`. This is distinct newer editable work, not mutation of an immutable sent capture. Native input used separate DOM pointer events because WebKitDriver W3C actions are unsupported; no OS-pointer claim. Other native attempts exposed intermittent DPR bitmap density and stale draft revision refusals; the result JSON retains them as open WEB-04/06 and WEB-05 diagnostics, not passing paths.
- Source explains both branches: `drafts.rs::apply_capture_receipt` consumes only annotations matching frozen bytes and without a newer editor note, whereas `BrowserPane` leaves its pre-receipt draft painted and keeps `pendingDeliveryIdsRef`/operation in component-local refs. `closeGuard` treats even durable rejected feedback as in-flight, and remount currently reads only draft inventory, not saved feedback. This is a **proven misleading edit and stranded handoff**, not loss of saved PNG. OBS-041 and A18/A20/A21 remain open.

## Accepted bounded repair: truthful saved-feedback handoff and explicit recovery — 2026-09-24

Baseline `083fc7da5b9acd2e02b393838aed6758b8754cdb` plus the OBS-041 diagnosis artifacts above. WEB-05 remains Main-owned under `browser-ui`, `browser-helper`, `browser-store`, `comment-delivery`; both private probe sets are cleaned. Only the verified saved-capture transition, rejected/unknown receipt visibility and hide/Space-return recovery are in this increment. Do **not** relax exact immutable PNG/annotation identity, replay a paste during load/hide, acknowledge pending work, rewrite newest draft edits, bypass prepared/pending-capture guards, change unrelated browser input/DPR or claim A14–A21 completion.

Main owns the shared `crates/cockpit-protocol/src/browser.rs` DTO, generated `src/protocol/generated/v1.ts`, `src/protocol/generated/v1.ts` export workflow and `src/client/CockpitClient.ts` parser. Extend existing `BrowserFeedbackLookup` by `deliveries: BrowserFeedbackDeliveryStatus[]` (serde default for old stored/transport envelopes). A status is `{ capture_id, operation_id, selected_ids, state, message }`, with `state` the existing `CommentPasteState`. This is a **read-only association lookup**, at most one relevant latest durable receipt per pending capture; prefer a pending/unknown outcome over a rejected one for overlapping IDs, never leak a different association, never turn a pending receipt into success, and bound directory scanning/response by existing store limits. The frontend uses the exact returned operation ID for an unknown/pending receipt; a rejected receipt permits a *new* operation only after an explicit user click. If no durable receipt exists, the deterministic `browser-feedback-${capture_id}` identity is recovered but no send occurs on mount. No new endpoint or transport fork.

Backend worker owns only `crates/cockpit-core/src/browser_feedback.rs`, `crates/cockpit-core/src/browser/drafts.rs` and any same-boundary focused Rust test, implementing the bounded read-only `deliveries` projection from authoritative pending captures and stored receipts. Frontend worker owns only `src/app/browser/BrowserPane.tsx` and its existing CSS if strictly needed: after `save_capture` or `retry_pending` saved receipt, reconcile the live draft from the store before allowing another edit; preserve newer annotated draft versions rather than clearing them. On mount/association rebind, read `client.browserFeedback` once, display pending saved feedback independently of current editable marks, and expose an explicit retry/review path even with no live draft. No auto-send. Clear frozen painted marks that the store consumed; a retained newer mark remains editable and its current note must not be misrepresented as the already-frozen feedback note. Saved feedback survives hide/Space handoff; block only unacknowledged local editor/mutation or not-yet-durable capture, not a durable pending saved receipt. Respect stale association/request identity and release live resources normally. For unknown status require explicit duplicate-risk acknowledgement before any new operation; same operation ID is only for non-dispatching receipt reconciliation. Multiple pending captures remain individually addressable, not silently overwritten by one ref.

Two independent writers may proceed concurrently only after Main publishes the DTO contract; no shared file edits. Workers skip builds, tests, formatters, runtime services and commits. Main integrates protocol generation/parser, reviews delivery/ownership/security boundaries, runs narrow affected checks once, then requests new disjoint browser and Linux-native *actual UI* probes with private Herdr/session/fixture roots. Positive: saved region/note Send/no-agent, visible truthful pending receipt after hide/Space switch/return, explicit retry after still-absent recipient retains exact capture ID/note/PNG hash, no auto paste/ack; on a retained-newer-note branch live draft edit remains distinct from immutable capture. Negative: select old consumed mark cannot open a false editor or trap handoff; an unknown receipt cannot silently generate a new operation or duplicate paste, and concurrent newer draft edits remain. Record both surfaces, exact state/receipt IDs, cleanup, review and owned commit before declaring this bounded repair verified. Main accepts this plan with no assigned-slice design decision left open.

## User-requested handoff — 2026-09-24

Stop here. The user explicitly said: “stop the loop of trying to fix this with subagents. do one attempt of fixing each issue and then test everything at the end once,” then “just do a handoff.” Do **not** start another implementation/review/verification subagent, run intermediate issue-by-issue test loops, or commit unverified product changes. The two already-running disjoint implementation workers finished their **first pass**; no runtime verification of that pass has begun. On resume, complete one direct integration/implementation pass across the remaining issues, then perform one consolidated end-of-batch test and actual-surface verification. Record any failure honestly instead of silently starting another repair cycle; a changed plan needs the user's explicit direction.

The last owned commit is `9cc51f8d8c48284a8a4000ab0405170bcc5c9549` (OBS-041 browser/native diagnosis, screenshots and accepted repair plan). The previous accepted PNG/no-recipient proof is `083fc7da5b9acd2e02b393838aed6758b8754cdb`. Ledger `tasks.json` is valid (`campaign.py check` returned `{"ok":true,"tasks":27}` after the diagnosis entry), but is **unstaged** with older pre-existing modifications plus owned WEB-05 evidence/commit entries. Required tasks: 17 `done`, WEB-05 `in_progress` and Main-owned under `browser-ui`, `browser-helper`, `browser-store`, `comment-delivery`; FLOW-01, WEB-04, WEB-06, WEB-08, PERF-01 and ACCEPT-01 `blocked`. The campaign goal is **not complete**; macOS-specific NATIVE-02 remains deferred by the user. Do not stage `.omp/*`, `.audit/` or `tasks.json` wholesale. The diagnosis workers cleaned their private Herdr sessions, browser/native fixtures, runtime services and `/tmp` roots.

Uncommitted **first-pass product code** is confined to `crates/cockpit-protocol/src/browser.rs`, `crates/cockpit-protocol/src/typescript.rs`, `src/protocol/generated/v1.ts`, `src/client/CockpitClient.ts`, `crates/cockpit-core/src/browser_feedback.rs`, `crates/cockpit-core/src/browser/drafts.rs` and `src/app/browser/BrowserPane.tsx`. Main added a generated `BrowserFeedbackDeliveryStatus` and `BrowserFeedbackLookup.deliveries` plus a bounded client parser; the protocol exporter ran and emitted only its existing ts-rs warning. `SavedFeedbackStore` added read-only association-scoped pending receipt projection, prioritized unresolved outcomes, and an **unrun** focused unit test `delivery_status_prefers_unknown_and_stays_association_scoped`. `SavedFeedbackUI` added authoritative saved-feedback hydration, exact receipt operation/ID handling, explicit per-capture recovery, post-save draft refresh, stale-mark editing guard and non-destructive saved-feedback Space handoff. Neither worker ran a build, typecheck, test, browser/native smoke, formatter, linter or commit. Main has reviewed the backend projection source but has **not** reviewed the full UI diff or integration. No claim that this first pass compiles or fixes the user-facing path.

Exact resume sequence: (1) inspect the seven uncommitted product paths as one integrated change; address only integration breakage in the same direct pass, preserving immutable capture ID/PNG/note, a genuinely newer editable draft, exact pending `selected_ids`/operation identity, explicit unknown duplicate-risk acknowledgement, and no paste/ack during hydration; (2) finish a single direct pass over remaining campaign issues without launching fix workers, respecting each task brief/locks and recording any unavailable external prerequisite; (3) once code is settled, run **one consolidated** protocol export `--check`, focused Rust feedback-status test, frontend typecheck/build, and affected Linux-native build plus real browser and Tauri/WebKit scenarios in new uniquely named disposable Herdr sessions; compare Space selection with the Herdr TUI in a disposable session and check positive no-agent capture/reopen/explicit retry plus negative stale-mark edit/unknown no-auto-retry, exact pending feedback ID/note/PNG hash and cleanup. A request returning 200 without rendered result and backend receipt is not a pass. (4) Review all results as one batch; commit only owned changes whose declared behavior passed, record exact full SHA/evidence in the ledger via an isolated status checkpoint, then run `campaign.py check`. If a gate fails, leave the affected task open with the exact failure; do not call `campaign.py complete` or complete the umbrella goal unless all 24 required tasks and ACCEPT-01 really pass.

## Direct integrated pass and one runtime gate — 2026-09-24

- Retained the uncommitted saved-feedback association projection and explicit recovery UI. Corrected a moved test fixture ID. The user's newly observed Element regression was traced to hover inspections invalidating the click inspection: Element pointer-down no longer sends a competing inspection, pointer-up owns the selection until its result settles, and hover samples are coalesced every 40 ms. Local frame coordinates do not use the independently advancing remote cursor token. The target/document/frame checks remain in place. A successful same-document URL change now clears the obsolete unsafe-URL rejection text.
- Consolidated static gate: protocol TypeScript `--check`, focused Rust delivery-status test (1 passed), frontend typecheck/Vite build, 28 focused frontend tests, host/native debug build, and `git diff --check` passed. The initial gate found a moved test string and stopped before the remaining commands; the ownership correction preceded this passing gate. The pre-existing ts-rs `deny_unknown_fields` warning and unrelated Rust dead-code warnings remain.
- Real disposable Chromium at 1568×1009/DPR1.25 used Herdr `polish-ix4yvrwy`, gateway `127.0.0.1:46237`, and page `127.0.0.1:35661/`. Actual Element hover over the paragraph produced an SVG preview of width 670; physical pointer-down/up created one element mark and its adjacent note editor. Saving “Element note private proof” painted its label and authoritative draft recovery returned the same element annotation and note at revision 4. Clicking Send with no eligible agent produced a visible rejection and a saved capture recovery control. Closing and reopening the owned browser returned a fresh start page **with the rejected saved capture still available** and no automatic paste. Captures: `/tmp/omp-sshots-158ba083b1a80905.webp` (pre-selection) and `/tmp/omp-sshots-158ba091d5680906.webp` (selected element/editor). The managed tab and only run-owned fixture processes were closed.
- The one native WebKit fixture `native-f87f8dd7a666` failed *before* annotation checks: first rendered canvas was 674×188 pixels over a 674×188.47 CSS surface at DPR2 (bitmap density ~1 instead of 2). `/tmp/cnative-onqq27r4/native-first.png` and `native-failure.png` record the result; the fixture stopped its gateway, Herdr, driver, compositor and HTTP servers. This reproduces the independently open native density problem, so native Element parity and saved-feedback remount remain **unverified**. No speculative native-density edit or repeat run was made. No product commit is claimed from this red integrated gate; WEB-05/WEB-04/WEB-06 and final campaign acceptance remain open.

## Active WEB-05 A14/A15 browser acceptance; Element status and point bounds

Private Herdr `polish-klen4tzd`, Space `w1` plus disposable `w2`, gateway `127.0.0.1:40771`, fixture `127.0.0.1:40115`, managed Chromium 1568×1009/DPR1.25. The actual Page URL navigation, toolbar color/tool selection and physical mouse path (35 moves) painted green freehand, simplified to **11** persisted points in draft `54f41b77-b757-4378-929e-1bd553554dc7`; note Save stored “Green freehand exact note.” A second red Region persisted with the first mark, selecting its SVG and pressing real Delete with `.browser-surface` focused removed **only** the region. After creating run-owned `w2` and returning to `w1`, the original target/document/draft ID/revision 8, 11-point freehand and note reappeared without a duplicate or retry. An immediate read at Live status found no mark while draft hydration was still in flight; waiting for the actual mark distinguished automation timing from data loss. The full-document navigation to private `/targets.html` painted the new page, retired the old unsubmitted draft and opened a clean revision-1 draft; this is OBS-030 navigation behavior, not a hide/reopen loss.

On the new page, actual Element hover/click saved an ordinary DOM `p` with `#normal`, real bounds and excerpt; the open-shadow text click saved the containing `#shadow-host` div with empty text rather than claiming its inner paragraph (shadow child evidence remains unproven). Hovering inside a same-origin iframe reported the visible, honest limitation “Element inspection cannot cross into a nested browsing context.” **Reproduced status defect:** moving the real pointer back over the normal paragraph restored its valid hover rectangle, yet the toolbar still reported that obsolete iframe limitation. The accepted inspect path in `BrowserPane.tsx` applies the new inspection but never clears an earlier inspect-specific unsupported notice. Repair only this request-scoped stale status and verify unsupported→supported recovery; do not hide an actual iframe limitation or clear unrelated feedback/error messages. Separately, A14 explicitly requires bounded iterative freehand simplification; current `simplify` recurses over arbitrary raw points and `onPointerMove` copies the whole growing array on each event, while the store/wire accepts at most 8192 points. Replace recursion with a bounded explicit stack and enforce that existing limit without silently truncating or deleting older marks. No protocol, helper, store or native density changes. This browser subset does not close WEB-05's native, PNG multi-size, delivery, idempotence, and race matrix.

## Verified bounded WEB-05 authoring, receipt, and direct-clear repair — 2026-09-24

- In private Herdr `polish-klen4tzd` at 1568×1009/DPR1.25, physical 35-move freehand persisted 11 simplified points, an Element inspection selected an ordinary paragraph and the containing open-shadow host, and a same-origin iframe reported its unsupported nested context. A valid paragraph hover after the iframe now clears **only** that obsolete inspection notice. Actual pointer capture through 8,200 synthetic moves cancelled the unfinished over-8,192-point stroke with a visible actionable alert, retained four earlier marks and released capture; a subsequent physical short stroke saved a fifth mark. The raw stroke now appends without per-move array copying, simplifies with an explicit stack only at release, and does not truncate a too-long stroke into a misleading annotation. `src/app/browser/BrowserPane.test.tsx` covers an independently observed draft-open race: a control lease change during inventory must use the current lease to reopen the same document's retained draft.
- Actual Browser UI Save produced immutable capture `04f51be9-c8d1-45f3-a4ec-6a5b08edd424`, 562×823 PNG SHA-256 `26b93818609a23cf50077b2c3126f6070b051574c87d267e0b3bdbd622aa1046`, five selected annotation IDs, provenance target `C8CE69403625EB5807B7E07A300F8929`/document `1790222280839`/frame 93/viewport revision 11. A second saved capture `407030ec-b30d-4215-aad5-be626bf8a4df` was a 562×791 PNG SHA-256 `39ecac7e0a8021da2828b9031d1751a0913b1ba729ff752911b7bfeed037d456` with one newer ID. With no eligible agent, the exact first receipt was **rejected** and its bytes/IDs remained pending; two correct association-scoped feedback reads left all five IDs untouched. Exact ack of the first five left the newer ID pending, and an identical second ack acknowledged none. Closing the owned browser did not prevent feedback lookup or image lookup: the returned newer PNG hash matched the stored file. No successful agent paste or no-Enter receipt is claimed.
- A trial change that exposed older-document unsent drafts in a separate Notes/list panel was **withdrawn**: it conflicted with user OBS-030's automatic retirement of ordinary obsolete page marks and OBS-036's explicit no-list, no-dialog basket shortcut. Final Browser UI has a passive `Notes N` count, mark-local editor, the existing ordinary-delete/Control-click basket, no extra list or confirmation; navigation again retires obsolete unsubmitted drafts. New regression checks the old draft's exact revisioned discard and zero current-document notes, while preserving the original same-document lease-race fix. Browser screenshot `WEB-05-current-browser-toolbar.png` (SHA-256 `c84a4126b639f58318902efbc582957373a8d5d06345d007acb479fa96254981`) shows the rebuilt 562px surface, `Notes 0`, no list, fixture pixels and independently recoverable rejected capture. A current-page mark saved before navigation is not evidence for retaining that old *unsent* mark after navigation; pending/saved captures remain separate authoritative feedback.
- The first extended native WebKit region→note→immediate Control-click basket scenario **failed** with the mark still painted and the old draft at revision 4: the editor autosave advanced the queued revision after `discardDraft` had captured revision 3. The header remained “Live browser view” without displaying the discard failure (`/tmp/cnative-4zac_w7i`; screenshot `WEB-05-native-basket-race-before.png`, SHA-256 `270e2ccdc74bdf43d65f455727b378875f7e10cf092bb6ee767b6978525cd8ca`). The repair selects the latest **same-draft, same-document** owner revision inside the serialized mutation; a genuinely different draft or out-of-band revision conflict still refuses deletion, and annotation-specific failures are visible. The focused regression queued an editor save before direct basket clear and required the acknowledged revision 3, not stale revision 2.
- Rebuilt native Tauri/WebKit `python3 scripts/verify/native_browser_acceptance.py --annotation` then passed in isolated Herdr `native-60c554e038e2` (`/tmp/cnative-7yv8pgzq`): region annotation `a861d344-44ee-4adc-b1ad-1a6ab0440c9a` persisted in draft `6de2f93e-cdb2-49dd-8767-6a55ed5d9f5b` revision 2; the mark-local note “Native note native-60c554e038e2” reached revision 3, Notes 1 and a painted label. One DOM Control-click on the **existing** basket, without a Notes list/confirmation, removed that exact old draft, opened clean draft `88ce9eb1-9f80-40f1-adfa-342192ca7d91`, painted zero marks and Notes 0. `WEB-05-native-note-basket-before.png` (SHA-256 `34c65f47a8d4a5fe307700996291cb9d57ccd2b0df96a026a9592ac1d11db215`) is the real pre-clear WebKit screenshot. WebDriver native pointer actions are unsupported; this checks native DOM handler/store/render, **not OS-pointer input**. The fixture also reported 361 actual canvas paints over 6.004s, 60.12 FPS and scroll Y 3900; native bitmap remained 674×188 for CSS674×188/DPR2, which is a permitted performance tradeoff but not full image-quality acceptance. All six owned native processes/services stopped.
- Rust draft serialization now rejects an oversized pretty-JSON revision **before** atomic replacement, preserving the last readable draft or exact pending capture. `cargo test -p cockpit-core oversized_` passed five filtered tests including two new preservation cases; browser `bunx vitest run src/app/browser/BrowserPane.test.tsx` passed 7/7 after the queued-revision regression, and `bunx tsc --noEmit` passed. The separate read-only feedback review found no source edit needed for exact/idempotent ack, association scope, focused recipient revalidation, pending→unknown interrupted dispatch, no auto duplicate, and raw-point redaction; positive selected-agent/native paste remains unverified.
- Diagnostic, not a navigation retry: in disposable `w2`, two Page URL submissions produced **“Input outcome unknown: Private browser helper did not confirm the command outcome”** and a subsequent stream Retry reported not ready. Direct read-only CDP `/json/list` showed the target actually at `/targets.html` while the frontend still displayed the start URL; no same gesture was automatically replayed. This is an input/helper-confirmation handoff for WEB-04/06, not a WEB-05 accepted delivery outcome or evidence that the navigation never occurred. The initially apparently ignored “Open browser” after reload was automation timing: waiting for the app hydration/request painted the existing w1 association, Notes 0 and saved rejection without another click.

WEB-05 remains `in_progress`: A14 native freehand/color/edit and full browser/native gesture matrix, A15/16 boundary/drift cases, A17 multiple DPR/zoom/resize and pinned composited PNG in this repair, A18 disk/store/unknown-save/restart races, A20/21 successful focused same-Space recipient/no Enter/unknown receipt, A24 multi-client conflicts and retention are not closed by this bounded increment. Do not infer WEB-04/06 sharpness completion from the native density observation.

## Verified focused-recipient paste and accepted UI receipt — 2026-09-24

- The owned Chromium session `polish-klen4tzd`, Space `w1`, gained a **run-owned Codex-named raw PTY receiver**, not an AI model or user's terminal. Herdr reported the focused agent in `w1:p1`. Explicit Retry of previously rejected capture `407030ec-b30d-4215-aad5-be626bf8a4df` delivered its one selected ID with a durable accepted receipt; the PTY received exactly one complete bracketed-paste frame without Enter or raw freehand points. A new physical Region drag/note Save/Send on `/targets.html` produced capture `e5757237-dcf6-49c8-9d40-882d961fc65d`, exact selected/acknowledged ID `f9251be3-4e6c-4579-aa6c-79ffcedc0153`, and a second one-frame PTY write with no trailing Enter. The actual Browser UI displayed **“Pasted to codex · Enter not sent”**; authoritative association lookup returned pending count 0. `WEB-05-browser-agent-accepted.png` (SHA-256 `e15f3a5c842fb5afe1771f0278b792b63ac02d917ba22682bc5875792fd91ca3`) shows the rendered receipt. The browser-only composited 562×823 PNG `WEB-05-browser-agent-capture.png` (176,680 bytes, SHA-256 `68b9e2223d5a8a47171febda65f03959d23d60ab0ee21bb7c3456d283cfe9853`) contains page pixels, region and note, not Cockpit chrome. The accepted durable operation receipt targets precisely the focused `w1:t1/w1:p1` agent and acknowledges only the selected ID.
- The native acceptance harness adds an **optional run-owned agent fixture** to its existing private WebKit scenario; no installed model/provider or active Herdr session is touched. Rebuilt native Tauri/WebKit in unique `native-08fb7b5b1e90` (`/tmp/cnative-dgfdgryu`) exercised actual Region/note/Send through the native UI. Herdr recognized its focused `w1:p1` agent; operation `browser-feedback-65cf342d-ed80-4593-b4a2-da9497fc03dc` accepted/acknowledged exact ID `4ae3403b-c3ea-4b6f-934b-bea50b25c5dd`; PTY received precisely 2,843 bytes, one bracketed frame, no trailing Enter or raw points; feedback lookup returned pending count 0. The verifier waited for the **saved-feedback recovery control to disappear** while the accepted receipt remained rendered, then captured `WEB-05-native-agent-accepted.png` (SHA-256 `6a425b1833a4929a804ec4fe5ea2f3e8f1387a04cdde354a1d15da375ace150e`). `WEB-05-native-agent-capture.png` is the separate 674×188 page-only PNG (50,714 bytes, SHA-256 `720aaa3030b60dc66fe053dec5204b74199ff3e96bd0a34d3697dbc0903caa75`). Native first painted at bitmap density ~1 with DPR2 and ran 360 paints/6.006 s (~59.94 fps); quality is best-effort, **not** a claim that native density or OS-pointer actions are fixed. Six fixture services stopped cleanly.
- The first native fixture extension stopped on a harness-only `KeyError` indexing the annotation ID at the wrong JSON level; correction used its observed authoritative `annotations[0].id`. The next run passed but photographed a transient saved control before UI reconciliation despite pending count 0; a post-ack UI wait produced the final screenshot above. Neither is a product retry/duplicate paste. The accepted-receipt repair only adds a truthful status to `BrowserPane.tsx` after exact delivery and clears it on new work; no automatic retry or generic live-status suppression. The Rust store now serializes bounded draft/pending records **once** and atomically writes those exact checked bytes rather than pretty-serializing them twice; `cargo test -p cockpit-core oversized_` passed 5 and `cargo test -p cockpit-core project_store::tests` passed 15 after the change. The rebuilt native scenario above exercised the ordinary write path. Frontend `bunx tsc --noEmit && bun run build` passed.
- These checks prove a focused same-Space **Herdr PTY** and native/Chromium rendered acceptance, not AI ingestion or a physical native OS pointer. A deliberately unknown paste outcome was **not** retried here; the earlier no-agent negative and exact repeated acknowledgement remain separate. WEB-05 still owns native freehand/edit/color, DOM inspection drift and iframe/shadow/transform/nested-scroll/canvas boundaries, multiple DPR/zoom/resize captures, store/unknown-save/restart paths and multi-client retention/conflicts (A14–A18/A21/A24). Do not mark it complete.

## Verified nested-scroll Element boundary and native parity — 2026-09-24

- Reproduction in private Chromium `polish-klen4tzd`: Element selected `#nested-content` with a 350×280 DOM box even though its overflow parent exposed only 100 CSS pixels; the annotation SVG crossed into the unrelated iframe. A physical wheel in Element mode did nothing **by design** (`onWheel` routes remote input only in Browse). After switching to Browse, a real wheel changed nested page pixels `(150,222,202)`→`(130,198,207)` while the saved element box stayed at y516.59, proving the descendant cannot remain anchored by the page-scroll descriptor. This is an Element geometry defect, not a wheel-routing failure. The run-owned Python `HTTPServer` fixture had separately stalled on Chrome's idle preconnected socket (`lsof`/`ss` identified its blocking connection), so `ui_polish_runtime.py` now uses `ThreadingHTTPServer`; the new isolated fixture served subsequent Chromium requests. A first verification against the pre-rebuild Rust gateway loaded the **old embedded helper** (`include_str!`), explaining an unchanged hover rectangle; `cargo build --bin cockpit` and a fresh disposable session supplied the actual new helper. No product edit was based on the stale-runtime result.
- The helper now refuses an Element pick if it or an ancestor has independently scrolling/clipping overflow that makes its visible bounds unanchorable; the UI explicitly shows **“Element inspection cannot anchor a nested scrolling or clipped element; use Region instead”** and persists no mark. In private Chromium `polish-on747asm` at 1568×1009/DPR1.25, the negative screenshot `WEB-05-nested-element-refused-browser.webp` (SHA-256 `3fee65ba8f874286b62912e6e40ff98d36e2e2da9c6786f1c521c32253943455`) shows that alert and no false rectangle. Physical hover over the transformed `#scaled` div cleared the obsolete alert, produced box `(44,412.59375,272.800018,90.200012)` and a real draft Element with locator `#scaled`, exact text/excerpt and ID `57f5c21a-6045-4e80-8b80-8c94ddb5fd95`. Page-owned Browse wheel then repainted its mark y412.59→252.59 (160 CSS pixels of page scroll), while durable document bounds stayed y412.59. `WEB-05-transformed-element-browser.png` (SHA-256 `b8284d171605816627558eede6325986e658ba9269ed8145ef4b85ac9f4d8117`) shows real transformed page pixels and aligned red border after scrolling. Actual canvas pick reported “Canvas content has no inspectable inner DOM element,” and a same-origin iframe pick reported “Element inspection cannot cross into a nested browsing context”; neither created a mark. A second fixture parent loaded an iframe at `http://localhost:33889/frame.html` from a `127.0.0.1` page (server logged the request): the cross-origin pick reported the same explicit boundary and Notes 0. Previously verified ordinary paragraph and open-shadow host evidence remain unchanged; closed shadow was not independently exercised.
- Private native Tauri/WebKit `native-c74890deffc1` (`/tmp/cnative-l6kx4ldu`) ran `python3 scripts/verify/native_browser_acceptance.py --element-boundary` after rebuilding the native helper and frontend. Separated DOM PointerEvents on the live native browser surface selected the sticky fixture's nested overflow child: no element was stored; the same visible refusal appeared in `WEB-05-nested-element-refused-native.png` (SHA-256 `9ddaf8ea214ebfd45983b47e84240e3f05dff14e70158b1592d5f049d3d77f4b`). A subsequent valid `h1` hover cleared the alert and picked/stored Element ID `81b06262-30ab-4dd6-a0e4-cab4c43927f1`, evidence tag `h1` and bounds `(12,3912,650,45)` after fixture root scroll3900. `WEB-05-valid-element-native.png` (SHA-256 `1aa224a621b1115a93ecbb6a5248b88d11fd4791e43a9ef3787812ee07a65aed`) visibly shows the aligned red border and adjacent editor. Native painted 361 frames/6.008s (~60.08fps); bitmap674×188 over CSS674×188 at device DPR2 remains best-effort lower resolution, not a resolved density or physical OS-pointer proof. All six native fixture services stopped. `node --check browser-runtime/browser-helper.mjs`, frontend `bunx tsc --noEmit && bun run build`, `cargo build --bin cockpit` and the full native scenario passed.
- A separate run-owned A18 write-denial probe in `polish-on747asm` made only its private draft directory read-only. A real Region drag displayed **“Retained annotation changes need review”** and an explicit Retry saves control; the last durable draft remained revision 1 with zero annotations. After restoring write permission, clicking Retry saves once removed the recovery control and durably wrote revision 2, one red region ID `2169573c-f01d-43cc-91de-10cf45154249` with its original `(85.3975,205,99.9167,85)` bounds. The retained intent was in memory; this does **not** prove restart after an unpersistable disk write or a literal ENOSPC condition. Saving its note reached durable revision 6. Closing the **entire managed Cockpit client tab** later caused the remote page document generation to change `1790227448219`→`1790227845656`; the old note remained in a stale draft revision 7 and the new document correctly showed Notes 0 per OBS-030. This was not a same-document Hide/Show test and must not be called a lost-draft bug.
- Browser-harness DPR change attempts are not an A17 pass: `emulate` advertised DPR2 but actual Chromium remained 1.25; a raw page `setViewport` briefly returned DPR2 before the next managed-tab operation restored 1.25, even for a newly opened shared tab. A dedicated Chrome CDP launch with an owned profile timed out before creating a tab. No DPR2 browser capture/zoom proof is claimed. A17 multi-size/zoom, A18 lost save response/restart with recoverable bytes, A21 unknown paste and A24 multi-client capacity/races remain open.
