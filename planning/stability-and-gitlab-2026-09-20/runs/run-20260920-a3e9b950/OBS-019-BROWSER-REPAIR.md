# OBS-019 — browser repair checkpoint

Date: 2026-09-22. Source parent: `fc74e47` (`fix(browser): preserve input across viewport updates`). macOS native acceptance is explicitly deferred by the user and is not represented as passed. Linux-native annotation acknowledgement remains required under WEB-05.

## WEB-04 code change

The UI now consumes wheel input during a viewport/frame transition instead of dropping it before dispatch. It waits for a frame matching current metadata, maps the original client pointer position against that frame, and dispatches the wheel with the fresh viewport identity. Adjacent pending wheel events at the same client point and modifier state accumulate their deltas while preserving queue boundaries. One two-second deadline covers the whole stalled frame transition, so queued wheel work cannot hold later keyboard/navigation boundaries for a new timeout per event. Disabling live input advances the input generation and invalidates a waiting wheel before a quick re-enable can revive it.

## Verification

- `bunx vitest run src/app/browser/BrowserPane.test.tsx src/app/browser/framePresenter.test.ts` — 5 tests passed across 2 files. BrowserPane regressions cover frame-mismatch wheel retention, accumulated pending deltas, fresh-frame viewport/coordinate mapping, queued keyboard order, timeout allowing a later tab command, and disable/re-enable cancellation.
- `bun run build` — TypeScript check and Vite production build passed. Vite printed its existing large-chunk advisory.
- `python3 planning/stability-and-gitlab-2026-09-20/campaign.py check` — passed for all 27 task records.
- Independent read-only review by GPT-6 Luna found the disable/re-enable race; the input-generation invalidation and regression were added before this checkpoint. A prior review also found the per-wheel timeout hazard; the implementation and timeout regression now use a shared transition deadline.

## Still open

At the 2026-09-22 checkpoint this was component-level regression proof, not live browser acceptance. Repeated wheel scrolling, nested scrolling, page-link navigation and recovery still needed a run-owned browser gateway. The Linux-native annotation save had persisted without UI acknowledgement; source review ruled out a property-order mismatch but had not captured the actual Tauri response. OBS-023 below supersedes **that specific native acknowledgement blocker** with a captured response and real-surface proof; WEB-05's broader capture/delivery/concurrency criteria remain open. FLOW-01, PERF-01, WEB-08 and ACCEPT-01 retain their separately recorded evidence gaps.

## OBS-023 follow-on — native annotation acknowledgement repaired

The previous native failure was reproduced in a new isolated Tauri/WebKit run with `python3 scripts/verify/native_browser_acceptance.py --skip-build --annotation`. The durable store had the region at revision 2 and the UI showed Notes 1 but retained a **Retry saves** intent. A temporary run-only source trace (removed before delivery) captured the actual `cockpit_browser_view_command` accepted draft response: expected region point/bounds `x=99.99999999999999`, `width=120.00000000000001`; Tauri/Rust round-trip returned `x=100`, `width=120`. The target/document/lease context was unchanged. Exact `JSON.stringify` annotation equality rejected those one-ULP differences, and its fallback upsert with the pre-write revision 1 was correctly rejected as `Draft changed; recover the latest revision before editing`. The transient error text could be overwritten by later `Live browser view` frame status even while the retry intent remained visible. This is a **false acknowledgement failure after a successful write**, not a failed storage write or a reason to relax revision checks.

`BrowserPane.tsx` now compares all annotation identity, kind, color, comment, evidence, point count, bounds, and coordinates; only finite numeric coordinates receive a two-machine-epsilon, magnitude-scaled round-trip tolerance. The same comparison governs live acknowledgements, recovery acknowledgements, and retained-intent reconciliation. Real revision/identity conflicts still fail; no uncertain mutation is automatically replayed. The temporary native invoke/comparator probes were removed before the final build.

Clean-source command `python3 scripts/verify/native_browser_acceptance.py --annotation` rebuilt frontend, embedded-helper gateway, and native app and passed in disposable session `native-7e722e99ddc9`: a DOM-dispatched region (not native physical pointer proof) produced one visible mark/Notes 1, no retry intent, and authoritative revision 2. Optional note Save then passed in `native-bb815403af79`: exact text `Native note native-bb815403af79` persisted at revision 3 on the **same annotation ID**, note editor closed, no retry intent, and the red region plus label visibly painted in [native note screenshot](OBS-023-native-annotation.png). That run also retained first-paint DPR-2 geometry (674 CSS px → 1348 image px) and **361 actual canvas paints / 6.0022 seconds = 60.14 FPS** during the six-second 3900-pixel scroll. Both native runs stopped only their owned services.

Shared-frontend browser proof used owned Herdr session `web-annotation-dh00Yb4V`, Space w1, fixture `127.0.0.1:56377`, and gateway `127.0.0.1:37563`: managed Chromium at 1392×835/DPR 1.25 painted a 488.55-CSS-pixel/611-image-pixel pane. A real Puppeteer mouse drag created one region; physical browser note editing and Save closed the editor, showed label `Browser region note dh00Yb4V`, left no Retry saves intent, and produced authoritative draft revision 5 with that exact comment. [Browser screenshot](OBS-023-browser-annotation.webp). The managed tab, owned gateway/helper, fixture server, and isolated Herdr were stopped. `bun run build`, two focused BrowserPane tests, and `git diff --check` passed after the source change. Neither this targeted repair nor the DOM-only native gesture closes WEB-05's broader capture/delivery/concurrency acceptance, WEB-04 physical native input, or the untested original Google page.
