# VIEW-01 — review page and scroll identity

## Accepted just-in-time plan

ACCEPTED Main/Astra openai-codex/gpt-6-astra, 2026-09-20. TERM-01 dependency complete at 4faf2030361411480571fb3cc7a05cea0f41afa9; status checkpoint c360e68. Lock context-review. All original VIEW-01 criteria preserved. No source-review candidate is claimed as a runtime defect before execution. Independent TERM-02 owns App/TerminalPane and its tests; workers must never overlap these files. Main owns shared integration, ledger, authorities and all runtime verification.

Inspected ReviewViewer appendSourcePage/sourcePages/sourceLoading/snapshot/renderFile, ReviewPane snapshot/file effects and scroll restoration, ContextViewer Context/Review view-state types/defaults, and full original brief/authorities. Current page loader creates an unretained AbortController; append callback checks offset/revision only; sourceKey lacks session/binding/review/generation. Diff scroll key contains only comparison/path, so new generation/revision shares restoration identity. Existing snapshot/file effects already model cancellation plus identity/generation checks: extend those patterns, not a new document store.

Chosen contract: ReviewPane owns exactly one source-page controller and request token. Abort and invalidate it on identity, review generation, file, side, comparison, refresh, or unmount; revalidate full session/pane/binding/review_id/generation/file_id/side/revision before exposing a page. Aborted/superseded result resolves as absent, never a stale error. Current real errors remain actionable. ReviewViewer must guard append and final loading publication by the full immutable source identity and request token, and clear old page caches on presentation/snapshot invalidation. No late old callback may alter selected text, loading, selection, scroll, comments or current error.

Use source key containing session/pane/binding/review_id/generation/file_id/side/revision. Preserve byte offsets and prevent appending a page against a missing/replaced prefix; existing offset/revision checks remain. No pagination contract/backend change. Distinguish same-path new revision/generation from same revision revisit. Extend existing ReviewViewState with revision-aware bounded scroll positions (at most 64 identities) and current scroll identity, initialized by createReviewViewState. Key diff/source separately, including comparison, review generation/file identity and side/revision; restore matching saved positions only, default new identity to top. Existing scalar scrollTop continues as current-view display state, not cross-identity authority. Preserve selection/comment drafts with their existing review/source provenance; no automatic relabeling of stale comment references. SourceLines must receive the active identity's recorded position, not another side's value.

Owned implementation paths: src/app/review/ReviewViewer.tsx, ReviewPane.tsx, relevant existing tests under src/app/review/ or src/app/Review*.test.tsx; src/app/context/ContextViewer.tsx only ReviewViewState/defaults and source restoration wiring if needed; directly relevant context behavioral tests. Run LSP references before exported type/component modification, migrate callers inside ownership, report any App.integration.test.tsx/shared callsite to Main instead of editing TERM-02 ownership. Do not change transport/protocol/backend, generic comments, presentation styling, media policy or renderer strategy. No speculative virtualization. Existing Context cancellation is retained unless real evidence identifies a gap.

Recipe: implement retained page controller/full identities, revision-aware bounded restoration and narrowly behavioral deferred-response regressions. Workers skip formatters, linters, builds, tests, services and commits. Main runs red-capable deferred-response control against baseline and repaired scope after both writers settle, then focused type/build/tests. Cancellation/scroll reviewer checks integrated change. Runtime proof is serialized after TERM-02, not shared concurrently.

Coverage: (1) browser/native hold a truncated Review source page, switch file/comparison/pane, resolve old success/error and observe no stale text/scroll/selection/error. (2) same revision revisit restores; new generation/revision starts top. (3) hold recursive picker/document/search work then change root/file and resolve obsolete responses; current directory/focus/comments/loading unchanged. (4) source/Markdown/Mermaid/image/safe HTML retain rendering and scrolling. (5) desktop/minimum keyboard/wheel/comment/page append keeps active line visible with stable scroll. (6) measure cold/revisit long Context (5,000 lines) and Review (200 changed files) DOM count, first presentation/append/scroll latency and memory; reject virtualization unless measured budget demands a separately accepted replan. (7) guarded Herdr/TUI confirms same pane hierarchy/focus and process continuity around GUI navigation. Retain durable unsent comments across identity change and require their current revision before save. All browser AND Linux-native criteria remain mandatory, not covered by component tests alone.

Resource/authorization: Main creates fixture nested dirs/documents/diffs within /tmp/csg-a3e9b950; no personal repo/active session. Use existing guarded csg-a3e9b950 only after TERM-02 releases runtime, or a separately named/rooted guard entry if concurrent runtime becomes necessary. Current headless GNOME/private D-Bus/WebKitWebDriver recipe supplies actual native proof; no product instrumentation. Recorded fixture metadata/markers and owned captures survive service teardown. No provider/remote mutation, no installation. Main owns cleanup/commit and evidence rows for all seven criteria.

Escalate to Main if proposed data identity does not match generated DTOs/current core behavior, if cancellation requires a shared transport edit, if a required callsite is outside owned files, or measurements exceed frozen budgets. Do not widen scope or decide a different state architecture as worker. Accepted plan is implementation authorization only, not verification evidence.

## Integration repair and validation scheduling

- Main addressed the review findings: unified diff scroll identity uses both revisions and no selected-line side; local scroll values are paired with their identity so a newly rendered file cannot inherit the previous scalar. Leaving source mode re-enables diff restoration, and source-mode outer scroll clamping is not saved as diff scroll.
- SourceLines is keyed by the complete existing review source identity, while CommentDrafts remains mounted under its durable comment-batch identity. ReviewPane receives the actual rendered mode immediately; duplicate mode publication was removed.
- Added targeted behavioral coverage for line selection/source-mode round trip and same-content new-generation scroll reset. Existing late-page/error cases remain.
- Per user OBS-011, the current wave uses minimal focused checks and one brief browser smoke after integration. Original exhaustive native/media/long-document/TUI criteria remain queued for final integration, not waived. No VIEW-01 completion claim.
