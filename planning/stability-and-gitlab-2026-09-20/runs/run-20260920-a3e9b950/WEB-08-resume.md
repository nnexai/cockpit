# WEB-08 resumed hostile-negative evidence — 2026-09-24

Scope: bounded browser-gateway hostile/positive probes plus a private Linux-native browser smoke. No product source, harness, ledger, or task-status edits. This is not full WEB-08 acceptance and does not close any parent criterion. No real provider or user session was mutated.

## Owned disposable resources

- Gateway run: Herdr session `web08-9d2c4ab7ac`, Space `w1` (“WEB08 Security”), root `/tmp/web08-sec-ZQ5t0a`; gateway `127.0.0.1:38643`; fixture `127.0.0.1:38644`; owned Chromium incarnation `pid=3647914` while open. Target `C8A7368B7E5BF75488E8A325DDD5188F`; view `b642a18c-69b7-4655-b28a-bb8e19135846`, stream epoch 1. Frame grant value intentionally omitted from durable evidence.
- Native run: `scripts/verify/native_browser_acceptance.py --skip-build --native-only-open --timeout 35`; session `native-c7ab913419be`, Space “Native browser 274a37c6”, root `/tmp/cnative-rbjvt5op`, native fixture `127.0.0.1:34543`. No other service used port 5173 during this run. This was ordinary browsing/input smoke, not native hostile-CSP acceptance.

## Observed negative and positive results

- Gateway status with matching Host+Origin: HTTP 200. Wrong Host and wrong Origin: HTTP 403 `invalid_request_authority`; earlier `BATCH-FINAL.md` evidence also covers duplicate Host and Origin (403). Correct-origin status remained 200 after the negative authority requests.
- Browser action opened a run-owned local HTML page. Authorized view admission returned HTTP 200 and a first-frame descriptor (720×480, sequence 1, JPEG length 5,935 bytes) with view/epoch/document/viewport identity. The authorized frame WebSocket delivered a binary packet of 6,031 bytes (96-byte header + 5,935-byte JPEG); the positive remote page title and metadata were readable by the owner.
- Frame-lane negatives on the retained view: no first message timed out and the server closed without a WebSocket close frame (client observed 1006) after the bounded pre-auth wait; binary bytes before the grant closed 1008 `frame grant required`; a grant naming another view closed 1008 `invalid browser frame grant`; altered credential, altered expiry (`expires_at=0`), and altered epoch each closed 1008 `Browser frame grant no longer belongs to this stream`. No frame payload arrived on these rejected attempts. The altered-expiry probe is **not** evidence of waiting for a legitimately issued grant to expire.
- Replaying the valid frame grant was attempted after a successful stream, but the probe terminated its first stream without returning frame credit; the replay connection received no frame before client timeout. This is inconclusive, not a replay-refusal pass.
- Wrong target association: wrong session returned HTTP 400 `session_not_selected`; wrong and traversal-shaped socket paths returned HTTP 400 `stale_endpoint`. Wrong view command returned 400 `browser_view_not_found`; wrong epoch returned HTTP 200 with `status=stale`, `code=stale_stream`. A mutation-shaped navigation command without adequate fresh presented-frame proof returned `stale_location`; this does not prove observer-mutation refusal or lease enforcement. Takeover and stale input-sequence requests in this same probe also failed the stale-location precondition, so lease/input-sequence cases remain unproven.
- Page-to-gateway probe: owned page JavaScript attempted `fetch(http://127.0.0.1:38643/api/v1/status)` and a WebSocket to the events route. The page title reported `fetch-blocked` and `ws:error`; page remained normally browsable and first-frame/metadata admission succeeded through the authorized Cockpit view. This demonstrates the page script did not obtain those attempted responses, but does not identify the precise CORS/Host/Origin rejection for each request, test privileged control POST, or establish CSP/native capability parity.
- Credential placement spot-check: the grant was negotiated in the first WebSocket message, not a URL parameter. A scan of 282 other run-owned text/profile/log files found no occurrence of the captured grant value; no grant appeared in gateway/helper/Herdr logs. The temporary open-response probe file did contain the grant and was excluded from the scan; it was outside the durable record and was removed with the run root. This did not inspect browser DOM or saved image pixels for credential exposure.
- State/profile checks while the private browser was running: run root 0700; `cockpit-state/browser`, profiles, workspaces, configs, associations, helper directories 0700; owner socket and lock 0600; Herdr API/client sockets 0600; browser profile directory 0700 and profile data primarily 0600. The outer `cockpit-state` parent was 0755, but the browser state subtree is 0700. Config/association JSON and Chromium `DevToolsActivePort` were 0644 inside that private 0700 subtree. A second startup using an owned `browser` symlink aimed at an outside sentinel failed before serving with `unsafe_path: browser root is not a real directory`; the symlink and sentinel contents were unchanged. A traversal-shaped endpoint path was rejected as stale.
- Native positive smoke: actual private Linux Tauri/WebKit surface navigated the run-owned fixture, produced live canvas paints (363 `drawImage` calls / 6.0125 s = 60.37 FPS), routed a pointer, scrolled to authoritative `scrollY=3900`, and painted the final green marker `(0,167,75)`. Bitmap was 674×188 CSS/image at DPR 2; density was observed, not treated as a failure or security result.

## Source-review context and confidence

The earlier read-only review recorded in `OBS-020-BROWSER-GEOMETRY.md` identified open-before-subscribe abort, backpressured send, and unauthenticated first-connection lifetime holes. That record classifies these as source-review findings; integration subsequently added opened-view release, bounded/cancellable writes, and a five-second authentication deadline. In this run the missing-grant connection closed after the deadline and malformed pre-auth bytes were refused, but open/subscribe races and backpressure were not reproduced. Confidence: source evidence supports the historical candidate and the bounded runtime evidence supports only the two exercised refusal paths; neither is a broad security proof.

## Acceptance disposition (1–13)

1. **Partial:** Host/Origin, pre-auth bytes, missing/forged/altered grants, wrong session/socket/view/epoch, genuine 30-second expiry, and replay of a consumed grant were exercised. Replay and oversized authenticated credit closed without a reason-coded close (1006); no frame arrived and separate positive viewers remained usable. Owner-process replacement/incarnation binding and all requested secret-leak surfaces remain unverified.
2. **Partial:** Observer mutation was rejected with `browser_control_required`; old lease was stale and input sequence 0 was rejected; held-input release on lost client passed. Endpoint replacement, stale target/document/frame-envelope mutation and uncertain-mutation recovery remain unverified.
3. **Partial:** Pre-auth bytes, invalid frame credit, oversized credit/control inputs, 60 control-size rejections, and slow-observer isolation were exercised. Malformed/oversized JPEG producer frames/envelopes, dimensions/area validation, metadata floods, hostile rendering strings, and broader simultaneous-view isolation remain open.
4. **Partial:** loopback gateway, Host/Origin and page fetch/WebSocket attempts plus positive browsing exercised. Native CSP/capability alignment, remote-client network denial from a second host, privileged control-route page requests and secret reads were not exercised.
5. **Unverified:** chooser/download/permission blockers, arbitrary file reads, auto-grant, navigation liveness and configured destination containment not exercised.
6. **Partial:** modes and symlink/traversal refusal were checked for the owned browser state root and endpoints; file/feedback writes, helper replacement races, and cleanup ownership across hostile interruption were not tested.
7. **Unverified:** no A25 clean-cutover, extension/pairing/Show-dependency, or saved-feedback readability check in this run.
8. **Partial:** positive controller/browse/frame controls followed observer, old-lease, stale-sequence, lost-client, expiry, replay and slow-peer probes, but not every individual negative has its own separate positive control; strict per-case matrix remains open.
9. **Partial:** first-message ticket placement and a 282-file log/profile scan were checked; genuine grant expiry/replay were attempted, but replay closed 1006 without a reason and DOM/image/error-message exposure remains unverified.
10. **Bounded pass:** with an authorized frame lane active, controller accepted held key/button inputs; closing its event/frame streams released both remotely (`k2/2-p1/1`) without frontend cleanup and invalidated the controller lease.
11. **Partial:** 65,537-byte text controls were rejected before forwarding in 60 requests; hostile title/locator/dialog strings and malformed/oversized producer-frame bytes before rendering/logging remain untested.
12. **Partial:** page fetch and WebSocket attempts failed while ordinary local browsing worked; no direct unauthorized privileged-control mutation attempt or full CSP enforcement test.
13. No defect was confirmed by this bounded run; therefore no source repair was made. This does not substitute for unrun cases.

## Cleanup

Native harness reported clean exits. Independently verified its WebDriver PID 3657807, compositor 3657756, gateway 3657734 and Herdr 3657697 absent; private Herdr socket absent; ports 5173 and fixture 34543 refused connections. Main was notified that port 5173 was released.

Gateway browser was closed through its owned action; owned Chromium/CLI descendants were absent afterward. Gateway PID 3645496 and fixture PID 3646793 were stopped as their own session-leader process groups; owned Herdr PID 3644677 was stopped separately. Verified all recorded gateway/browser PIDs absent, Herdr API/client and browser owner sockets absent, and gateway/fixture ports 38643/38644 refused connections. Only after these checks, the exact run root and disposable probe script were removed; no shared process/profile was signaled.

## Additional gateway matrix — controller, grant, flood, slow peer (2026-09-24)

Main requested a second private gateway run with current-frame proofs. It used session `web08-r3-fe272ce282`, Space `w1`, root `/tmp/web08-r3-620q0soh`, gateway `127.0.0.1:43201`, fixture `127.0.0.1:41519`, and owned Chromium PID 3759565. The browser was opened without a URL first, then a separate current inline view/control navigation was used; the run-owned fixture included a harmless local animated page and key/pointer event counters.

- **Observer authorization with current identity:** controller A admitted a view and received binary frame 12,593 bytes; `take_control` was accepted at lease generation 2. Observer B also received its own authorized first frame. B's navigation command used the current target/document and lease proof but was `rejected`, code `browser_control_required`; controller A's subsequent navigation was `accepted`. No observer navigation occurred.
- **Lease takeover and stale sequence:** A released at generation 3; B acquired control at generation 4. A's old-lease navigation was `stale`, code `stale_location`, with no page change; B's current navigation was accepted. B's `input_sequence=0` with current target/document/lease was rejected as `stale_input_sequence`; current key-down and key-up were both accepted.
- **Lost client with held input and live frame lane:** while B's authenticated frame stream was active, pointer-down and Shift key-down were accepted. Closing both B event and frame sockets retired the client. The still-open page's title/event counters became exactly `k2/2-p1/1` (held key and pointer each had a matching release); A's observer snapshot showed no controller and lease generation 5. A then took control successfully at generation 6. This is concrete process-independent cleanup on stream loss, with no frontend release command.
- **Grant expiry:** an unconsumed issued grant had ~29,993 ms lifetime. After waiting 31,193 ms, its view had retired/closed; connecting with that genuine expired grant closed 1008, `Browser view is not attached to this owner`, with no frame. A newly opened authorized view immediately afterward received a 11,235-byte frame.
- **Grant replay after successful use/credit:** resubmitting a consumed grant on another socket closed without a frame (client observed close 1006, no close reason). Existing controller stream remained usable; a later fresh authorized view received a 3,819-byte frame. The close is bounded/no-data evidence but has no protocol close reason, so do not characterize it as a reason-coded 1008 refusal.
- **Invalid credit and oversized controls:** after a fresh view received a valid binary frame and returned its normal frame credit, an ACK for an unissued sequence closed 1008 `invalid frame credit`. Sixty 65,537-byte text-control requests were each refused HTTP 400 `invalid_browser_view_command` before helper forwarding. An authenticated frame connection receiving an oversized credit message closed abruptly (1006, no reason); a subsequent fresh view/control navigation/frame succeeded. This exercises the transport bound, not malformed producer JPEG/envelope validation.
- **Slow uncredited observer:** on the run-owned animated page, a slow observer received exactly one frame and returned no credit. The current controller's independently credited stream advanced from 127 to 259 frames in 2.2 seconds (132 new frames); controller input remained accepted, and a new authorized view received frame sequence 521 afterward. This is an actual isolated slow-view positive, not only source evidence.

These probes close the earlier missing current-frame/control precondition and add true 30-second expiry, consumed-grant replay, invalid-credit, oversized-client-control, flood, slow-observer and lost-held-input evidence. Frame source corruption/oversized JPEG, metadata flood, page-script privileged POST/CSP, endpoint replacement, and per-negative individual positive controls remain open. These remain bounded checks; no product defect was reproduced and no source change was made.

### Additional-run cleanup

Closed Chromium through the owned gateway action before stopping services. Recorded gateway 3758717, fixture 3758707, Herdr 3758673, Chromium 3759565 and helper 3781597 were absent after shutdown; all private Unix sockets were absent and ports 43201/41519 refused connections. Removed only `/tmp/web08-r3-620q0soh` and its disposable probe/setup files. No native service was started for this additional run, and no shared resource was signaled.