# WEB-05 resumed browser matrix — 2026-09-24

Scope: private managed Chromium + private Cockpit browser gateway only. No product, shared campaign, native, ledger, or commit changes. This is a browser-surface supplement, not a WEB-05 completion claim.

## Run ownership and cleanup

- Private fixture A: root `/tmp/cpol-6ehqpwl4`; Herdr `polish-6ehqpwl4`, Space `w1`, socket `/tmp/cpol-6ehqpwl4/config/herdr/sessions/polish-6ehqpwl4/herdr.sock`; fixture `http://127.0.0.1:44439/`; gateway `http://127.0.0.1:39189/`.
- Private fixture B: root `/tmp/cpol-gmiy4z10`; Herdr `polish-gmiy4z10`, Space `w1`, socket `/tmp/cpol-gmiy4z10/config/herdr/sessions/polish-gmiy4z10/herdr.sock`; fixture `http://127.0.0.1:39033/`; gateway `http://127.0.0.1:35183/`.
- Managed Chromium tab `BrowserAnnotationMatrix`, 1568×1009 CSS, device scale factor 1.25. It was opened against the actual gateway Cockpit UI, not a standalone fixture page. Private test page `matrix.html` added under each owned fixture's `www/` directory; first includes amber pixels, transformed element, nested clipped content, same-origin `srcdoc` iframe, open shadow content, and canvas pixels.
- Cleanup: released only `BrowserAnnotationMatrix`; invoked the fixture helper's scoped stop for both roots. Verified gateway/fixture/Herdr recorded PIDs absent (`3689724/3689683/3689692`, `3721535/3721493/3721502`), both Herdr sockets absent, and ports 39189, 44439, 35183, 39033 no longer accepting connections. Another managed tab belonging to another run remained untouched.

## New actual browser observation

In fixture A, the actual Browser UI painted `/matrix.html`. Selecting Element and clicking inside the same-origin iframe produced the inline, rendered limitation **“Element inspection cannot cross into a nested browsing context”**; the screenshot shows no false annotation rectangle and `Notes 0`. The gateway's read-only `POST /api/v1/browser/drafts/recovery` list returned HTTP 200, `type=draft_inventory`, three draft IDs (`2585afdc-9d6e-4e3b-b6ee-cd0078855b67`, `5a0bac29-2d7d-402f-aae1-e2c4a6758523`, `83a659f1-0f5f-4c11-b62e-78e00fc1cc3e`), each revision 1 with `annotations=[]`; `pending_capture=null`. I do not infer which empty inventory entry owns the matrix target from the list alone.

- Screenshot: `WEB-05-resume-browser-iframe-limitation.png`, 299,759 bytes, SHA-256 `50768bf5cc8f75b7162c9504e23475fff84b20e6443e4bef48d7a1776323f1a0`.
- The stale limitation remained visible during the subsequent browse/scroll attempt. That action did not produce a new valid Element hover, so this run does **not** claim stale-notice clearing (the earlier specific valid-hover repair evidence remains the one to reuse).
- Fixture B's gateway returned `browser_cli_failed` for a malformed URL attempt; a subsequent valid open returned `connection=outcome_unknown` and explicitly said it had not retried the previous URL action. The managed UI then showed the Browser-ready surface and later **“Input rejected: Another browser view holds the input lease”**. I did not retry that uncertain action or claim a successful navigation/capture. This is not an annotation save or capture outcome and does not satisfy A18's exact-ID/PNG restart case.
### Isolated browser-control lease diagnosis and safe recovery

- A separate managed tab, `BrowserLeaseDiagnostics`, used private root `/tmp/cpol-332313bp`, Herdr `polish-332313bp`, Space `w1`, socket `/tmp/cpol-332313bp/config/herdr/sessions/polish-332313bp/herdr.sock`, gateway `http://127.0.0.1:36453/`, fixture `http://127.0.0.1:43503/`, 1568×1009 outer CSS / DPR1.25. The actual `POST /api/v1/browser/action` returned HTTP 200, `connection:"open"`, `message:"browser opened"`, `opened_tab:"0"`, association `a5457426c9db028c448f4b5d`, owner `66139425-b4ea-41f6-8c4b-8eac912286ea`, browser incarnation `pid=3755782:start=34915225:receipt=e7275c2af3e50e8cf3ff3c240dbf311f9ed76b42af3b7b8f334332ff68a04d65`.
- Actual UI `POST /api/v1/browser/view/open` returned HTTP 200. Its snapshot identified view `ac60c4a8-1bbe-4ba1-a27a-7c8a014896fb`, stream epoch 1, sole target `4C2A146080CF07C7E61023FDC4D323ED` (`http://127.0.0.1:43503/`, title “Polish annotation fixture”), document/frame generation `1790241230106`, viewport revision 1 (562×823 CSS, scroll 0, DPR1.25), first frame sequence 1 at 562×823, and control `{status:"observing", controller_view_id:null, lease_generation:1, next_input_sequence:1, can_take_control:true}`. The visible tab list contained only this fixture tab. The read-only `GET /api/v1/browser/view/events/ac60c4a8-1bbe-4ba1-a27a-7c8a014896fb` WebSocket `attached` snapshot agreed: no controller view and one page target. No second Browser view/client was opened in this root before the failure.
- Clicking the actual UI `New browser tab` (+) produced a definite HTTP-200 command response, not an uncertain dispatch: request ID `browser-command-58dd1c23-8364-4019-a7f7-0ea49e66e39e`, view/epoch as above, `status:"rejected"`, `code:"browser_control_required"`, `message:"Another browser view holds the input lease"`. The tab list remained one tab; screenshot `WEB-05-resume-browser-lease-rejected.png` (82,345 bytes, SHA-256 `d4c56960647dce840805112bfe8891f46291c00a4b0baf4bb32dd1e1854c878a`) visibly shows the rejection while the original fixture is still rendered. The post-error event-stream `attached` snapshot still had `controller_view_id:null`, lease generation 1 and `can_take_control:true`; therefore the visible “another view” message did **not** establish that a second owner actually existed.
- Main identified the source cause as `BrowserPane.tabCommand` issuing a tab operation without `ensureControl`, while the helper requires a controller. This is an actual tab-switch/new-tab path rejection, not the earlier `browser_cli_failed`/`outcome_unknown` startup result. The gateway continued using the pre-rebuild `dist`; no source/build was changed or tested by this worker.
- For a non-destructive recovery check, an ordinary click on inert fixture heading text triggered the UI's existing automatic take-control action. No explicit “Take control” button was present in the actual accessibility tree. The authoritative command accepted controller view `ac60c4a8-1bbe-4ba1-a27a-7c8a014896fb`, status `controlled`, lease generation 2, `next_input_sequence:1`, `can_take_control:false`. No other tab/client was present in the private session.
- With that acquired lease, one real Page URL submission navigated to the owned `/lease-positive.html`; no pending command or automatic replay was involved. The UI painted the page text (“Positive navigation after browser input lease acquisition”) and the address bar showed the exact fixture URL. Command request `browser-command-b944cd84-9bb5-4ab9-ac09-ba964c2b7292` returned HTTP 200 / `accepted`; authoritative WebSocket attached snapshot showed that URL/title, document/frame generation `1790241457660`, same target ID, viewport revision 1 at 562×823 CSS/DPR1.25, and control still owned by the same view at lease generation 2. Screenshot `WEB-05-resume-browser-lease-nav-positive.png` (122,777 bytes, SHA-256 `a5a60935dd57a39ad82a90e029714d07ee2de1088eb4845474503ce19f37bdd0`).
- An additional fresh-gateway isolation reproduction used root `/tmp/cpol-dzi8uot7`, Herdr `polish-dzi8uot7`, gateway `127.0.0.1:41977`, fixture `127.0.0.1:46399`, and separate managed tab `BrowserAnnotationMatrixClean`: opening the fixture returned `connection:"open"`/`opened_tab:"1"`; selecting its UI tab painted the same lease rejection. This root and the detailed diagnostic root were stopped: PIDs 3745210/3745160/3745176 and 3753558/3753503/3753526 are absent, sockets absent, ports 41977/46399 and 36453/43503 closed. Only these owned tabs were closed; sibling managed tabs remained untouched.

The empty-draft inventory and the earlier iframe screenshot are separate evidence; the lease traces do not change their results.

## Rebuilt Chromium new-tab lease regression — 2026-09-24

- Fresh isolated root `/tmp/cpol-xq4xs_ar`, Herdr `polish-xq4xs_ar`, Space `w1`, fixture `http://127.0.0.1:38153/`, gateway `http://127.0.0.1:34777/`, socket `/tmp/cpol-xq4xs_ar/config/herdr/sessions/polish-xq4xs_ar/herdr.sock`. Before any browser action, `/proc/3895316/environ` confirmed owned Herdr PID 3895316 ran with `HOME=/tmp/cpol-xq4xs_ar`, `XDG_CONFIG_HOME=/tmp/cpol-xq4xs_ar/config`, `XDG_STATE_HOME=/tmp/cpol-xq4xs_ar/state`; all matched the fixture root. Managed Chromium tab `BrowserLeaseRegressionGreen` used 1568×1009 CSS / DPR1.25. Gateway served rebuilt frontend asset `assets/index-CwwUE_r2.js` (HTTP 200).
- Actual UI Open browser established view `557f23f5-192d-43e0-957c-fc1e423ba1f1`, epoch 1, initial target `EF5F930100C4CB9D6FD3B6854B7AC135` (Cockpit browser-ready page), document generation `1790242778910`, frame sequence 1, viewport 562×823 CSS/DPR1.25, control `observing`, `controller_view_id=null`, lease generation 1, `can_take_control=true`.
- The first browser-page action was the actual UI **New browser tab** (+), with no preceding page click, pointer takeover, address entry, or other user interaction. In gateway order, request `browser-command-3deeaceb-c59a-4cfa-a49b-42dea0fc0855` was accepted as `take_control`, returning the same controller view, `controlled`, lease generation 2, `can_take_control=false`; next request `browser-command-f7958c4b-2082-48ba-856d-3de85e3a3829` was accepted with a snapshot containing new target `6FD76401F2F7AC05950A5478FC6A262D`, selected `about:blank`, document generation `1790242795478`, viewport revision 2 at 562×823/DPR1.25, control still held by that view at lease generation 3. The actual UI selected the second tab, painted a blank page, and displayed no lease/error notice. Screenshot `WEB-05-resume-browser-new-tab-green.png` (24,146 bytes, SHA-256 `70c1f23b421762471bdfe8d98f1e7c0f2ad26fed2aa4009621c97dbffdbc9028`).
- After proving the first-action path, the selected tab loaded the run-owned fixture. A first address fill appended to the existing `about:blank` field and visibly produced `about:blank#blocked`; after observing that state, an exact CSS address-field fill followed by one Enter loaded the intended local fixture URL (no replay of an uncertain command). The rendered title/content became “Polish annotation fixture”; the attached WebSocket snapshot confirmed selected target `6FD76401F2F7AC05950A5478FC6A262D`, URL `http://127.0.0.1:38153/`, document/frame generation `1790242859176`, viewport revision 2 (562×823 CSS, DPR1.25), control still `controlled` by view `557f23f5-192d-43e0-957c-fc1e423ba1f1` at lease generation 3, no blocker. Screenshot `WEB-05-resume-browser-new-tab-fixture-green.png` (80,284 bytes, SHA-256 `d99a3fe164cadbaea2e7f411000508c78d06e404f1c3cf5aa1fd8c9d6506cb4a`).
- Cleanup closed only `BrowserLeaseRegressionGreen`, stopped the scoped fixture, and verified gateway/fixture/Herdr PIDs `3895365/3895290/3895316` plus run-owned Playwright daemon and shell PIDs `3898561/3895360` absent; Herdr socket absent and ports 34777/38153 released. Other managed tabs remained untouched.

This fresh rebuilt-frontend run is green for the previously red first-action New-tab lease path; it does not itself close the remaining WEB-05 authoring/capture/concurrency matrix.

## Criterion reconciliation

Status uses earlier evidence in the resumed `WEB-05.md` when stated; no earlier result is repeated as a new run result.

| Criterion | Browser evidence/status | Remaining browser gap |
|---|---|---|
| A14 freehand/region, colors, edit/remove, optional note, hide/reopen | **PARTIAL; reuse**: `WEB-05.md` lines 110–123 records actual Chromium freehand with 11 simplified points, red Region and selected removal, note persistence, 8,200-point refusal, and existing-tool clear behavior; lines 128–133 records actual focused paste/receipt. This run added no successful authoring sequence. | Complete positive/negative reverse-direction gesture matrix and hide/reopen on this surface; color/edit/remove/note in missing combinations remain open. |
| A15 ordinary DOM, iframe, shadow, transformed/nested/canvas | **PARTIAL; reuse + new iframe limit**: prior lines 116–118 cover ordinary paragraph, open-shadow host and same-origin iframe refusal; lines 135–140 cover transformed and nested-scroll refusal plus actual canvas refusal. This run independently observed/rendered same-origin iframe refusal with no mark (screenshot/hash above). | Cross-origin and other drift cases are not established by this run; closed shadow is explicitly not established in prior evidence. |
| A16 stale hover/pick and document drift | **PARTIAL; reuse**: prior line 118 records valid normal hover clearing the obsolete iframe limitation; lines 135–140 record nested bounds defect and repair. | Stale asynchronous hover/pick replies, page-document drift during pick, and full navigation boundary matrix remain open. |
| A17 multiple viewport/zoom/resize pinned PNG | **PARTIAL; reuse**: prior lines 119 and 135–141 retain one actual browser capture’s PNG hash, frame/target/document/viewport provenance and page/mark/comment/chrome inspection. | No new multiple-DPR/zoom/resize PNG was made. DPR2 managed-Chromium attempts remain explicitly unverified in prior evidence; page-pixel/mark/comment/chrome checks at each size remain open. |
| A18 failed composition, full store/disk, lost save response, concurrency, navigation, restart | **PARTIAL; reuse**: prior lines 140–141 record private write-denial, visible retry and durable revision recovery; lines 143–149 record receipt reconciliation and rejected-capture refresh. | No ENOSPC/full-store, lost capture-save response followed by restart, exact original IDs+PNG hash across restart, or full navigation/capture concurrency matrix was exercised here. Fixture B's unrelated browser-open unknown outcome is not a pass. |
| A19 repeat lookup, old exact ack, newer pending, closed-browser lookup | **PASS for documented subset; reuse**: prior line 119 explicitly records two non-consuming reads, exact ack of the older five IDs, newer ID remaining pending, identical second ack acknowledging none, and closed-browser feedback/image lookup/hash. | No new A19 scenario was needed; broader navigation/read matrix is not implied. |
| A20/A21 no-agent/wrong-Space/accepted/refused/unknown/duplicate risk | **PARTIAL; reuse**: prior lines 119, 128–133 and resumed lines 146–148 record no-agent rejection/retention, accepted same-Space PTY paste with no Enter, one frame, redacted payload, durable exact receipt and UI rendering. | Wrong-Space, unknown paste response, and explicit duplicate-risk resolution remain open; do not auto-paste on uncertainty. |
| A24 multi-client CAS/64-mark cap/tombstone | **PARTIAL; reuse**: prior resumed line 145 records receipt digest reconciliation preserving independent newer edits; lines 120–123 record revision-safe discard and bounded draft write preservation. | No browser multi-client CAS race, 64-mark capacity boundary, explicit capacity refusal/no eviction, or tombstone-resurrection race was run in this supplement. |

## Outcome

This supplement adds actual Chromium A15 same-origin iframe refusal evidence and the red/green tab-command lease regression. The `browser_control_required` rejection was promptly reported to Main; Main fixed `BrowserPane.tabCommand` to acquire control before sending tab commands and rebuilt the frontend. A fresh private Chromium run against the rebuilt `index-CwwUE_r2.js` proved the first New-tab action took control then selected a real new target and painted a page without rejection; view/lease/frame/viewport evidence and screenshots are above. The original required WEB-05 A14–A21/A24 matrix remains incomplete; rows marked partial/open above remain acceptance work. No product source, shared campaign file, ledger, or commit was changed by this worker.