# WEB-06 — sustained native frame/resource increment

Accepted plan, 2026-09-23. Baseline `58f2158834515576d4d84a0c71aa139e4b607417`. WEB-06 is blocked and its A01/A03/A13/A22/A25 lifecycle, slow-observer, crash/reconnect, and five-minute plateau requirements remain attached; this increment measures only a sustained animated native surface without pretending the task is complete. RUN-01 froze a 30-second warm-up, one CPU/PSS sample per second, at least five minutes of active sampling, process identity and a reported memory slope. Earlier OBS-022 proved six-second painted FPS but not retained-resource stability.

Own `scripts/verify/native_browser_acceptance.py`, this evidence, and only run-owned screenshots/artifacts. Reuse its isolated Herdr session, Space, fixture server, gateway, frontend, private Niri/WebKitWebDriver and retained first-frame proof. Add an explicit sustained mode to the existing page animation and sample the run-owned process trees once a second. Start 330 seconds of movement; exclude the first 30 seconds as warm-up, then report 300 PSS/CPU samples, live process-count range and least-squares PSS slope with pid/process scope. Count actual native canvas paints throughout, final authoritative page position and final painted green marker. Positive: no blank/recovery frame, >15 actual painted FPS (30–60 preferred), plateau or explicitly investigate monotonic growth. Negative: static page after finishing should not keep accumulating paints/requests; teardown must stop only owned resources. This mode does not prove malformed frames, slow observer, crash, hide/show, stream socket/decoder counts, or browser-gateway/resource parity. No product source edit unless a confirmed defect has a reproducible negative and a passing positive control.

Native release resources must use the harness's existing `finally` path even on timeout. Do not touch an occupied Tauri frontend port, default Herdr session, installed app, unrelated profile or user fixture. Preserve compact sample summary in committed evidence, raw numeric samples in the run root with hash, exact build/runtime versions and all cleanup states. A final task status remains blocked until WEB-04 and remaining original WEB-06 criteria pass.

## First sustained native run — pass for paint, not memory plateau

On 2026-09-23, `python3 scripts/verify/native_browser_acceptance.py --skip-build --sustain-seconds 335` used unique session `native-c142a6e32b19` and isolated Niri/WebKit/gateway. Requested outer 1392×835 at DPR 2; the compositor actually provided logical width 708 and the full-column WebKit content box was 676×835 at DPR 2. The browser canvas filled its 674×188.47 CSS pane without an internal Cockpit splitter; its first frame bitmap was 1348×376 physical pixels. `WEB-06-native-first.png` SHA-256 `197e457cc3f7190c79a7780394266f5b6e9187897153c97a79280dc620a111fc` is the observed pre-gesture page. This does **not** establish a 1392-wide physical window.

The fixture oscillated page scroll for 335.0039 seconds; 19,383 `CanvasRenderingContext2D.drawImage` calls were observed on the displayed native canvas, or 57.86 actual painted FPS. WebDriver performed 48 commands including polling; none were counted as paints. The authoritative page finished at `scrollY=3900`; the native canvas painted its final marker RGB `(0,167,75)` and `WEB-06-native-final.png` SHA-256 `6ac3d16bda1d348a65395cd54501b7051551286e5e11ccb7b24955bf4e9b5efa` visually shows `scroll complete`. After a two-second settle, the static canvas painted zero frames during three seconds. All run-owned frontend, WebDriver, compositor, gateway, Herdr, and fixture services stopped; no user session or active desktop was mutated.

One `/proc` PSS/CPU sample per second used host `time.monotonic` for 336 samples; 306 fell in the 30–335-second post-warm-up interval. All nine owned processes stayed present. Aggregate PSS was 465,439→542,301 KiB, min 465,439, max 550,077, median 505,650.5, p95 539,279; least-squares slope **+252.81 KiB/s**, not a plateau. The WebDriver/native/WebKit child tree accounted for 354,593→430,919 KiB and +251.01 KiB/s; runner, Herdr, gateway, and compositor trees were individually near-flat. Aggregate CPU consumption from tick deltas was median 84% and p95 93% of one core. `WEB-06-native-samples-first.json` contains all raw numeric samples (158,070 bytes; SHA-256 `c8b3cff5419540ac4f686ea6a0b4ca1635acb268ae572c4e666d7c174f9ad49e`). Each minute through 330 seconds kept growing; the final 270–330-second segment still rose +261.16 KiB/s. A second isolated five-minute run is in progress to identify the exact PID/start identity; no leak repair or plateau is claimed yet.

Build identity: gateway SHA-256 `228df0da0ff99d60208ef8d9c418e0325a1e3f38dc3a2b8d3840c596c2a976c6`, Tauri SHA-256 `ff851f2474d236a0560c7608b415c218f798bb30da2e6b7d3bf61962a9319263`, embedded helper source SHA-256 `f1117a7add8aeef674393d16d7da0d56c6511239ec6300ff592f9d5b396a791d`; Herdr 0.9.1, Niri 26.04, WebKitGTK owning `/usr/bin/WebKitWebDriver` 2.52.5-1.fc44, Playwright CLI 0.1.19 paired with `playwright-core` 1.63.0-alpha-2026-08-31, Bun 1.3.14. Capture timestamps are browser/WebKit `performance.now`; animation duration came from fixture reports received on the host's monotonic clock, and process samples used the host monotonic clock. A prior helper patch rejected all low-DPR screencast pixels but left 0 painted frames; a second produced ~57 FPS while repeatedly painting the page top. The final helper explicitly clips its high-density screenshot to measured document-space scroll and brackets geometry, which passed the final marker; no assertion or FPS threshold was relaxed.

## Per-process attribution run — memory plateau remains open

A second unchanged-product native run (`native-6767304d9028`, fixture `?sustained=335`) again passed DPR2 first bitmap, 19,383 paints / 335.0043 seconds = 57.86 FPS, final `scrollY=3900` with painted green marker, zero static idle paints, and all owned process/port cleanup. It recorded 339 one-second samples; 309 post-warm-up samples span 30–338 seconds. `WEB-06-native-samples-per-pid.json` holds each sampled PID, process `comm`, Linux `/proc/<pid>/stat` start tick, PSS, and CPU tick (485,783 bytes; SHA-256 `91752b67cf6216391dc10eefa32d889bb07d94aed053d092bd82c3ce773af822`). No command lines, credentials, or profile bytes are logged.

The same nine sampled processes remained live throughout. Aggregate PSS was 461,491→535,773 KiB, median 499,485, p95 534,010.6, slope **+241.52 KiB/s**; second-run CPU median 46% and p95 50% of one core. By stable `(PID,start_ticks)`, the WebKit web-content process `166549/26248290` grew 191,934→254,592 KiB (+200.32 KiB/s); its final 60-second slope remained +193.01 KiB/s. The WebKit network process `166528/26248283` grew 23,538→33,416 KiB (+35.93 KiB/s; final minute +13.22). Native Tauri `166480/26248271` remained 129,864→130,032 KiB, gateway `166102/26248050` 26,350→26,382 KiB; Herdr, compositor, runner, and WebDriver controller did not account for the large increase. The second run corroborates non-plateauing WebKit memory rather than proving an application leak or a WebKit cache cause. The sampler covers its explicitly owned process trees; a detached external Chromium/helper would not be included in these nine-process totals.

No speculative WebKit/Blob/GC mitigation was shipped: `FramePresenter` currently closes `ImageBitmap` and revokes fallback object URLs in `finally`, and the remaining cause is not isolated by these PSS samples. WEB-06's five-minute **paint** and static-idle criteria passed, but its memory-plateau, decoded-object/queue ledger, slow observer, crash/reconnect, owner/observer lifecycle, and full browser/native matrix remain blocked. A later run must distinguish retained decode/transport objects from engine memory before claiming a resource fix.

## Accepted follow-up: distinguish active growth from idle retention

Baseline `9f758f448b081cdd78a25640f16f3395298c901d`. Keep the same private Linux-native Herdr/Niri/WebKitWebDriver fixture, current binaries, animated 335-second viewport, and sampled process identities. Add an optional 90-second post-animation idle observation to the existing harness; do not change the product capture path or relax the DPR, paint, final-pixel, or cleanup checks. Positive: sustained rendering still passes and each WebKit PID's post-warm-up active slope can be compared with its 90-second idle PSS change and final-minute idle slope. Negative: if PSS continues to grow or fails to recover, record the shortfall rather than calling cached memory a leak or a plateau. Stop only run-owned services, leave user resources untouched, and commit only owned harness/evidence/ledger files after one real native run.

## Active-to-idle control — static release is partial

The accepted follow-up ran `python3 scripts/verify/native_browser_acceptance.py --skip-build --sustain-seconds 335 --idle-resource-seconds 90` in disposable session `native-44ca42070740`, root `/tmp/cnative-perdh1z_`. The native viewport remained **676×835 CSS at DPR 2** (not the requested 1392-wide viewport), with a 1348×376 physical browser bitmap. It passed 19,392 native canvas paints / 335.0145 seconds = **57.88 FPS**, final authoritative `scrollY=3900`, painted RGB `(0,167,75)`, and **zero additional canvas paints over the full 90-second static observation**. The final screenshot `WEB-06-native-idle90-final.png` (PNG, 106,629 bytes; SHA-256 `bb039fe1fc96697f9485f0aa04d35dc354ce89e287d36d7fce38a5bf46200a2e`) was visually checked after idle: the green `scroll complete` marker remained displayed. All fixture/frontend/gateway/Herdr/compositor/WebDriver resources from this run were stopped; WebDriver's `-15` is intentional owned-process termination.

`WEB-06-native-samples-idle90.json` preserves 428 per-second samples and stable `(PID,start_ticks)` process identities for the same nine sampled processes (615,600 bytes; SHA-256 `7237a8f9dd6076f8d746f1b49078d25f77ba3a8a37933a272baa5f10f3556db7`). The 306 **active post-warm-up** samples (30–335 seconds) show total sampled PSS 466,795→547,631 KiB, least-squares slope **+259.37 KiB/s**. WebKit content `209852/26338168` grew 198,477→265,798 KiB at +215.83 KiB/s; WebKit networking `209831/26338161` grew 23,356→35,116 KiB at +37.84 KiB/s; native Tauri and gateway stayed near-flat. The final 60 active seconds still grew +250.07 KiB/s in WebKit content, not an active plateau. Sampled process-tree CPU was median 84%, p95 92.8% of one core while animating.

After a two-second settle, the 90 **idle** samples show WebKit content 261,734→255,030 KiB (−6,704 KiB); its **final 60 idle seconds** were nearly flat, 254,998→255,030 KiB and +1.18 KiB/s. WebKit networking stayed 33,196→33,196 KiB during idle (final-minute slope +0.01 KiB/s); total sampled PSS was 542,511→536,210 KiB and final-minute slope +6.38 KiB/s. Idle CPU was median 3%, p95 5% of one core. The content process nevertheless ended **56,553 KiB above its 30-second animated baseline**. Thus this experiment distinguishes ongoing growth under sustained animation from near-flat static idle and partial memory recovery; it neither proves a leak nor satisfies WEB-06's **active/reconnect memory plateau**, decoded-object/queue reconciliation, observer/crash, or full browser/native lifecycle criteria. No product mitigation was inferred from PSS alone.

## Accepted follow-up: hide/reopen after retained-memory workload

Baseline `bf165186a9a2fa4c9903e1955d3e0dfb9d3d637b`. Use the same owned native fixture and a full 335-second animation, then activate **Hide browser view** through the actual Commands UI. Sample the same stable WebKit process identities for 90 seconds with the view hidden, then activate **Show browser view** and require the same page/scroll marker to paint in the reopened view without opening a new browser or mutating the user's session. Distinguish process allocation released at hide from idle-only shrinkage measured above; no product change or memory-leak conclusion from PSS alone. Record exact visual, authoritative page, process/port cleanup and blocked WEB-06 criteria. Implement the narrow optional harness path without weakening its existing active, final-frame, or idle assertions; commit only after a real native positive and a failed/unsafe action remains visible rather than silently passing.

### Red native lifecycle reproduction

The first full run (`native-ff6a1973b7f8`, `/tmp/cnative-swmh8m1c`) passed 19,385 canvas paints / 335.0164 seconds = 57.86 FPS, the green final marker at `scrollY=3900`, then the real **Hide browser view** Commands action. The browser canvas painted zero frames during 90 seconds hidden. The UI showed only the terminal in `WEB-06-native-hidden-red.png` (19,807 bytes; SHA-256 `84a16de9de93e71e3655ca96749e16e9e3693b1e07b52dc75dc909cbb66cac21`). Active post-warm-up WebKit content PSS grew 201,048→256,213 KiB (+168.93 KiB/s), and hidden PSS fell 261,777→255,265 KiB (−6,512 KiB; final 60 seconds +0.13 KiB/s). The 90 hidden samples, 306 active samples, and nine stable process identities are preserved in `WEB-06-native-samples-hidden-red.json` (630,010 bytes; SHA-256 `1e68b1cd5e7a6f2183288fbac8f35472a9d94f424772a52fda56442a5ead798a`).

**Show browser view failed the positive check.** The reopened native view retained the exact fixture URL but showed `Loading browser view…` over a black canvas for the full 20-second wait. `WEB-06-native-reopen-failed.png` (51,302 bytes; SHA-256 `d390602ae44191882d9d099b980daa622714ddcb7164c3d14c1b02a9b190b722`) is the observed failure. All owned processes/ports stopped, and no active Herdr session was touched. Source diagnosis: last-view retirement clears the helper's `latestFrame` and stops the screencast; reattaching only starts the screencast, so a static scrolled page may emit no new frame. This is a hypothesis to repair and recheck, not an accepted lifecycle or memory-release result. The candidate repair must publish a fresh capture bound to the measured current scroll and epoch, without replaying the old image or creating a second owner browser.

Read-only advisor `StaticReattachAdvice` recommended extending the existing guarded `captureCurrentFrame` with measured-scroll clipping, then capturing after the serialized 0→1-view screencast start only while the new view and captured CDP binding remain current. Accepted: no new wire path or replay, and `captureStableDensityFrame` still rejects stale token/document/viewport geometry before publication. The alternative of directly manufacturing a frame for `requestDensityScreenshot` was rejected because it lacks capture-bound state. Advisor's remaining discriminant if native show stays blank: establish whether a fresh helper descriptor reached the authenticated socket; this run did not yet measure that boundary.

### Static hide/reopen and hidden-scroll negative control

The second full native run (`native-b996208b0b94`, `/tmp/cnative-liyk3wkq`) exercised the real Commands **Hide browser view** and **Show browser view** actions after 335.016 seconds of animated scrolling and 90 seconds hidden. It passed 19,386 actual canvas paints / 335.016 seconds = **57.87 FPS**, finished at the page's `scrollY=3900`, painted the green `(0,167,75)` header, and painted **zero frames** while hidden. Show retained the same exact URL and repainted four further frames with the green marker; `WEB-06-native-reopen-green.png` (192,055 bytes; SHA-256 `7363328800f3bd62baba52abd8f21727a084258e6bd241c7c3adb572bb4d0a8d`) visually confirms full-size page text rather than the first run's black canvas. All run-owned processes and ports stopped. This run used the guarded new-frame capture on a 0→1 helper view transition; it did **not** prove correct behavior if a page changes its scroll or viewport while no view is visible.

This green run is historical evidence from a helper candidate subsequently reverted after the same candidate failed on the current rebuilt tree. It is **not** acceptance of the delivered source.

`WEB-06-native-samples-hidden-green.json` preserves all 431 numeric samples (619,906 bytes; SHA-256 `09e7707d20bf79db8b2a3665357410a2881cddc6ba897abdc42c6eb932b88dd1`). The 306 active post-warm-up samples show total PSS 469,572→542,948 KiB, +255.29 KiB/s; WebKit content `298165/26517097` grew 200,888→263,738 KiB at +213.09 KiB/s and WebKit networking `298139/26517091` grew 25,254→33,996 KiB at +36.56 KiB/s. The 90 hidden samples show content 262,928→255,203 KiB (−7,725 KiB), network 33,407→33,404 KiB; the final 60 hidden seconds still show +1.89 KiB/s content. The page retained **54,315 KiB** above its 30-second active baseline, so hiding partially releases memory but does not establish an active plateau or reconcile decoded frame/queue objects.

A separate six-second native `--hidden-scroll-smoke` intentionally changes the fixture's page position from 3900 to 4100 **only after** the Commands Hide action has hidden the UI, then requires a newly painted blue marker, the original 674×188 CSS viewport at DPR 2, and a routed wheel on Show. An experimental candidate's red reproduction (`native-7edd3dcedbe2`, `/tmp/cnative-0hir650j`) preserved 356 paints / 6.0027 seconds = **59.31 FPS** before hiding, zero hidden paints, and the authoritative hidden fixture reported `scrollY=4100`, but its viewport changed to **1348×376 at DPR 1**. In that now-reverted candidate, Show refused to present mismatched pixels: `WEB-06-native-hidden-scroll-drift.png` (60,471 bytes; SHA-256 `afece647cc10cdf799f9219a8e7163edd61fcfad7b8689a8798fbc348bc95d8d`) shows an explicit bounded-correction viewport-drift error and black canvas rather than falsely calling a half-scale frame live. This is an unresolved positive acceptance failure; the delivered helper does **not** include that experimental guard, and WebKit ownership/override reset cannot be counted as fixed.

`WEB-06-native-duplicate-helper-trace.jsonl` (20 events, 4,588 bytes; SHA-256 `e671c3fa3d4967454a1536e0c030f64b1780b8904b639a619f512626edc2ef0b`) preserves disposable instrumentation from another red run: two helper PIDs attached the *same* fixture/request 674×188@2 within 0.4 seconds. One still had one view when Show attached a second view and the page had scrolled to 4100; the old 0→1-only refresh would miss that state. A **reverted** experimental helper repair remeasured on any new view, emitted viewport metadata before frame publication, and used clipped capture for the bounded viewport-correction path. A per-association Rust single-flight experiment serialized same-association opens but caused **0.82 and 1.15 native painted FPS** in independent six-second runs while the green final marker remained correct. It was rolled back before delivery: shipping a throughput regression or accepting duplicate CDP ownership as safe would both be invalid. The source of physical-density screencast under a single helper versus slow high-density screenshot fallback remains an owner-lifecycle/performance investigation. WEB-06 stays blocked for this red case, active-memory plateau, ledger/slow-observer, crash/reconnect, and the original browser/native matrix.

The experimental every-`attach_view` refresh was **reverted**, not delivered: on the same rebuilt native branch two independent 335-second attempts (`native-83b38184164e`, `native-c320e8a1ac54`) timed out before the first native canvas paint. A separate default six-second native run (`native-b499f92da93f`) reached “Live browser view” with a **978×1234 bitmap stretched into 674×1234 CSS pixels** despite requested DPR 2; the fixture viewport never matched the canvas. `WEB-06-native-initial-distortion.png` (79,885 bytes; SHA-256 `c345472b4cd0e9361020b66f920dae36d980eed754539a306acb853c9e464b68`) records the visibly doubled text. A further 335-second attempt after restoring the **narrow 0→1-view repair** (`native-33667ccc199c`, `/tmp/cnative-7b_520jh`) failed **before scrolling** at the same mismatched 978×1234 bitmap and 674×1234 CSS canvas. The narrow repair was reverted too. These red runs do not isolate the broader refresh as the cause: two CDP-attaching helper PIDs confound initial viewport ownership. No static hide/show repair is being delivered or counted as accepted on the current tree.

Finally, the exact `--hidden-scroll-smoke` script was run against rebuilt binaries from the **restored helper source** in isolated `native-81bf6ce7c54f` (`/tmp/cnative-my1gyuty`). It failed earlier: after the real Commands Open action, no browser surface or first canvas paint appeared within the 20-second readiness budget; `WEB-06-native-restored-no-canvas.png` (20,514 bytes; SHA-256 `de361c8ef62e85852e8d6e62a3f582419068c4f64cef268f8f5df55ebd62f28d`) preserves the terminal-only view. The harness therefore **did not reach** hidden-scroll assertions on this restored-source run. Both initial opening reliability and hidden-scroll reattach remain blocked; the six-second late failure above only verifies that the new probe can expose the hidden mismatch when initial attach succeeds.

## Accepted diagnostic follow-up: initial owner without gateway pre-open

The restored-source native harness currently opens the fixture through the gateway process *before* Tauri launches, then invokes native **Open browser for Space**. The duplicate-helper trace shows two CDP owners but does not prove whether pre-opening caused the unreliable first paint or low-density frame. Add an optional **native-only-open** diagnostic to that same private Niri/WebKit/Herdr harness: leave the gateway serving but do not issue its browser-open request; invoke only the actual native Commands action, enter the fixture URL through the actual Page URL field, then enforce the same fixture, DPR2 first-bitmap, native frame, scroll, and cleanup checks. Do not modify product capture/ownership paths or weaken existing default checks. Record native canvas/fixture metrics, gateway/native helper process evidence, error screenshot and owned logs on failure. One green run does not establish repeatability, but a red single-owner run narrows the cause. Keep WEB-06 blocked for both initial ownership/geometry and its wider sustained/observer/reconnect/resource acceptance.

### Native-only open still fails geometry and first-paint readiness

The optional `--native-only-open` branch skipped the gateway's browser-open request and invoked only native **Open browser for Space** in new, private Herdr/Niri/WebKit fixtures. The real native **Page URL** field accepted an explicitly selected URL and the form submitted it; this WebKitWebDriver build rejected both `/element/value` and `/actions`, so the diagnostic used `document.execCommand('insertText')` on the focused/selected real input, then verified the controlled value after another event-loop turn and submitted its actual form. Its callback recorded the exact fixture URL and `defaultPrevented: true`; the native tab showed that URL and the fixture POSTed reports. Those initial WebDriver API/input failures were harness limitations, not browser navigation failures. Binary identities remained gateway `86d909c7b364a5636bbf4874906e6a79d35633bfbaab71f80dd6d5eb82f3fe2f`, Tauri `583900cc080b1f04ec6147dd6bef576d11ddc4fdd265e111188be12a11a8bd9d`, and embedded helper source `f1117a7add8aeef674393d16d7da0d56c6511239ec6300ff592f9d5b396a791d` (SHA-256). No product helper/host/UI source repair was delivered.

One native-only red run `native-fce078ae6c2b` first received a fixture report of **674×188 CSS at DPR 2**, while the first canvas had a **1348×376 bitmap** in a *transient* **674×376 CSS bounding rect**. No fixture viewport matched that rect within 30 seconds. At the end the actual surface/canvas CSS rect had settled to 674×188.47, but a later fixture report was **1348×376 CSS at DPR 1**, contradicting the accepted 674×188@2 request. The screenshot `WEB-06-native-only-geometry-red.png` (114,556 bytes; SHA-256 `01bbdec4eced9e3b950c3a7a2c075b0522fc598fda70bc445c82cf2e1e37d88b`) visually confirms the page at the native URL after this failed first-paint comparison; no animation, five-minute sampling or hide/show check was reached.

The next private native-only run `native-d2bef5ad6d80` used a read-only view-socket tap, before Commands Open, to record frame metadata without adding a view or ACKing packets. One native view socket was observed. The `attached` event and first **four descriptors**, same target `886D73757058C62DA932AF0DCBB9B6E6`, sequences 1–4, revision 1 at page `performance.now()` 3410–3634 ms, all specified **674×188 CSS / 1348×376 pixels**, matching the first two fixture reports of 674×188@2. A subsequent fixture report was **674×188 at DPR 1** about 29 seconds later. This run never presented a ready native frame: the canvas was black with **Showing last confirmed frame** and Retry, even though frame metadata had arrived; `WEB-06-native-only-stale-red.png` (50,448 bytes; SHA-256 `fd80812fecf0746bfdc31ee50d0212fd7969b694192c94f8f66777ea0ce653b2`) was visually inspected. `WEB-06-native-only-frame-trace.json` (2,595 bytes; SHA-256 `3ad86283527546af29fbf08306a6484882271586224b0953c4ea62c55f2459b4`) preserves exact descriptor/fixture timestamps and the separate clock bases.

The native-only mode proves that a gateway **pre-open** is not required to reproduce failed native first-paint/DPR behavior. It does **not** prove one helper process: the process-tree sampler did not account for Chromium/helper descendants, and one view socket is not one CDP owner. An experimental `window.__TAURI_INTERNALS__.invoke` tap saw no browser commands despite observed successful native navigation; it was removed, and its empty resize list is **not** evidence that no resize occurred. Neither a native UI ResizeObserver nor a competing CDP owner has been shown to issue a DPR1 override. The next discriminant is a temporary, run-owned helper CDP `Emulation.setDeviceMetricsOverride` send/result trace carrying helper PID/start identity, target binding, requested CSS dimensions/DPR, and monotonic time, correlated with these page reports; do not ship or attribute a repair before that trace. Both runs stopped their own Herdr, gateway, compositor, WebDriver, frontend, and fixture services; no active session or borrowed profile was touched. WEB-06 remains blocked on initial opening/physical presentation, repeated hide/show, active memory plateau, slow observer, reconnect, and full resource acceptance.
