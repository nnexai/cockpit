# Resume

Continue the complete handoff. Prioritize product changes; do not expand the terminal capture matrix now. Default Herdr and the manual Cockpit gateway at4173 remain protected.

## Verified increment

BOOT: `45ff8d03663da5fb4dc0a124b1325c38cea7cce3`. Finite requests/recovery: `62f55e0`. Temporal capture/replay tooling: `f16cb21`.

The current recovery increment passes 69 frontend tests, 108 workspace tests, typecheck and an isolated frontend build. The current AppImage starts and renders a newly created owned fixture through native session events. Browser proof covers visible sequence-gap recovery, GUI prefix consumption, settled ordinary input and copied text selection. Eight mounted App cases include a red-green stale session-list race. G02 records evidence and outstanding scope; do not mark all reliability repairs complete.

## Immediate priority: application mouse

OBS-009 asks to enable attach mouse handling and forward xterm interactions, following Herdr's working client. The concrete missing link is stable `ServerMessage::MouseCapture { enabled, sgr_pixels }`. Upstream client/mod.rs applies this control message; Cockpit terminal_wire.rs discards it with other auxiliary tags.

Propagate that control message through the shared terminal stream to xterm. Preserve application-driven enable/disable and ownership gating; let xterm reports use ordinary raw input. Do not invent full-app coordinate translation. An owned baseline fixture accepts keyboard `7a` but no SGR reports from application clicks. No temporal video is needed for this repair.

Then finish REPAIR-03 attachment identity/cancellation/queued input, REPAIR-04 cancellation reporting, REPAIR-05 validation/error/registry scope, and the remaining adapter corpus. Continue cleanup, contracts and every selected feature in timeline15; do not stop at terminal repairs.

## Evidence and runtime

Durable evidence: `/home/nnex/.local/state/cockpit-execution/run-20260904T214621Z/`. The paired scroll recordings are archived there; G01 still has open workloads/repetitions and application-mouse acceptance. Native temporal verification is excluded by the user's WebUI-first policy.

Homebrew Herdr0.8.2/protocol20 is pinned. The shadowing local executable is archived. Owned session: `run-20260904T214621Z-boot`; root `/tmp/cockpit-214621Z-gqhguyl2`. Gateway38339 serves `/tmp/cockpit-214621Z-gqhguyl2/finite-web-dist`; it still runs the finite-request backend until the next rebuild/restart. Current frontend bundle: `index-fTuv4hBQ.js`. Xvfb :191 and owned Chromium CDP43083 are available. The order fault proxy has exited. Native smoke is stopped; its current AppImage is retained as `Cockpit-recovery-0.1.0-amd64.AppImage`.

The fixture shell is now mode=mouse, capped at300s. The latest workspace was wP, label `repair-mouse-red-214621Z`. Prepare each driver before creating a fresh guarded workspace. `guarded_call` accepts only the command suffix: it adds the pinned binary/session itself.
