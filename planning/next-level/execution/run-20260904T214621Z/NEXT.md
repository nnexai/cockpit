# Resume

G00A passed: pinned stable/source baseline;17 safety guard tests; actual CLI allowed/default/foreign/omitted cases; independent Terra ACCEPT. Commit S00 owned state/profiles/guard files, then bootstrap runtime. No Herdr test server started yet.

BOOT Rust/frontend migration built. TypeScript+54frontend tests+browser build pass. Rust suite initially had one stale DTO protocol expectation, repaired;13protocol tests, Tauri compile, host binary build pass. Full Rust final run and runtime/browser proof remain. Main regenerated TS, removed image addon and obsolete reducer helper/graphics branch. Rust wire now emits many dead-code warnings from full stable enum copies; remove unnecessary wire types after real smoke before BOOT commit, not suppress warnings.

Use resources.json canonical guard for startup [server] and later fixed operations; session run-20260904T214621Z-boot is planned. Homebrew only. Source /home/nnex/.local/state/cockpit-execution/run-20260904T214621Z/herdr-0.8.2. Main must configure test-owned fixture shell to avoid user shell history.

LunaStartupInventory owns inventory collector, unfinished. Temporal fixture is settled, but decoder still fails actual5fps reproduction with NameError deadline and retains all fullRGB frames. Reassign bounded decoder/CLI repair. Saved before-video/report at /home/nnex/.local/state/cockpit-execution/run-20260904T214621Z. Application mouse remains G01 capability gap; click focus and scroll preserved by contract but not yet runtime-proved.

User overrides: WebUI-first full behavioral checks; native AppImage startup/simple smokes. Protected default untouched. Xvfb cockpit-run-xvfb ready :191; browser cockpit-run-bootstrap about:blank. Follow handoff, state and resources for complete selected scope.
