# Resume

Continue the complete handoff. The user explicitly said to continue, not pause. Prioritize product changes; do not expand the terminal capture matrix now.

## Current increment

BOOT is committed as `45ff8d03663da5fb4dc0a124b1325c38cea7cce3`.

Bounded Herdr request/process lifetimes and browser unknown-mutation recovery are verified and ready for their commit. G02 records 101 workspace tests, 62 final Herdr tests, 57 frontend tests, native compilation, and real CLI/browser red-green evidence. A dropped future has no durable cancellation/dispatch receipt yet; do not claim the whole REPAIR-04 or G02 complete.

Next implement REPAIR-01 shared stream ordering and mutation reconciliation alongside REPAIR-02 input routing. Main owns their shared App.tsx integration. Then REPAIR-03 and REPAIR-05, followed by cleanup, contracts and selected features in timeline15. Never stop at terminal tooling.

## User observations

OBS-007: pane focus works; application clicks do not. Scrolling is fine with no flicker. Content selection is unavailable. Otherwise usable. These are ground truth, not requests to repeat confirmation runs.

OBS-008: continue useful implementation, automation and maintenance toward the full goal. Avoid disproportionate verification work.

## Evidence and resources

Durable evidence: `/home/nnex/.local/state/cockpit-execution/run-20260904T214621Z/`.

The repaired temporal CLI produced a PASS text control and distinct scroll replay under `/tmp/cockpit-214621Z-gqhguyl2/`: `scroll-cli-pinned-control-replay.json` and `scroll-cli-paired-replay.json`. Scroll: 3601 frames/60s, 100 motion samples, 28ms p95 against 48ms control, two unchanged away-tail intervals. Preserve these files before cleaning the owned root. G01 still lacks remaining workloads/repetitions and application mouse support. Native temporal verification is excluded by OBS-004.

Homebrew Herdr 0.8.2/protocol20 is pinned. The shadowing local executable is archived. Protected default Herdr is untouched. `cockpit-web-real` at4173 is the user-authorized manual gateway; do not automate there or restart without authorization.

Owned session: `run-20260904T214621Z-boot`; root `/tmp/cockpit-214621Z-gqhguyl2`. `cockpit-run-web` at38339 now uses the rebuilt bounded backend and `/tmp/cockpit-214621Z-gqhguyl2/finite-web-dist`. Its readiness banner was mis-specified; actual log says `listening http://127.0.0.1:38339`, and HTTP status returned compatible. Xvfb :191 and owned Chromium CDP43083 are available. Native AppImage is stopped and remains the BOOT build. Fixtures have a strict 300s maximum. Prepare the driver before creating a fresh guarded workspace.

Uncommitted temporal Python tools are a separate increment. Do not absorb them into the bounded Herdr commit. Preserve their existing passed tests/evidence; no new recordings now.
