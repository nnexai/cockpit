# Daily development improvements

Requested 2026-09-06. Baseline worktree was clean. This plan covers the user's daily-use fixes, independently of unfinished historical next-level goals.

## Implementation and acceptance

1. Input reliability. Reproduce Ctrl+B, Shift, command and preserve prefix state across modifier-only events. Keep Escape cancellation and terminal input ownership. Reproduce mouse tab switching, fix the event/focus cause, and repeat mouse switching against acknowledged Herdr state. Add useful pane and tab navigation to the command help.
2. Files and Review navigation. Provide a keyboard tree/content focus switch and a searchable file picker with fuzzy path matching, arrows, Enter, and Escape. Keep searches bounded and scoped to the authorized pane root. Verify nested files, cancellation, empty results, and typing in comment editors.
3. Files presentation. Remove the Read-only badge. Open preview-capable documents in rendered mode and use one Source toggle. Plain text remains readable. Show each discovery warning once across the pane.
4. Discovery diagnostics. Report exhaustion once for the shared repository catalog budget and explain its current numeric value and TOML setting. Verify multiple roots and a low-budget fixture, plus a higher configured budget. Document the difference from directory listing and preview limits.
5. Native installation. Add a repeatable local build/install command with a stable desktop launcher and atomic executable updates. Verify installation and replacement in a disposable XDG tree, build the real native application, and smoke it against a disposable Herdr session. Preserve existing configuration and running sessions.

## Ownership and verification

Input worker owns App/keymap and their tests. Viewer worker owns context/review components and shared file navigation. Installer worker owns the new install script and documentation. Integration owner owns Rust catalog changes, shared manifests, planning, integration checks, runtime proof, review, and commits. No worker runs shared gates or commits.

Herdr remains authoritative for resource selection and terminal ownership. Files/Review navigation is local document focus. Modifier-only prefix persistence and additional shortcuts deliberately extend the desktop workflow. All runtime mutations target a run-owned disposable session; `default` is protected.

Gates: TypeScript check, focused frontend regression tests, catalog regression tests, installer tests, browser interaction through real gateway state, native build and launch. Record actual results here before delivery; compilation alone does not establish runtime acceptance.

## Status

All five implementation items are verified. The user clarified that pressing Shift cancels the armed prefix; the fix preserves modifiers, with no timer change.

## Results

- `bun run test`: 19 files, 132 tests passed. `bun run typecheck` passed. Focused regressions cover modifier sequences, queued focus ordering and reset/session boundaries, nested file picking, cancellation, focus trapping, preview comments, and duplicate warnings.
- `cargo test -p cockpit-core --lib`: 92 passed. The low-budget regression failed before the catalog fix with two warnings and passes with one; a larger configured budget removes exhaustion. `cargo fmt --all -- --check` passed.
- `python3 scripts/test_install_native.py`: 8 passed, including actual GLib desktop discovery and atomic replacement. Quoted TryExec was rejected by GLib before the fix. `desktop-file-validate` passed on the installed desktop entry.
- Real browser gateway with stable Herdr 0.8.2, protocol 20: 40 ordinary mouse tab switches passed. Delaying the first focus request by 400 ms reproduced the older-click-wins bug; after serialization, the latest clicked tab remains selected. This injected delay demonstrates the request-ordering failure, not a claim that every reported intermittent click had that cause.
- Real Files and Review extensions: Ctrl+B then Shift held for 1.2 seconds then T opens rename; Markdown defaults to preview; a single source toggle works; fuzzy lookup opens an unexpanded nested HTML file; local and prefix tree/content/picker shortcuts work; Review searches changed files and moves focus correctly. A forced one-entry catalog budget produces exactly one Files warning with its value and configuration key.
- `python3 scripts/install-native.py --debug --prefix /tmp/cdu/install`: built, installed, launched, and updated the actual native application. Under XWayland on niri, screenshots show the armed prefix during Shift and the resulting rename field. Ten actual mouse tab switches matched Herdr snapshots. Replacing the installed executable while it ran left the running process on its old inode and made the new inode available for future launches. No app restart or Herdr mutation was part of installation.
- Independent review found and prompted repairs for per-session focus ordering, cross-session blocking, Markdown draft visibility, and picker cancellation/focus behavior. No remaining known material finding in the delivered scope.

Browser captures use 1440 by 900. Native captures use the compositor's tiled size and display scaling. Native event injection used X11; this does not establish a separate Wayland input-automation result. Existing Vite chunk-size and Tauri identifier advisories remain build warnings.

## Evidence and cleanup

Baseline: `7084603beeb5261fbefd7b658496767a1d716983`, clean worktree. The compact evidence manifest beside this plan records source and artifact hashes. Runtime captures and reproducible scripts are under `/tmp/cdu`; they are temporary and excluded from Git.

Only run-owned session `daily`, its workspace/tabs/plugin panes, loopback gateway, copied plugin registry, and disposable native installation were used. The native app and gateway were stopped, the session was stopped through the guarded helper, and the disposable installation was uninstalled. Test fixture data and evidence remain under `/tmp/cdu`. The protected `default` session and the user's native process were not changed.

Installation and updates: `bun run native:install --debug`. This builds and installs to the user's XDG paths when they run it; this task exercised the same installer in a disposable prefix. See `docs/native-install.md`, `docs/keyboard-shortcuts.md`, and `docs/discovery-limits.md`.
