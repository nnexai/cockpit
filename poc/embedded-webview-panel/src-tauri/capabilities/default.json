{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the embedded native WebView panel POC",
  "windows": ["main"],
  "permissions": ["core:default"]
}
