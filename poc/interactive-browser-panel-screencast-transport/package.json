{
  "name": "interactive-browser-panel-transport-poc",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bun install --frozen-lockfile && bunx playwright install chromium",
    "dev": "cargo run --manifest-path src-tauri/Cargo.toml",
    "dev:wayland": "./scripts/run-wayland.sh",
    "build": "cargo build --manifest-path src-tauri/Cargo.toml",
    "smoke": "node helper/smoke.mjs",
    "screencast-smoke": "node helper/screencast-smoke.mjs"
  },
  "dependencies": {
    "playwright": "1.63.0"
  }
}
